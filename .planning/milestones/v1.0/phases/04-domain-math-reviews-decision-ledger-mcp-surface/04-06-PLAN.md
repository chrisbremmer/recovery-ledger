---
phase: 04-domain-math-reviews-decision-ledger-mcp-surface
plan: 06
type: execute
wave: 2
depends_on:
  - 04-02
  - 04-05
files_modified:
  - src/infrastructure/db/repositories/decisions.repo.ts
  - src/infrastructure/db/repositories/decisions.repo.test.ts
  - src/services/decision/index.ts
  - src/services/decision/index.test.ts
  - src/services/api-gap/data.ts
  - src/services/api-gap/index.ts
  - src/services/api-gap/index.test.ts
autonomous: true
requirements:
  - DEC-01
  - DEC-02
  - DEC-03
  - MCP-01

must_haves:
  truths:
    - "decisions.repo extends with updateOutcome (DEC-02), countSince (D-22 for weekly decision_prompt gating), findByPrefix (D-20 short-prefix lookup), and listAll (D-20 --all flag); all writes use db.transaction({behavior: 'immediate'}) per Phase 3 Pitfall 13"
    - "services.addDecision composes ulid() + decisionsRepo.insert() + decisionsRepo.byId() with smart defaults per D-19 (category default 'general', confidence null, expectedEffect null, followUpDate null OR caller-resolved ISO)"
    - "services.reviewDecisions dispatches on mode discriminator (D-21): mode='list' calls listOpen/listAll; mode='update' calls updateOutcome + byId"
    - "services.getApiGap returns { entries: ApiGapEntry[] } from in-source API_GAP_ENTRIES const per D-28; v1 list covers Healthspan + ECG + BP + journal + continuous HR + hormonal insights (6 named in REQUIREMENTS Out of Scope) plus any others discovered"
    - "Zero new migrations — schema audit in 04-RESEARCH.md §Decision Ledger Persistence confirms decisions table already has every DEC-01/02 column"
  artifacts:
    - path: "src/infrastructure/db/repositories/decisions.repo.ts"
      provides: "Extended with updateOutcome, countSince, findByPrefix, listAll methods"
      contains: "updateOutcome"
    - path: "src/services/decision/index.ts"
      provides: "addDecision, reviewDecisions, updateDecision orchestration"
      min_lines: 80
    - path: "src/services/api-gap/data.ts"
      provides: "API_GAP_ENTRIES module-load const (≥6 entries per D-28)"
      contains: "API_GAP_ENTRIES"
    - path: "src/services/api-gap/index.ts"
      provides: "getApiGap() service accessor"
      min_lines: 12
  key_links:
    - from: "src/services/decision/index.ts"
      to: "ulid (npm), src/infrastructure/db/repositories/decisions.repo.ts"
      via: "ulid() generation BEFORE repo.insert(); repo.byId() round-trip read for return value"
      pattern: "ulid|decisionsRepo"
    - from: "src/infrastructure/db/repositories/decisions.repo.ts"
      to: "drizzle-orm (transactions, eq, gte, desc, sql)"
      via: "db.transaction({behavior: 'immediate'}) for write paths; eq + desc + sql LIKE for read paths"
      pattern: "db\\.transaction|behavior:\\s*'immediate'"
---

<objective>
Extend `decisions.repo.ts` with four new methods (DEC-02 + D-20 + D-22) and ship the decision service orchestration layer + the API gap data + service accessor. Zero new migrations: the Phase 3 schema audit confirms every DEC-01/02 column already exists (per 04-RESEARCH.md §Decision Ledger Persistence).

Purpose: this plan unlocks all decision CLI commands (Wave 4 Plan 04-11) + the `whoop_add_decision` + `whoop_review_decisions` MCP tools (Wave 4 Plan 04-10) + the `decision_prompt` slot computation in the weekly service (Wave 2 next plan via `countSince`). DEC-01 + DEC-02 + DEC-03 anchor here at the service layer. API gap data ships here so it is available to `whoop_api_gap` tool + `whoop://api-gaps` resource (both in Wave 4).

The repo extension is additive — `insert`, `byId`, `listOpen` from Phase 3 stay untouched per the existing repo contract; the 4 new methods append to the `DecisionsRepo` interface.

Output: 1 extended repo file + 1 decision service file + 1 api-gap data file + 1 api-gap service file + 3 test files. Bootstrap wiring lands in Plan 04-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md
@agent_docs/decisions/0003-score-state-discipline.md
@src/infrastructure/db/repositories/decisions.repo.ts
@src/infrastructure/db/repositories/cycles.repo.ts
@src/services/decision/types.ts
@src/services/api-gap/types.ts
@src/domain/types/entities.ts

<interfaces>
Phase 3 existing repo (src/infrastructure/db/repositories/decisions.repo.ts lines 18-36):
- `DecisionsRepo` interface with: `insert(d) → void`, `byId(id) → Decision | null`, `listOpen() → Decision[]`.
- `rowToDecision(row): Decision` mapper at line 81 — reuse for the 4 new methods.

Phase 3 entity type (src/domain/types/entities.ts):
- `Decision = { id; createdAt; category; decision; rationale; confidence; expectedEffect; followUpDate; status; outcomeNotes }`.

Wave 0 service types (Plan 04-02):
- `AddDecisionInput = { decision: string; category?; rationale?; confidence?; expectedEffect?; followUpDate?: string }`.
- `ReviewDecisionsInput = { mode: 'list'; includeAll? } | { mode: 'update'; id; status; notes? }`.
- `ReviewDecisionsResult = { mode: 'list'; decisions } | { mode: 'update'; decision }`.
- `ApiGapEntry = { feature; whoop_consumer_path; available_via_v2_api: false; alternative_via_v2; notes }`.

ULID (npm, installed Plan 04-01):
- `import { ulid } from 'ulid'` — `ulid()` returns a 26-char uppercase Crockford Base32 string; lexicographically sortable.

D-28 v1 API gap list:
1. Healthspan — WHOOP app → Health Monitor → Healthspan
2. ECG — WHOOP app → Heart → ECG
3. Blood Pressure — WHOOP app → Heart → Blood Pressure
4. Journal — WHOOP app → Journal
5. Continuous Heart Rate — WHOOP app → Heart → Continuous HR
6. Hormonal Insights — WHOOP app → Health Monitor → Hormonal Insights
(Plus any others discovered during execution — the planner should review WHOOP's consumer-app feature list against the v2 API endpoints. Default minimum is these 6 named in REQUIREMENTS §Out of Scope.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend decisions.repo.ts with updateOutcome, countSince, findByPrefix, listAll</name>
  <files>src/infrastructure/db/repositories/decisions.repo.ts, src/infrastructure/db/repositories/decisions.repo.test.ts</files>
  <behavior>
    - `updateOutcome(id: string, status: 'open'|'followed_up'|'abandoned', notes: string | null): void` — updates row by id; uses `db.transaction({behavior: 'immediate'})` (Phase 3 Pitfall 13).
    - `updateOutcome` is idempotent: updating with the same status + notes twice is a no-op write.
    - `updateOutcome` of a non-existent id silently no-ops (drizzle's update where match=0 returns 0 rows changed; surfacing as an error is the caller's job — the service layer checks via byId after).
    - `countSince(date: string): number` — returns count of decisions with `created_at >= date` (D-22 weekly-prompt gating; `date` is ISO yyyy-mm-dd OR full ISO timestamp; SQLite lexicographic compares work on ISO strings).
    - `findByPrefix(prefix: string): Decision[]` — case-insensitive prefix match against `id` using `LIKE upperCasePrefix || '%'`; returns all matches mapped to Decision[] via rowToDecision.
    - `listAll(): Decision[]` — returns ALL decisions regardless of status, newest first (created_at DESC).
  </behavior>
  <read_first>
    - @src/infrastructure/db/repositories/decisions.repo.ts (the existing Phase 3 stub — extend lines 18-36 interface + lines 40-79 factory; rowToDecision at line 81 reused verbatim)
    - @src/infrastructure/db/repositories/cycles.repo.ts (Phase 3 D-04 default-filter + transaction({behavior: 'immediate'}) precedent; the `eq` + `desc` drizzle imports)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/infrastructure/db/repositories/decisions.repo.ts extension (lines 1138-1196 — the verbatim method bodies)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Decision Ledger Persistence §Repo audit (confirms which methods to add + the SQLite LIKE prefix-scan note)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-20 (CLI decision-update prefix lookup; D-21 dual-mode whoop_review_decisions) + D-22 (countSince for weekly prompt)
    - @agent_docs/conventions.md (no default exports; strict TS; comments only when why isn't obvious)
  </read_first>
  <action>
    Extend `src/infrastructure/db/repositories/decisions.repo.ts`:
    1. Append 4 method signatures to the `DecisionsRepo` interface (lines 18-36) — `updateOutcome`, `countSince`, `findByPrefix`, `listAll`.
    2. Update the imports: add `gte` and `sql` to the drizzle-orm import line (already imports `desc`, `eq`).
    3. Implement the 4 methods in the `createDecisionsRepo` factory body verbatim per 04-PATTERNS.md §Phase 4 new methods:
       - `updateOutcome`: `db.transaction((tx) => tx.update(decisionsTable).set({ status, outcome_notes: notes }).where(eq(decisionsTable.id, id)).run(), { behavior: 'immediate' })`. The Phase 3 transaction-behavior discipline (Pitfall 13) — explicit `behavior: 'immediate'` — is non-negotiable here.
       - `countSince`: `db.select({ n: sql<number>`COUNT(*)` }).from(decisionsTable).where(gte(decisionsTable.created_at, date)).get(); return row?.n ?? 0`.
       - `findByPrefix`: `db.select().from(decisionsTable).where(sql`${decisionsTable.id} LIKE ${prefix.toUpperCase() + '%'}`).all().map(rowToDecision)`. The ULID Crockford Base32 alphabet is uppercase so `prefix.toUpperCase()` normalizes user input.
       - `listAll`: `db.select().from(decisionsTable).orderBy(desc(decisionsTable.created_at)).all().map(rowToDecision)`.
    4. Update the file header comment (lines 1-11) to reflect "Phase 4 extends with 4 methods" — replace the "Phase 4 will extend with..." stub note. Avoid the literal `drizzle-orm` substring in comments (Phase 1-3 convention; phrase as "the ORM" / "drizzle imports above").
    Create `src/infrastructure/db/repositories/decisions.repo.test.ts` (currently absent — the Phase 3 stub did not ship a test file; this plan fills the gap per VALIDATION.md):
    - Use `tests/helpers/in-memory-db.ts` (Phase 3 helper) for SQLite-in-RAM with migrations applied.
    - Tests:
      - insert + byId round-trip (verifies Phase 3 stub still works).
      - insert + listOpen returns the new row (Phase 3 carry-forward).
      - insert + updateOutcome to 'followed_up' + notes → byId returns updated status + notes.
      - updateOutcome of a non-existent id is silent (no throw, no row changed).
      - Repeated updateOutcome with same args is idempotent.
      - countSince — insert 3 decisions on different dates; countSince(middleDate) returns 2 (decisions ON or AFTER the date).
      - countSince(futureDate) returns 0.
      - countSince(epochStart) returns total count.
      - findByPrefix — insert 2 decisions whose ULIDs start with `01HK7`; findByPrefix('01HK7') returns both; findByPrefix('01hk7') (lowercase) returns both (case-insensitive); findByPrefix('NOMATCH') returns [].
      - findByPrefix with prefix matching exactly 1 → returns 1-element array (caller does ambiguity check).
      - listAll — insert 3 decisions; mark one followed_up; listAll returns all 3; listOpen returns only the 2 open.
  </action>
  <verify>
    <automated>npx vitest run src/infrastructure/db/repositories/decisions.repo.test.ts</automated>
  </verify>
  <done>
    Repo file has 7 methods total (3 Phase 3 + 4 new). Test file green with ≥ 10 assertions covering each new method. All writes go through `db.transaction({behavior: 'immediate'})`. The `prefix.toUpperCase()` discipline anchors Pitfall 11 + Pitfall 13 prevention. Phase 3 byId/listOpen tests still pass (sanity).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: services/decision orchestration — addDecision, reviewDecisions, updateDecision</name>
  <files>src/services/decision/index.ts, src/services/decision/index.test.ts</files>
  <behavior>
    - `addDecision(input, deps): Promise<Decision>` — generates ULID, defaults category to 'general' if absent, passes to `decisionsRepo.insert()`, reads back via `byId(id)` for return.
    - `reviewDecisions(input, deps): Promise<ReviewDecisionsResult>` — dispatches on `input.mode`:
      - `mode='list'`: if `input.includeAll` then call `listAll()` else `listOpen()`; wrap as `{ mode: 'list', decisions }`.
      - `mode='update'`: call `updateOutcome(input.id, input.status, input.notes ?? null)` then `byId(input.id)` → if null throw `new Error('decision not found: ' + sanitize(input.id))`; else wrap as `{ mode: 'update', decision }`.
    - `updateDecision(input, deps): Promise<Decision>` — convenience CLI wrapper that delegates to `reviewDecisions({mode: 'update', ...input})` and unwraps the result.
    - Pitfall 17 carry-forward: no decision text logged inline; only `{ event: 'decision_added', id }` style structured logs (id is non-PII).
    - Pitfall 9 (ADR-0001): no `console.*`; use `logger.info({ event, ... })` from `src/infrastructure/config/logger.js`.
  </behavior>
  <read_first>
    - @src/services/decision/types.ts (Wave 0 contracts — AddDecisionInput, ReviewDecisionsInput, ReviewDecisionsResult, UpdateDecisionInput)
    - @src/services/sync/index.ts (canonical service orchestrator shape — Deps + Input + Result triple; clock + logger injection)
    - @src/infrastructure/db/repositories/decisions.repo.ts (extended in Task 1)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-19 (decision-add field defaults) + D-20 (decision review/update; --all flag → listAll vs listOpen) + D-21 (dual-mode reviewDecisions)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/services/decision/index.ts §Excerpt template (lines 583-617 — the verbatim ulid + insert + byId pattern)
    - @agent_docs/decisions/0001-mcp-stdout-purity.md (no console.* anywhere in MCP-reachable services)
    - @tests/helpers/in-memory-db.ts (Phase 3 helper for tests)
  </read_first>
  <action>
    Create `src/services/decision/index.ts`:
    ```
    import { ulid } from 'ulid';
    import type { Logger } from 'pino';
    import type { Decision } from '../../domain/types/entities.js';
    import type { DecisionsRepo } from '../../infrastructure/db/repositories/decisions.repo.js';
    import type { AddDecisionInput, ReviewDecisionsInput, ReviewDecisionsResult, UpdateDecisionInput } from './types.js';
    ```
    Export 3 service functions:
    - `addDecision(input: AddDecisionInput, deps: { repos: { decisions: DecisionsRepo }; clock: () => Date; logger: Logger }): Promise<Decision>`. Body per 04-PATTERNS.md §Excerpt template: `const id = ulid()` → `deps.repos.decisions.insert({ id, createdAt: deps.clock().toISOString(), category: input.category ?? 'general', decision: input.decision, rationale: input.rationale ?? null, confidence: input.confidence ?? null, expectedEffect: input.expectedEffect ?? null, followUpDate: input.followUpDate ?? null })` → `const created = deps.repos.decisions.byId(id); if (created === null) throw new Error('addDecision: insert succeeded but byId returned null for ' + id)` → `deps.logger.info({ event: 'decision_added', id, category: created.category })` (NO decision text in log per Pitfall 17) → `return created`.
    - `reviewDecisions(input: ReviewDecisionsInput, deps: { repos: { decisions: DecisionsRepo }; logger: Logger }): Promise<ReviewDecisionsResult>`. Dispatch on `input.mode`:
      - `'list'`: `const decisions = input.includeAll === true ? deps.repos.decisions.listAll() : deps.repos.decisions.listOpen(); return { mode: 'list', decisions }`.
      - `'update'`: `deps.repos.decisions.updateOutcome(input.id, input.status, input.notes ?? null); const decision = deps.repos.decisions.byId(input.id); if (decision === null) throw new Error('reviewDecisions: id not found after update: ' + input.id); deps.logger.info({ event: 'decision_updated', id: input.id, status: input.status }); return { mode: 'update', decision }`.
    - `updateDecision(input: UpdateDecisionInput, deps): Promise<Decision>`. Calls `reviewDecisions({ mode: 'update', ...input }, deps)` and returns `result.decision` (narrows via discriminator).
    Create `src/services/decision/index.test.ts` using the in-memory-db helper. Test cases:
    - `addDecision({ decision: 'sleep more' })` — returns a Decision with category='general', valid ULID id, createdAt close to test clock.
    - `addDecision` with all fields explicit — every field round-trips through repo.
    - `addDecision` injects a fake clock and asserts createdAt matches.
    - `reviewDecisions({ mode: 'list' })` after 2 adds + 1 update-to-followed_up — returns only the 1 open.
    - `reviewDecisions({ mode: 'list', includeAll: true })` — returns all 3.
    - `reviewDecisions({ mode: 'update', id: <full-id>, status: 'followed_up', notes: 'worked well' })` — returns `{ mode: 'update', decision }` with updated fields.
    - `reviewDecisions({ mode: 'update', id: 'NOT_A_REAL_ID', status: 'open' })` — throws Error.
    - T-04-S2 SQL-injection-style payload: `addDecision({ decision: "'; DROP TABLE decisions; --" })` — decision text round-trips unchanged; subsequent listOpen still returns the row (drizzle's prepared statements neutralize the payload).
    - T-04-S2 shell-metacharacter payload: `addDecision({ decision: "$(rm -rf /)" })` — text round-trips unchanged (Commander argv discipline + drizzle prepared statements; verify in test).
    File MUST avoid `console.*` (ADR-0001) — use the Pino logger imported from `src/infrastructure/config/logger.js`; tests use a dummy logger.
  </action>
  <verify>
    <automated>npx vitest run src/services/decision/index.test.ts</automated>
  </verify>
  <done>
    3 service functions exported. Test file green with ≥ 9 assertions including 2 T-04-S2 fixtures (SQL-injection + shell-metacharacters). ULID generation lives in the service before repo write. Phase 1 sanitize discipline carries through structured logs (no decision text logged).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: services/api-gap data + accessor</name>
  <files>src/services/api-gap/data.ts, src/services/api-gap/index.ts, src/services/api-gap/index.test.ts</files>
  <behavior>
    - `data.ts` exports `API_GAP_ENTRIES: readonly ApiGapEntry[]` with ≥ 6 entries (the v1 named list from REQUIREMENTS §Out of Scope per D-28). Each entry has `available_via_v2_api: false` (literal type).
    - `index.ts` exports `getApiGap(): Promise<ApiGapResult>` returning `{ entries: API_GAP_ENTRIES }`.
    - Every entry's `feature` + `whoop_consumer_path` + `notes` passes the banned-word lint at module-load (test calls `containsBannedToneToken` over each string field).
    - No "medical advice" language (REQUIREMENTS Out of Scope); notes are factual descriptions of what the feature does + why it's API-unavailable.
  </behavior>
  <read_first>
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-28 (in-source catalog shape; v1 6-entry minimum; Phase 5 will generate markdown from this)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/services/api-gap/data.ts + §src/services/api-gap/index.ts entries
    - @.planning/REQUIREMENTS.md §Out of Scope (the 6 named consumer-app features that are unavailable via v2 API)
    - @src/services/api-gap/types.ts (Wave 0 ApiGapEntry + ApiGapResult)
    - @src/domain/banned-words.ts (Wave 0 lint utility)
  </read_first>
  <action>
    Create `src/services/api-gap/data.ts`:
    - `import type { ApiGapEntry } from './types.js';`
    - `export const API_GAP_ENTRIES: readonly ApiGapEntry[] = Object.freeze([` followed by ≥ 6 entries per the D-28 list:
      1. `{ feature: 'Healthspan', whoop_consumer_path: 'WHOOP app → Health Monitor → Healthspan', available_via_v2_api: false, alternative_via_v2: 'closest proxy: long-run trends in recovery_score', notes: 'WHOOP-only composite score combining recovery, strain, sleep, and other inputs. Not exposed on the public v2 API.' }`,
      2. `{ feature: 'ECG (electrocardiogram)', whoop_consumer_path: 'WHOOP app → Heart → ECG', available_via_v2_api: false, alternative_via_v2: null, notes: 'Single-lead ECG capture for atrial-fibrillation detection. Raw waveforms are not exposed on the v2 API.' }`,
      3. `{ feature: 'Blood Pressure', whoop_consumer_path: 'WHOOP app → Heart → Blood Pressure', available_via_v2_api: false, alternative_via_v2: null, notes: 'Cuffless blood-pressure estimation. Not exposed on the v2 API.' }`,
      4. `{ feature: 'Journal', whoop_consumer_path: 'WHOOP app → Journal', available_via_v2_api: false, alternative_via_v2: null, notes: 'Daily lifestyle-factor logging (alcohol, caffeine, etc.). Not exposed on the v2 API.' }`,
      5. `{ feature: 'Continuous Heart Rate', whoop_consumer_path: 'WHOOP app → Heart → Continuous HR', available_via_v2_api: false, alternative_via_v2: 'closest proxy: cycle.day_strain reflects HR-derived load over the day', notes: 'Second-by-second heart-rate stream. The v2 API exposes summary fields per cycle but not the raw stream.' }`,
      6. `{ feature: 'Hormonal Insights', whoop_consumer_path: 'WHOOP app → Health Monitor → Hormonal Insights', available_via_v2_api: false, alternative_via_v2: null, notes: 'Menstrual-cycle phase tracking with strain/recovery overlays. Not exposed on the v2 API.' }`,
    - The `Object.freeze` + `as const`-equivalent discipline anchors immutability at the type + runtime level.
    Create `src/services/api-gap/index.ts`:
    ```
    import type { ApiGapResult } from './types.js';
    import { API_GAP_ENTRIES } from './data.js';
    export async function getApiGap(): Promise<ApiGapResult> {
      return { entries: API_GAP_ENTRIES };
    }
    ```
    (Async return for service-interface uniformity; the data is module-load constant so no async work happens.)
    Create `src/services/api-gap/index.test.ts`:
    - `getApiGap()` returns `entries.length >= 6`.
    - Every entry has `available_via_v2_api === false` (literal type lock).
    - `Object.isFrozen(entries) === true` (mutation prevention).
    - **One assert per entry × per string field**: `containsBannedToneToken(entry.feature).hit === false`, `containsBannedToneToken(entry.whoop_consumer_path).hit === false`, `containsBannedToneToken(entry.notes).hit === false`, and when `entry.alternative_via_v2 !== null`, `containsBannedToneToken(entry.alternative_via_v2).hit === false`. (D-26 source-layer lint — Wave 3 contract test extends to rendered output.)
    - At least one entry covers each of the 6 named features from REQUIREMENTS Out of Scope (Healthspan, ECG, Blood Pressure, Journal, Continuous HR, Hormonal Insights).
    No `console.*` anywhere; pure data + accessor.
  </action>
  <verify>
    <automated>npx vitest run src/services/api-gap/index.test.ts</automated>
  </verify>
  <done>
    Data file has ≥ 6 entries matching the D-28 list. Service accessor returns the frozen array. Tests assert tone-lint, immutability, and the 6 named-feature coverage. Phase 5 (DOC-03/04) will read this same module to generate markdown.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv / MCP tool input → decision service → DB | T-04-S2 — user-controlled decision text + notes. Commander's argv array discipline + drizzle's prepared statements neutralize SQL injection + shell metacharacters. |
| services.decision → logger | Decision text is PII-adjacent — never logged inline. Structured logs use only `id` + `category` + non-text fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-S2 | Injection | `src/services/decision/index.ts`, `src/infrastructure/db/repositories/decisions.repo.ts` | mitigate | Decision text + notes pass through drizzle's prepared statements (`tx.update().set(...).where(eq(...))`). Test fixtures with SQL-injection (`'; DROP TABLE decisions; --`) and shell-metacharacter (`$(rm -rf /)`) payloads assert the strings round-trip unchanged and the DB is intact after. Commander argv passes arrays (no shell expansion) — verified by the CLI test in Wave 4. |
| T-04-S3 | Information Disclosure | `src/services/decision/index.ts` log lines | mitigate | Per Pitfall 17 + ADR-0001: NO `console.*`, NO decision text in `logger.info({event, id, category})`. The id (ULID) is non-PII; category is a label not a payload. |
</threat_model>

<verification>
```sh
npx vitest run src/infrastructure/db/repositories/decisions.repo.test.ts
npx vitest run src/services/decision/index.test.ts
npx vitest run src/services/api-gap/index.test.ts

# Full Wave 2 group not yet present (Plan 04-07 + 04-08 land after) but these pass standalone:
npx vitest run src/services/decision/ src/services/api-gap/ src/infrastructure/db/

# Gates green
bash scripts/ci-grep-gates.sh

# tsc green
npx tsc --noEmit
```
</verification>

<success_criteria>
- `decisions.repo.ts` extended with 4 methods; sibling test file ships green with full coverage including SQL-injection / shell-metacharacter fixtures.
- `services/decision/index.ts` ships 3 functions; ULID generated at service layer before repo insert (per D-19).
- `services/api-gap/data.ts` ships ≥ 6 entries with literal `available_via_v2_api: false`; all string fields pass banned-word lint at module-load.
- Zero new migrations (schema audit confirmed in 04-RESEARCH.md).
- No `console.*` anywhere; all 10 grep gates green.
- DEC-01 + DEC-02 + DEC-03 + MCP-01 (api-gap data) anchored at service layer; Wave 4 surfaces compose.
</success_criteria>

<output>
Create `.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-06-SUMMARY.md` when done summarizing: repo extended with 4 methods, decision service + api-gap service shipped, T-04-S2 mitigations in test, DEC-01/02/03 service-layer ready.
</output>
