---
phase: 04-domain-math-reviews-decision-ledger-mcp-surface
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - scripts/ci-grep-gates.sh
  - src/mcp/register-resource.ts
  - src/mcp/register-prompt.ts
  - src/domain/banned-words.ts
  - tests/contract/formatter-tone.test.ts
  - tests/contract/mcp-tool-shape.test.ts
  - tests/contract/mcp-resource-shape.test.ts
  - tests/contract/mcp-prompt-shape.test.ts
  - tests/contract/mcp-shim-loc.test.ts
  - tests/contract/daily-review-shape.test.ts
autonomous: true
requirements:
  - REV-08
  - MCP-02
  - MCP-03
  - MCP-04
  - MCP-05
  - MCP-06
user_setup: []

must_haves:
  truths:
    - "ulid + simple-statistics installed and pinned via npm semver (D-19 ULID source, D-15/D-14 stats source)"
    - "D-36 register-resource.ts wrapper exists and is the ONLY call site of server.registerResource"
    - "D-36 register-prompt.ts wrapper exists and is the ONLY call site of server.registerPrompt"
    - "Gate H refuses `tools.length === 1` outside tests/__legacy__/ (D-33)"
    - "Gate I refuses `server.registerResource(` outside src/mcp/register-resource.ts (D-36)"
    - "Gate J refuses `server.registerPrompt(` outside src/mcp/register-prompt.ts (D-36)"
    - "BANNED_TONE_WORDS const exported from src/domain/banned-words.ts is the single source of truth for ADR-0005 banned tokens (consumed by Gate A regex + D-26 contract test)"
    - "Five contract-test scaffolds exist as failing/pending tests so Wave 4 / Wave 3 plans fill them in (formatter-tone, mcp-tool-shape, mcp-resource-shape, mcp-prompt-shape, mcp-shim-loc, daily-review-shape)"
  artifacts:
    - path: "src/mcp/register-resource.ts"
      provides: "Sanitize-wrapped server.registerResource"
      contains: "registerResource"
    - path: "src/mcp/register-prompt.ts"
      provides: "Sanitize-wrapped server.registerPrompt"
      contains: "registerPrompt"
    - path: "src/domain/banned-words.ts"
      provides: "Banned-tone-words tuple (ADR-0005 SoT for code-callable form)"
      contains: "BANNED_TONE_WORDS"
    - path: "scripts/ci-grep-gates.sh"
      provides: "Gates H + I + J added"
      contains: "Gate H"
    - path: "tests/contract/formatter-tone.test.ts"
      provides: "D-26 banned-word lint over rendered output (scaffold; populated by formatter plan)"
      contains: "BANNED_TONE_WORDS"
    - path: "tests/contract/mcp-tool-shape.test.ts"
      provides: "MCP-02 dual-shape contract scaffold"
      contains: "structuredContent"
    - path: "tests/contract/mcp-resource-shape.test.ts"
      provides: "MCP-04 resource read shape scaffold"
      contains: "contents"
    - path: "tests/contract/mcp-prompt-shape.test.ts"
      provides: "MCP-05 messages-array shape scaffold"
      contains: "messages"
    - path: "tests/contract/mcp-shim-loc.test.ts"
      provides: "MCP-03 ≤5-line shim static analysis scaffold"
      contains: "5"
    - path: "tests/contract/daily-review-shape.test.ts"
      provides: "REV-03/04 data-status-leads scaffold"
      contains: "data_status"
  key_links:
    - from: "scripts/ci-grep-gates.sh"
      to: "src/domain/banned-words.ts"
      via: "shared banned-word list (Gate A uses inline regex; bash script verifies in-sync with TS export — Wave 1 banned-words.test.ts asserts parity)"
      pattern: "BANNED_TONE_WORDS"
    - from: "src/mcp/register-resource.ts"
      to: "src/mcp/sanitize.ts"
      via: "sanitize + serializeError on error path; sanitize string leaves on success path"
      pattern: "sanitize|serializeError"
    - from: "src/mcp/register-prompt.ts"
      to: "src/mcp/sanitize.ts"
      via: "sanitize + serializeError on error path; sanitize message.content.text on success path"
      pattern: "sanitize|serializeError"
---

<objective>
Land Phase 4's Wave 0 infrastructure: install the two new npm dependencies (`ulid`, `simple-statistics`), ship the D-36 sanitizer wrappers (`register-resource.ts` + `register-prompt.ts`) so subsequent waves can register MCP resources + prompts safely, extract the ADR-0005 banned-word list to a TS-callable constant for D-26 defence-in-depth, extend `scripts/ci-grep-gates.sh` with Gates H / I / J, and create the six new contract-test scaffolds Wave 3 / Wave 4 plans fill in.

Purpose: every later wave assumes these are in place. Domain stats imports `simple-statistics`; the decision service imports `ulid`; every MCP resource + prompt registration calls the new wrappers; the formatter tone contract test imports the banned-word constant; the MCP attestation transition (D-29) requires Gate H to refuse the old `tools.length === 1` assertion. Without Wave 0, every subsequent plan blocks.

Output: deps installed, two wrapper files, one shared banned-word constant, three new grep gates, six contract-test scaffolds. No business logic ships in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-VALIDATION.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md
@agent_docs/decisions/0001-mcp-stdout-purity.md
@agent_docs/decisions/0005-banned-tone-words.md
@src/mcp/register.ts
@src/mcp/sanitize.ts
@scripts/ci-grep-gates.sh
@package.json

<interfaces>
<!-- Key types and contracts. Wave 0 wrappers mirror src/mcp/register.ts:60-82 — read that file in this wave. -->

The existing tool wrapper `register()` at src/mcp/register.ts:60-82 is the verbatim shape both new wrappers must mirror, varying only in:
- SDK call: `server.registerResource(name, uri, metadata, handler)` vs `server.registerPrompt(name, config, handler)`
- Success-path return shape:
  - Resource: `{ contents: Array<{ uri: string; text: string; mimeType?: string }> }`
  - Prompt: `{ messages: Array<{ role: 'user' | 'assistant'; content: { type: 'text'; text: string } | { type: 'image'; data: string; mimeType: string } }> }`
- Sanitize walker target:
  - Resource: walk `contents[].text` strings
  - Prompt: walk `messages[].content.text` strings

`sanitize` + `serializeError` are exported from `src/mcp/sanitize.ts` and consumed UNMODIFIED (D-30).

ADR-0005 banned tone words (CI-enforced list, per scripts/ci-grep-gates.sh:96-97 TONE_WORDS_RE):
  `optimize`, `wellness`, `honor`, `journey`, `crush`, `nail`, `tune`, `vibe`, `unlock`, `dial in`
plus any emoji codepoint per Gate A's `EMOJI_RE` byte-level pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ulid + simple-statistics; verify package legitimacy</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Standard Stack §Supporting + §Package Legitimacy Audit (verifies versions on npm registry + zero-dep confirmation)
    - @package.json (current state — confirms neither dep is installed)
    - @agent_docs/decisions/0006-fixture-only-tests.md (no live network in tests; both new deps must be installable offline-safe)
  </read_first>
  <action>
    Run `npm install ulid@^3.0.2 simple-statistics@^7.8.9` from the repo root. Before installing, run `npm view ulid version` + `npm view ulid dependencies` + `npm view simple-statistics version` (per 04-RESEARCH.md §Standard Stack version-verification block); confirm `ulid` version is `^3.0.2`, `ulid` dependencies list is empty (zero-dep per A1 / A2), and `simple-statistics` is `^7.8.9`. These are the [VERIFIED] approved packages — no legitimacy checkpoint task required (per 04-RESEARCH.md §Package Legitimacy Audit table, both packages are long-tenured ecosystem staples since 2016 / 2012 with Approved disposition). Do NOT add any other dependencies in this task. Verify the resulting `node_modules/simple-statistics/` exports `median`, `medianAbsoluteDeviation`, `wilcoxonRankSum`, `cumulativeStdNormalProbability` by reading the package's `index.d.ts` (Assumption A2 in 04-RESEARCH.md flagged this for verification at plan time). If `cumulativeStdNormalProbability` is absent, abort and fall back to hand-rolling Abramowitz & Stegun in a follow-up plan — do not silently work around.
  </action>
  <verify>
    <automated>node -e "import('ulid').then(m => { if (typeof m.ulid !== 'function') throw new Error('ulid: ulid() missing'); }); import('simple-statistics').then(m => { for (const fn of ['median','medianAbsoluteDeviation','wilcoxonRankSum','cumulativeStdNormalProbability']) if (typeof m[fn] !== 'function') throw new Error('simple-statistics: ' + fn + ' missing'); });"</automated>
  </verify>
  <done>
    `package.json` lists `ulid@^3.0.2` + `simple-statistics@^7.8.9` under `dependencies`; `package-lock.json` resolves both; both exports are importable from Node; `simple-statistics` `cumulativeStdNormalProbability` is confirmed present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/domain/banned-words.ts as ADR-0005 callable source of truth</name>
  <files>src/domain/banned-words.ts, src/domain/banned-words.test.ts</files>
  <behavior>
    - Test: `BANNED_TONE_WORDS` is exported as a `readonly` tuple containing exactly: `optimize`, `wellness`, `honor`, `journey`, `crush`, `nail`, `tune`, `vibe`, `unlock`, `dial in` (10 entries, matching scripts/ci-grep-gates.sh Gate A TONE_WORDS_RE verbatim per 04-PATTERNS.md §Shared Pattern 5).
    - Test: `BANNED_TONE_WORDS_SET: ReadonlySet<string>` is exported and contains the same 10 entries.
    - Test: every entry is lowercase (the Gate A regex is case-insensitive but TS callers normalize via `.toLowerCase()`).
    - Test: an `EMOJI_RE: RegExp` is exported matching the byte ranges `[\u{1F000}-\u{1FFFF}\u{2600}-\u{27BF}]` per 04-RESEARCH.md §Formatter Architecture §D-26 contract test sketch.
  </behavior>
  <read_first>
    - @agent_docs/decisions/0005-banned-tone-words.md (the ADR; §Enforcement bullet 3 calls for "Contract test that runs the renderer on every fixture and re-checks the rendered output for banned tokens")
    - @scripts/ci-grep-gates.sh lines 95-110 (current `TONE_WORDS_RE` + `EMOJI_RE` definitions — the TS export must match these verbatim)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §Shared Pattern 5 §Tone discipline (notes that `scripts/ci-grep-gates.sh` does NOT export a callable function and that the contract test SHOULD re-implement the list inline; this task hoists the list to a shared TS module so both sources stay in lockstep)
    - @agent_docs/conventions.md (no default exports; strict TS)
  </read_first>
  <action>
    Create `src/domain/banned-words.ts` exporting (per ADR-0005 §Enforcement + the 04-PATTERNS.md §Shared Pattern 5 directive that the list be code-callable):
    - `BANNED_TONE_WORDS` — a `readonly ['optimize','wellness','honor','journey','crush','nail','tune','vibe','unlock','dial in'] as const` tuple. Order matches the Gate A regex order. Per ADR-0005 D-01 this is the single source of truth in code.
    - `BannedToneWord = (typeof BANNED_TONE_WORDS)[number]`.
    - `BANNED_TONE_WORDS_SET: ReadonlySet<BannedToneWord>` constructed from the tuple (Shared Pattern 2 closed-tuple + runtime Set discipline).
    - `EMOJI_RE: RegExp` — a unicode regex `/[\u{1F000}-\u{1FFFF}\u{2600}-\u{27BF}]/u` matching the same byte ranges Gate A uses for emoji detection.
    - `containsBannedToneToken(text: string): { hit: false } | { hit: true; word: BannedToneWord; index: number }` — pure function returning the first match using word-boundary semantics matching Gate A's `\b` boundaries (case-insensitive). For `dial in`, match the literal substring (whitespace-bounded). No I/O, no module-load side effects.
    Add a `src/domain/banned-words.test.ts` Vitest file with: (a) tuple-length assertion (`expect(BANNED_TONE_WORDS).toHaveLength(10)`), (b) per-entry membership of the canonical 10 words, (c) `BANNED_TONE_WORDS_SET.size === 10`, (d) `EMOJI_RE.test('✓')` true and `EMOJI_RE.test('x')` false (sanity), (e) `containsBannedToneToken('we should optimize sleep')` returns `{hit:true, word:'optimize', index:13}`, (f) `containsBannedToneToken('we should sleep more')` returns `{hit:false}`, (g) `containsBannedToneToken('dial in the experiment')` matches `dial in` at index 0, (h) `containsBannedToneToken('the journey home')` matches `journey`, (i) `containsBannedToneToken('respiratory_rate')` returns `{hit:false}` (Pitfall 13 — underscored metric names never match per word-boundary discipline). NO `console.*` calls anywhere (ADR-0001 / Gate B). This file lives under `src/domain/` so it is pure (Shared Pattern 1 — no I/O imports).
  </action>
  <verify>
    <automated>npx vitest run src/domain/banned-words.test.ts</automated>
  </verify>
  <done>
    Source file exports `BANNED_TONE_WORDS` (10-tuple), `BANNED_TONE_WORDS_SET`, `EMOJI_RE`, `containsBannedToneToken`. Test file passes all 9 assertions. The Gate A regex in `scripts/ci-grep-gates.sh` and this TS export are textually parallel (10 words, same order, no emoji shorthand mismatch).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship src/mcp/register-resource.ts + register-prompt.ts D-36 wrappers</name>
  <files>src/mcp/register-resource.ts, src/mcp/register-resource.test.ts, src/mcp/register-prompt.ts, src/mcp/register-prompt.test.ts</files>
  <behavior>
    - Test (resource wrapper success path): a resource handler returning `{contents: [{uri, text: 'normal text'}]}` round-trips through `registerResource` and the sanitizer runs over `contents[].text` (verifiable by inserting a `Bearer xyz` substring into the handler's return text and asserting it is redacted by the wrapper's sanitize step).
    - Test (resource wrapper error path): a resource handler that throws yields `{contents: [{uri, text: sanitize(serializeError(err)), mimeType: 'text/plain'}], isError: true}` — no token material leaks via stack trace.
    - Test (prompt wrapper success path): a prompt handler returning `{messages: [{role: 'user', content: {type: 'text', text: '...Bearer xyz...'}}]}` has its `messages[].content.text` sanitized.
    - Test (prompt wrapper error path): a thrown error returns `{messages: [{role: 'user', content: {type: 'text', text: sanitize(serializeError(err))}}], isError: true}`.
    - Test (D-36 single-call-site discipline): both files import `sanitize` + `serializeError` from `../sanitize.js` and call `server.registerResource(` / `server.registerPrompt(` exactly once each — the only such call site in the codebase.
  </behavior>
  <read_first>
    - @src/mcp/register.ts (the verbatim wrapper shape — copy-paste-and-rename; D-30 says THIS file stays UNMODIFIED, the two new files mirror its discipline)
    - @src/mcp/sanitize.ts (the recursive walker; consumed UNMODIFIED per D-30)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §MCP Surface §registerResource + §registerPrompt (signatures + return shapes from MCP SDK v1.29.0)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §Shared Pattern 8 (the wrapper skeleton + the gate I/J additions)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-36 (post-research lock)
    - @agent_docs/decisions/0001-mcp-stdout-purity.md (the load-bearing why)
  </read_first>
  <action>
    Create `src/mcp/register-resource.ts` exporting `registerResource(server: McpServer, name: string, uri: string, metadata: {title?: string; description: string; mimeType: string}, handler: (uri: URL) => Promise<{contents: Array<{uri: string; text: string; mimeType?: string}>}>): void`. Wrap `handler` in try/catch following register.ts:60-82 verbatim:
    - success path: `await handler(uri)` then walk `contents[].text` calling `sanitize(item.text)` in place (mirrors register.ts:90-102 `sanitizeResult` discipline but over `contents` not `content`).
    - error path: return `{contents: [{uri: uri.href, text: sanitize(serializeError(err)), mimeType: 'text/plain'}], isError: true}` — same isError envelope register.ts uses.
    - call `server.registerResource(name, uri, metadata, wrapped)` exactly once at the end.
    Create `src/mcp/register-prompt.ts` exporting `registerPrompt(server, name, config, handler)` where `config: { title?: string; description: string; argsSchema?: ZodObject<...> }` and `handler: (args) => Promise<{messages: Array<{role: 'user'|'assistant'; content: {type: 'text'; text: string} | {type: 'image'; data: string; mimeType: string}}>}>`. Same try/catch discipline:
    - success path: walk `messages[]`; for each, if `content.type === 'text'` call `sanitize(content.text)`. Image content passes through (no string field to sanitize).
    - error path: return `{messages: [{role: 'user' as const, content: {type: 'text' as const, text: sanitize(serializeError(err))}}], isError: true}`.
    - `as const` on `role` + `content.type` is load-bearing per 04-RESEARCH.md §MCP Surface (SDK types `role` + `type` as literal union, not `string`).
    Add the two test files mirroring `src/mcp/register.test.ts` shape (success path with embedded Bearer that gets redacted; error path that surfaces sanitized message; isError flag set on throw). Both source files MUST avoid `console.*` (ADR-0001 / Gate B); they use Pino-via-import only if logging needed (not needed for the wrappers themselves). Both must follow `agent_docs/conventions.md`: no default exports, strict TS, comment style avoids the literal `server.registerResource` / `server.registerPrompt` substrings outside the one call site (the doc-comment phrasing per Phase 1-3 convention precedent: "the wrapped SDK call", "the registration boundary"). D-36 locks these two files; Gate I + Gate J ship in Task 5.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/register-resource.test.ts src/mcp/register-prompt.test.ts</automated>
  </verify>
  <done>
    Both source files exist with the wrapper signatures above. Both test files green. Sanitizer redaction verified on success + error paths for both shapes. No `console.*` anywhere in either source file. The literal `server.registerResource(` appears exactly once in `src/mcp/register-resource.ts`; the literal `server.registerPrompt(` appears exactly once in `src/mcp/register-prompt.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create six contract-test scaffolds (formatter-tone, mcp-tool-shape, mcp-resource-shape, mcp-prompt-shape, mcp-shim-loc, daily-review-shape)</name>
  <files>tests/contract/formatter-tone.test.ts, tests/contract/mcp-tool-shape.test.ts, tests/contract/mcp-resource-shape.test.ts, tests/contract/mcp-prompt-shape.test.ts, tests/contract/mcp-shim-loc.test.ts, tests/contract/daily-review-shape.test.ts</files>
  <behavior>
    - Test scaffolds use `it.skip` / `it.todo` / a single sanity `it` so they run green without business logic. Later plans fill in the real assertions.
    - `formatter-tone.test.ts`: a single `it('BANNED_TONE_WORDS exposes 10 entries')` asserting the constant from Task 2; subsequent plans add `describe.each` over fixtures.
    - `mcp-tool-shape.test.ts`: single `it.todo('every registered MCP tool returns {content, structuredContent}')` placeholder.
    - `mcp-resource-shape.test.ts`: single `it.todo('every registered MCP resource returns {contents: [{uri,text,mimeType}]}')` placeholder.
    - `mcp-prompt-shape.test.ts`: single `it.todo('every registered MCP prompt returns {messages: [{role,content}]}')` placeholder.
    - `mcp-shim-loc.test.ts`: single `it.todo('every src/mcp/tools/*.ts register() body has ≤5 non-blank non-comment statements')` placeholder.
    - `daily-review-shape.test.ts`: single `it.todo('renderDailyReview output leads with Data status: section')` placeholder.
  </behavior>
  <read_first>
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-VALIDATION.md §Wave 0 Requirements §Contract tests (the six test files to scaffold)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Formatter Architecture §D-26 contract test (the shape Wave 3 will fill)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §MCP Surface §MCP-02 dual-shape contract + §D-29 attestation update (the shape Wave 4 will fill)
    - @agent_docs/conventions.md §Testing (Vitest pool:'forks'; fixtures under tests/fixtures/)
  </read_first>
  <action>
    Create six skeleton files under `tests/contract/`. Each MUST:
    - Import the right surface (`formatter-tone.test.ts` imports `BANNED_TONE_WORDS, EMOJI_RE, containsBannedToneToken` from `src/domain/banned-words.js` — the Task 2 export).
    - Include a header comment naming the REQ-ID + the Phase-4 plan that will populate the real assertions (e.g., `// REV-08 / D-26 — populated by Plan 04-09 (formatters wave)`).
    - Use `it.todo(...)` for the pending real assertion + `it(...)` for any sanity test that can ship green this wave (Task 2 already gives `formatter-tone.test.ts` a green sanity test against BANNED_TONE_WORDS).
    - Never call `vi.mock` of `node:fs` or anything else that would change at the framework level — these are scaffolds, not test infrastructure.
    For `mcp-shim-loc.test.ts` specifically, do NOT yet implement the AST/regex line-counter (Wave 4 owns that). Just the `it.todo`. Same for the other 4 `*.test.ts` files.
    All six files MUST avoid `console.*` (Gate B applies to tests too per scripts/ci-grep-gates.sh line 80 — verify).
  </action>
  <verify>
    <automated>npx vitest run tests/contract/formatter-tone.test.ts tests/contract/mcp-tool-shape.test.ts tests/contract/mcp-resource-shape.test.ts tests/contract/mcp-prompt-shape.test.ts tests/contract/mcp-shim-loc.test.ts tests/contract/daily-review-shape.test.ts</automated>
  </verify>
  <done>
    All six files exist under `tests/contract/`. `vitest run` over the six files returns green with `it.todo` placeholders + the formatter-tone sanity test. No production source files modified by this task.
  </done>
</task>

<task type="auto">
  <name>Task 5: Extend scripts/ci-grep-gates.sh with Gates H, I, J</name>
  <files>scripts/ci-grep-gates.sh</files>
  <read_first>
    - @scripts/ci-grep-gates.sh (current 7-gate file; add the new gates in the same Bash-script idiom as Gates A-G)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-33 (Gate H — `tools.length === 1` anti-regression for D-29 transition)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-36 (Gate I + Gate J for the new wrappers)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-VALIDATION.md §Wave 0 Requirements §CI gate extensions
  </read_first>
  <action>
    Append three new gates after Gate G in `scripts/ci-grep-gates.sh`:
    - Gate H — `\btools\.length\s*===\s*1\b` MUST NOT appear outside `tests/__legacy__/` (D-33 forces the D-29 1→8 attestation transition). Exclude `*.test.ts` only when under `tests/__legacy__/`; the standard `**/*.test.ts` exclusion that other gates use does NOT apply (the attestation IS in a test file and Gate H must catch a regression there). Match Gate G's grep idiom.
    - Gate I — `\bserver\.registerResource\s*\(` MUST NOT appear outside `src/mcp/register-resource.ts` (per D-36; mirrors Gate D enforcement for tools). Excludes `*.test.ts` (same as Gate D: tests may invoke the wrapper but never the raw SDK call directly).
    - Gate J — `\bserver\.registerPrompt\s*\(` MUST NOT appear outside `src/mcp/register-prompt.ts` (per D-36).
    For each gate: add a comment block in the header style matching the existing `Gate A/B/C/D/E/F/G` documentation (purpose, rationale citing the ADR or decision ID, exclusions). Update the script header's table-of-gates summary lines to list 10 gates total. Run `bash scripts/ci-grep-gates.sh` after the edit — at the moment of running, the script MUST exit 0 (the new wrappers from Task 3 are the only places that contain the banned strings; everything else is clean). If exit nonzero, investigate and fix BEFORE marking task done — do not relax the gate.
  </action>
  <verify>
    <automated>bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    Three new gate sections present in `scripts/ci-grep-gates.sh` matching the existing Gate A-G style. The script exits 0 on the current repo. Header table lists 10 gates. No existing gate has been weakened (manual audit: A through G grep targets unchanged).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules/` | Supply-chain trust — `ulid` + `simple-statistics` install via npm; we depend on the registry's package immutability + the SHA-512 in `package-lock.json` |
| MCP SDK → wrapper layer | Every resource + prompt registration flows through the D-36 wrappers; direct `server.registerResource` / `server.registerPrompt` from outside the wrappers is forbidden (Gates I + J) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-S5 | Tampering | `src/mcp/register-resource.ts`, `src/mcp/register-prompt.ts`, `scripts/ci-grep-gates.sh` | mitigate | The D-36 wrappers ARE the mitigation. Gates I + J refuse direct `server.registerResource(` / `server.registerPrompt(` outside the wrapper files (Task 5). Any future plan that bypasses the wrapper fails CI before merge. T-04-S5 in 04-VALIDATION.md. |
| T-04-SC | Tampering | npm installs of `ulid` + `simple-statistics` | mitigate | Both packages have an Approved disposition in 04-RESEARCH.md §Package Legitimacy Audit — `ulid` is the canonical TS ULID since 2016 (zero deps verified via `npm view ulid dependencies`), `simple-statistics` is a 14-year-old ecosystem staple. Versions pinned via `^3.0.2` and `^7.8.9`; `package-lock.json` records the exact resolved SHA-512s. No `[ASSUMED]` or `[SUS]` verdict — no human-verify checkpoint required. |
| T-04-S3 | Information Disclosure | resource handler error path leaks token material | mitigate | The D-36 resource wrapper funnels thrown errors through `sanitize(serializeError(err))` before returning. Same defence as register.ts:74-79 for tools. Test in Task 3 asserts a Bearer token in an error message gets redacted (parallel to Phase 1 register.test.ts). |
</threat_model>

<verification>
After all 5 tasks complete, the following MUST hold:

```sh
# Deps installed
node -e "import('ulid').then(m => console.log('ulid:', typeof m.ulid))"
node -e "import('simple-statistics').then(m => console.log('ss:', typeof m.median, typeof m.cumulativeStdNormalProbability))"

# Wrappers + banned-words ship
npx vitest run src/domain/banned-words.test.ts src/mcp/register-resource.test.ts src/mcp/register-prompt.test.ts tests/contract/

# All 10 grep gates pass
bash scripts/ci-grep-gates.sh

# Build still works
npm run build
```
</verification>

<success_criteria>
- `ulid@^3.0.2` and `simple-statistics@^7.8.9` are listed under `dependencies` in `package.json`; both are importable from Node.
- `src/domain/banned-words.ts` exports `BANNED_TONE_WORDS` (10-tuple), `BANNED_TONE_WORDS_SET`, `EMOJI_RE`, `containsBannedToneToken`. Sibling test green.
- `src/mcp/register-resource.ts` + `src/mcp/register-prompt.ts` exist and pass their tests; each is the sole call site of its raw SDK method.
- `scripts/ci-grep-gates.sh` has 10 gates total (A-J); the script exits 0 on the current tree.
- Six contract-test scaffolds exist under `tests/contract/` and run green with `it.todo` placeholders for Wave 3 / Wave 4 fill-in.
- Full suite `npm test` is still green (no regressions).
</success_criteria>

<output>
Create `.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-01-SUMMARY.md` when done summarizing: deps installed (versions + lock entries), files created, gates added, contract scaffolds in place.
</output>
