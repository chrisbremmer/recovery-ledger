---
phase: 05-doctor-polish-install-guide-20-minute-setup-validation
plan: 08
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - INSTALL.md
  - docs/install/claude-code.md
  - docs/install/claude-desktop.md
  - docs/install/cursor.md
  - docs/install/launchd.md
  - docs/install/whoop-app.md
  - templates/com.recovery-ledger.daily-sync.plist
  - README.md
autonomous: true
requirements:
  - DOC-04
  - DOC-05

must_haves:
  truths:
    - "INSTALL.md exists at repo root with: WHOOP dev-app setup checklist + quickstart (3-line happy path) + per-client links + troubleshooting link + launchd link + api-gap link per D-06"
    - "docs/install/claude-code.md, claude-desktop.md, cursor.md each ship the identical core CLI flow (init → auth → sync → review daily) and differ only in their MCP wiring JSON snippet per D-10"
    - "docs/install/launchd.md documents the sed + launchctl load + verification dance per Specifics line 265; references templates/com.recovery-ledger.daily-sync.plist"
    - "docs/install/whoop-app.md captures the WHOOP developer-app setup checklist (scopes, redirect URI, client_id/secret retrieval)"
    - "templates/com.recovery-ledger.daily-sync.plist is a static template with ${HOME} + ${RECOVERY_LEDGER_BIN} placeholders per D-15; valid XML per `man launchd.plist`"
    - "README.md gains an 'Install' link pointing at INSTALL.md per RESEARCH §Finding 10"
    - "Every new docs/install/*.md + INSTALL.md passes Gate A (banned tone words + emoji) — verified by running bash scripts/ci-grep-gates.sh after the writes"
    - "troubleshooting.md NOT created in this plan — Plan 05-09 owns it (alongside its contract test)"
  artifacts:
    - path: "INSTALL.md"
      provides: "Root install entry: WHOOP setup, quickstart, per-client links, troubleshooting link, launchd link, api-gap link"
      contains: "WHOOP developer-app setup"
    - path: "docs/install/claude-code.md"
      provides: "Claude Code-specific MCP wiring (.mcp.json snippet + `claude mcp add` invocation)"
      contains: "claude mcp add"
    - path: "docs/install/claude-desktop.md"
      provides: "Claude Desktop-specific config (~/Library/Application Support/Claude/claude_desktop_config.json)"
      contains: "claude_desktop_config.json"
    - path: "docs/install/cursor.md"
      provides: "Cursor-specific config (.cursor/mcp.json or ~/.cursor/mcp.json)"
      contains: "cursor"
    - path: "docs/install/launchd.md"
      provides: "Step-by-step launchd template loading instructions"
      contains: "launchctl"
    - path: "docs/install/whoop-app.md"
      provides: "WHOOP developer-app setup checklist (scopes, redirect URI, credentials)"
      contains: "developer.whoop.com"
    - path: "templates/com.recovery-ledger.daily-sync.plist"
      provides: "Static plist template with documented placeholders"
      contains: "com.recovery-ledger.daily-sync"
    - path: "README.md"
      provides: "Link to INSTALL.md"
      contains: "INSTALL"
  key_links:
    - from: "INSTALL.md"
      to: "docs/install/claude-code.md"
      via: "Per-client link in the Connect-to-your-AI-client section"
      pattern: "claude-code.md"
    - from: "docs/install/launchd.md"
      to: "templates/com.recovery-ledger.daily-sync.plist"
      via: "sed + launchctl load workflow referencing the template path"
      pattern: "templates/com.recovery-ledger.daily-sync.plist"
---

<objective>
Ship the install guide tree (DOC-04) plus the launchd template (DOC-05). This plan is documentation-heavy — no source code changes — but every doc must respect ADR-0005 (banned tone words + no emoji) and reference the locked file paths from CONTEXT.md §Decisions D-07.

Purpose: a new clone reaches the first daily review in under 20 minutes only if the user can FIND the docs and the per-client wiring is correct. Phase 5 D-10 minimizes per-client divergence; only the MCP JSON snippet differs. DOC-05 ships the launchd template as static documentation (D-15 + D-16 — zero runtime install, zero doctor probe).

Output: 6 new markdown files + 1 static plist + 1 README.md edit. No tests change in this plan; the troubleshooting contract test (Plan 05-09) is what enforces the troubleshooting-coverage shape. ADR-0005 grep gate (Gate A) is the implicit forcing function for tone discipline on every new doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md
@agent_docs/decisions/0005-banned-tone-words.md
@README.md
@scripts/ci-grep-gates.sh

<interfaces>
<!-- The shape each install doc must follow per D-06 + D-07 + D-10. Read CONTEXT + RESEARCH before drafting. -->

INSTALL.md skeleton (per RESEARCH §Finding 4.2):
  - "# Installing Recovery Ledger" header
  - Prereqs section (Node 22, macOS/Linux, WHOOP dev app)
  - "WHOOP developer-app setup" section (sign in, create app, scopes, save creds) — OR link to docs/install/whoop-app.md
  - "Quickstart" code block (git clone + npm install + npm run build + init + auth + sync + review daily)
  - "Connect to your AI client" bullet list with 3 links
  - "Troubleshooting" one-line link to docs/install/troubleshooting.md (Plan 05-09 ships the file)
  - "Scheduled daily sync (macOS)" one-line link to docs/install/launchd.md
  - "What's available via the WHOOP API (and what isn't)" one-line link to docs/install/api-gap.md (Plan 05-07 shipped)

docs/install/<client>.md skeleton (per D-10):
  - Identical core flow (init → auth → sync → review daily) — DO NOT repeat the full quickstart; link back to INSTALL.md for the shared portion
  - Client-specific MCP wiring (the JSON snippet path + content + reload instructions)
  - "Verified against client docs as of 2026-05-26" datestamp per RESEARCH §Pitfall 3

docs/install/launchd.md skeleton (per Specifics line 265):
  - Why this exists (scheduled daily sync; not a feature flag — pure docs)
  - Prereq: `recovery-ledger init` + `recovery-ledger auth` complete
  - The sed + launchctl load command sequence
  - Verification (launchctl list | grep)
  - Troubleshooting hint (doctor's last_sync_recency surfaces whether yesterday's scheduled run fired)

templates/com.recovery-ledger.daily-sync.plist (per RESEARCH §Finding 7):
  - Apple-blessed XML schema per `man launchd.plist`
  - Keys: Label, ProgramArguments, StartCalendarInterval, StandardOutPath, StandardErrorPath, RunAtLoad
  - Placeholders: ${HOME}, ${RECOVERY_LEDGER_BIN} (these are sed-substituted; launchd does NOT shell-expand them at load time per Specifics line 265)

ADR-0005 banned tone words (Gate A enforces; the docs must avoid these tokens):
  optimize, wellness, honor, journey, crush, nail, tune, vibe, unlock, dial in, emoji
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INSTALL.md (root entry)</name>
  <files>INSTALL.md</files>
  <read_first>
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding 4.2 (the INSTALL.md skeleton — reproduce closely)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-06 (hybrid layout)
    - @agent_docs/decisions/0005-banned-tone-words.md (the banned list — avoid in copy)
    - @README.md (the project's existing voice/tone for consistency)
  </read_first>
  <action>
    Create `INSTALL.md` at the repo root with the structure documented in RESEARCH §Finding 4.2:

    Sections, in order:
      1. `# Installing Recovery Ledger` — H1
      2. One-paragraph project description: "Recovery Ledger is a local-first TypeScript CLI + MCP stdio server. Not affiliated with WHOOP. BYO OAuth."
      3. `## Prerequisites` — bullet list:
         - Node 22 LTS or newer (verify via `node --version`)
         - macOS 14+ or Linux (libsecret recommended on Linux; chmod 600 file fallback otherwise — see `agent_docs/decisions/0002-single-flight-oauth-refresh.md` for the file-fallback posture)
         - A WHOOP developer app — see [`docs/install/whoop-app.md`](docs/install/whoop-app.md) for the step-by-step
      4. `## WHOOP developer-app setup` — one-line link to `docs/install/whoop-app.md` (the full checklist lives there; INSTALL.md keeps it terse). Add a 2-line summary: "Sign in at developer.whoop.com/dashboard/applications, create an app, register the redirect URI `http://127.0.0.1:<port>/callback` (`init` prompts for the port), select the read-scopes per [docs/install/whoop-app.md](docs/install/whoop-app.md)."
      5. `## Quickstart` — code block (fenced ```sh):
         ```sh
         git clone https://github.com/<owner>/recovery-ledger.git
         cd recovery-ledger
         npm install
         npm run build
         node dist/cli.mjs init      # paste client_id + client_secret
         node dist/cli.mjs auth       # opens the OAuth browser flow
         node dist/cli.mjs sync        # pulls 30 days of WHOOP data
         node dist/cli.mjs review daily  # your first brief
         ```
         (The repo owner placeholder `<owner>` stays generic; this doc ships before the repo URL is finalized.)
      6. `## Connect to your AI client` — bullet list with three links:
         - [Claude Code](docs/install/claude-code.md)
         - [Claude Desktop](docs/install/claude-desktop.md)
         - [Cursor](docs/install/cursor.md)
      7. `## Troubleshooting` — one-line link to [`docs/install/troubleshooting.md`](docs/install/troubleshooting.md) noting "one section per check name from `recovery-ledger doctor`." (The file lands in Plan 05-09; the link target is valid as a forward reference within the same PR set.)
      8. `## Scheduled daily sync (macOS)` — one-line link to [`docs/install/launchd.md`](docs/install/launchd.md)
      9. `## What's available via the WHOOP API (and what isn't)` — one-line link to [`docs/install/api-gap.md`](docs/install/api-gap.md) (Plan 05-07 generated)
      10. `## Verifying your install` — one-line: "Run `node dist/cli.mjs doctor` (or `recovery-ledger doctor` if you `npm link`-ed). All checks should show `pass` or a documented warning."

    Tone discipline (Gate A — verify after write):
      - Avoid every word in BANNED_TONE_WORDS_SET: optimize, wellness, honor, journey, crush, nail, tune, vibe, unlock, dial in.
      - No emoji.
      - Use direct, instructional voice: "Sign in at..." not "Embark on your journey to..."
      - Verb-first action items (per ADR-0005 + Phase 4 D-26 conventions).

    NO `console.*` references inside fenced code blocks — Gate B is sensitive only to source files; markdown is exempt — but verify by reading scripts/ci-grep-gates.sh after the write.

    After writing, run `bash scripts/ci-grep-gates.sh` to confirm Gate A passes. If it trips, identify the offending substring (the gate prints the matched file + line) and re-word.
  </action>
  <verify>
    <automated>test -f INSTALL.md && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    INSTALL.md exists with the 10-section structure. Gate A green. All cross-links point to files that either exist now (api-gap.md from Plan 05-07) or land in this PR set (whoop-app.md, claude-code.md, claude-desktop.md, cursor.md, launchd.md from this plan; troubleshooting.md from Plan 05-09).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/install/whoop-app.md (WHOOP dev-app setup checklist)</name>
  <files>docs/install/whoop-app.md</files>
  <read_first>
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-06 (the WHOOP dev-app checklist is part of INSTALL.md per D-06; this plan factors it to its own file for length/findability — verify with the executor: D-06 calls for the checklist to LIVE inside INSTALL.md OR a sibling file. The deferred-to-sibling-file approach is consistent with D-06's "shared across all clients" language. Both are acceptable; the executor picks the sibling-file form for length reasons.)
    - @.planning/phases/02-oauth-token-store-single-flight-refresh/02-CONTEXT.md (Phase 2 D-13 scope list — the canonical OAuth scopes used by `recovery-ledger init`)
    - @agent_docs/decisions/0007-whoop-read-only.md (read-only API surface — informs the scope list)
    - @agent_docs/decisions/0005-banned-tone-words.md (tone discipline)
  </read_first>
  <action>
    Create `docs/install/whoop-app.md` with the following sections (avoid banned tone words throughout):

      `# WHOOP Developer App Setup`

      `## Why you need this`
      Two-sentence explanation: Recovery Ledger uses your own WHOOP developer app to access the v2 API on your behalf. No shared relay; your tokens stay local (see [agent_docs/decisions/0002-single-flight-oauth-refresh.md](../../agent_docs/decisions/0002-single-flight-oauth-refresh.md)).

      `## Step-by-step`
      Numbered list (1-5):
        1. Sign in at https://developer.whoop.com/dashboard/applications
        2. Click "Create New Application." Name it whatever you want (e.g., `Recovery Ledger - <your name>`).
        3. Set the redirect URI to `http://127.0.0.1:4321/callback` (or whatever port you plan to give `init`; the default is 4321 and `init` will prompt).
        4. Select these scopes (all read-only — see [agent_docs/decisions/0007-whoop-read-only.md](../../agent_docs/decisions/0007-whoop-read-only.md)):
           - `read:profile`
           - `read:cycles`
           - `read:recovery`
           - `read:sleep`
           - `read:workout`
           - `read:body_measurement`
           - `offline` (required for token refresh)
        5. Save. Copy the `client_id` and `client_secret` — you will paste them when you run `recovery-ledger init`.

      `## What WHOOP shows you next`
      Two-sentence explanation: the dashboard shows usage stats, recent API errors, and a button to rotate secrets. Recovery Ledger does not require any further setup on the WHOOP side.

      `## Common mistakes`
      Bullet list:
        - Wrong redirect URI (must match exactly, including the trailing path)
        - Forgetting the `offline` scope (token refresh will fail; doctor's `token_freshness` check surfaces this)
        - Pasting `client_secret` into a public chat or commit (rotate immediately via the WHOOP dashboard if this happens)

    Tone: direct, instructional. No banned words.
  </action>
  <verify>
    <automated>test -f docs/install/whoop-app.md && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    docs/install/whoop-app.md exists with the documented sections. Gate A green. Scope list matches Phase 2 D-13 verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create the 3 per-client docs (claude-code.md, claude-desktop.md, cursor.md)</name>
  <files>docs/install/claude-code.md, docs/install/claude-desktop.md, docs/install/cursor.md</files>
  <read_first>
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding 4.3 (the per-client config snippets — VERIFIED current as of 2026-05; the executor reproduces them verbatim with the "verified as of 2026-05-26" datestamp)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-10 (no per-client deviation from the core CLI flow)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Pitfall 3 (drift over time + the datestamp mitigation)
    - @agent_docs/decisions/0005-banned-tone-words.md (tone discipline)
  </read_first>
  <action>
    Create THREE files with the same skeleton but client-specific config:

    `docs/install/claude-code.md`:
      `# Claude Code — Recovery Ledger MCP Setup`

      "Verified against Claude Code docs as of 2026-05-26."

      `## Prerequisites`
        - Recovery Ledger installed + built (`npm install && npm run build`) per [INSTALL.md](../../INSTALL.md)
        - `recovery-ledger init` + `recovery-ledger auth` complete (you can verify with `node dist/cli.mjs doctor` — every check should be `pass` or a documented warning)

      `## One-liner setup`
      Fenced code block:
        ```sh
        claude mcp add recovery-ledger -- node /absolute/path/to/dist/mcp.mjs
        ```
      Note: replace `/absolute/path/to/dist/mcp.mjs` with the result of `realpath dist/mcp.mjs` from the recovery-ledger repo root.

      `## Manual config`
      For users who prefer hand-editing: Claude Code reads project config from `.mcp.json` at the repo root, and user-scoped config from `~/.claude.json`. Either file uses the same shape:

      ```json
      {
        "mcpServers": {
          "recovery-ledger": {
            "command": "node",
            "args": ["/absolute/path/to/dist/mcp.mjs"]
          }
        }
      }
      ```

      `## Verifying`
      Open Claude Code. Type `/mcp` and confirm `recovery-ledger` appears with 8 tools. Try the `whoop_doctor` tool; the response should list 14 checks.

      `## Troubleshooting`
      If the tool list is empty: confirm `dist/mcp.mjs` exists (run `npm run build`). If the tool fails on invocation: run `node dist/cli.mjs doctor --text` to see which check is failing. Cross-reference [docs/install/troubleshooting.md](troubleshooting.md).

      Reference: [Claude Code MCP docs](https://code.claude.com/docs/en/mcp).

    `docs/install/claude-desktop.md`:
      Same skeleton (Prerequisites, One-liner setup, Manual config, Verifying, Troubleshooting, Reference). Differences:
        - No one-liner — Claude Desktop is GUI-config-only. Skip the one-liner section; the manual config section is the primary path.
        - Config file path: `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) or `%APPDATA%\Claude\claude_desktop_config.json` (Windows; project is macOS-first, Windows is mentioned as a Phase 5+ deferral).
        - Restart Claude Desktop after editing (different reload semantics than Claude Code).
        - 2026 note: "Desktop Extensions (`.mcpb`) bundles are an alternative we may ship in v2 (V2-01-adjacent); for v1 use the JSON config approach above."
        - Reference: [Claude Desktop MCP quickstart](https://modelcontextprotocol.io/quickstart/user).

    `docs/install/cursor.md`:
      Same skeleton. Differences:
        - Config file path: `.cursor/mcp.json` (project-scoped; commit-worthy) OR `~/.cursor/mcp.json` (global).
        - Same JSON shape as the other two.
        - January 2026 note: "Cursor's January 2026 update changed how multiple MCP servers are dispatched (dynamic tool-description loading). The config shape is unchanged."
        - V2-01-deferral note: "Cursor v1 had some MCP compatibility caveats. If you hit a regression, fall back to using Recovery Ledger through Claude Code or Claude Desktop while we track V2-01."
        - Reference: [Cursor MCP docs](https://cursor.com/docs/mcp).

    For all three files: avoid every banned tone word; no emoji; instructional voice.
  </action>
  <verify>
    <automated>test -f docs/install/claude-code.md && test -f docs/install/claude-desktop.md && test -f docs/install/cursor.md && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    All 3 client docs exist. Each contains the documented sections + verified 2026-05-26 datestamp. JSON snippets are valid (executor verifies by parsing each fenced JSON block with `jq` or by visual inspection). Gate A green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create docs/install/launchd.md + templates/com.recovery-ledger.daily-sync.plist</name>
  <files>docs/install/launchd.md, templates/com.recovery-ledger.daily-sync.plist</files>
  <read_first>
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding 7 (the exact plist XML + the sed/launchctl command sequence)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-15 + D-16 (no auto-install, no CLI command, no doctor probe — pure docs)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Specifics line 265 (the canonical sed + launchctl command)
    - @agent_docs/decisions/0005-banned-tone-words.md (tone discipline)
  </read_first>
  <action>
    Create `templates/com.recovery-ledger.daily-sync.plist` reproducing RESEARCH §Finding 7's XML verbatim:

      ```xml
      <?xml version="1.0" encoding="UTF-8"?>
      <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
      <plist version="1.0">
        <dict>
          <key>Label</key>
          <string>com.recovery-ledger.daily-sync</string>
          <key>ProgramArguments</key>
          <array>
            <string>${RECOVERY_LEDGER_BIN}</string>
            <string>sync</string>
            <string>--days</string>
            <string>3</string>
          </array>
          <key>StartCalendarInterval</key>
          <dict>
            <key>Hour</key>
            <integer>6</integer>
            <key>Minute</key>
            <integer>30</integer>
          </dict>
          <key>StandardOutPath</key>
          <string>${HOME}/.recovery-ledger/launchd.log</string>
          <key>StandardErrorPath</key>
          <string>${HOME}/.recovery-ledger/launchd.log</string>
          <key>RunAtLoad</key>
          <false/>
        </dict>
      </plist>
      ```

      The literal `${HOME}` and `${RECOVERY_LEDGER_BIN}` are sed-substituted by the user before `launchctl load`. launchd does NOT shell-expand these (per Specifics line 265).

    Create `docs/install/launchd.md`:

      `# Scheduled Daily Sync via launchd (macOS)`

      `## What this gives you`
      Two sentences: a scheduled daily sync at 6:30 AM local time so your morning review has fresh data. Pure macOS-native, no third-party scheduler.

      `## Prerequisites`
      Bullet list:
        - macOS (Phase 5 is macOS-only — Linux users use cron; systemd timers are tracked as V2-03)
        - `recovery-ledger` on PATH (run `npm link` from the repo root, or note the absolute path to `dist/cli.mjs`)
        - `recovery-ledger init` + `recovery-ledger auth` complete

      `## Installation`
      Numbered list with the sed + launchctl commands:
        1. Resolve the recovery-ledger bin path:
           ```sh
           RECOVERY_LEDGER_BIN=$(which recovery-ledger)
           # Or: RECOVERY_LEDGER_BIN=$(realpath dist/cli.mjs)
           ```
        2. Substitute placeholders + copy to LaunchAgents:
           ```sh
           sed -e "s|\${RECOVERY_LEDGER_BIN}|$RECOVERY_LEDGER_BIN|g" \
               -e "s|\${HOME}|$HOME|g" \
               templates/com.recovery-ledger.daily-sync.plist \
             > ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist
           ```
        3. Load the agent:
           ```sh
           launchctl load ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist
           ```
        4. Verify it's loaded:
           ```sh
           launchctl list | grep com.recovery-ledger
           # Should show: <PID-or-dash> <exit-code> com.recovery-ledger.daily-sync
           ```

      `## Verifying the schedule fires`
      The agent fires at 6:30 AM by default. The morning after install, run:
        ```sh
        recovery-ledger doctor
        ```
      The `last_sync_recency` check should show "last sync <H>h ago" within the threshold. Logs are written to `~/.recovery-ledger/launchd.log`.

      `## Customizing the schedule`
      Edit the `StartCalendarInterval` keys in `~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist`. Reload after editing:
        ```sh
        launchctl unload ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist
        launchctl load ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist
        ```

      `## Uninstalling`
        ```sh
        launchctl unload ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist
        rm ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist
        ```

      `## Troubleshooting`
      Bullet list:
        - If `launchctl list` doesn't show the agent: re-check the load step's exit code. macOS may reject the plist if XML is malformed; `plutil -lint ~/Library/LaunchAgents/com.recovery-ledger.daily-sync.plist` validates it.
        - If the morning sync didn't fire: the laptop must be awake at the scheduled time. macOS doesn't run launchd agents on a sleeping machine; the next wake fires the job once.
        - If `last_sync_recency` shows the sync didn't run: check `~/.recovery-ledger/launchd.log` for errors. Common: stale OAuth token (`recovery-ledger doctor` will surface this as `token_freshness: warn`; running `recovery-ledger auth` to re-authorize fixes it).

      Reference: `man launchd.plist`, `man launchctl`.

    Both files: avoid banned tone words; no emoji.
  </action>
  <verify>
    <automated>test -f templates/com.recovery-ledger.daily-sync.plist && plutil -lint templates/com.recovery-ledger.daily-sync.plist 2>&1 | grep -q OK && test -f docs/install/launchd.md && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    plist template exists and passes `plutil -lint`. launchd.md exists with the documented sections. Gate A green. The plist uses `${HOME}` and `${RECOVERY_LEDGER_BIN}` placeholders verbatim (no shell-expansion expected at load time).
  </done>
</task>

<task type="auto">
  <name>Task 5: Link INSTALL.md from README.md</name>
  <files>README.md</files>
  <read_first>
    - @README.md (current content — locate the Install section or the most relevant placement point)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding 10 (README mentions INSTALL.md as the front door)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-22 (phase-close README update; this plan does the link, the close plan does any final touch-ups)
  </read_first>
  <action>
    Edit `README.md`:
      1. Find the appropriate location — typically near the top after the project description, before the technical sections (stack / status / architecture per AGENTS.md's existing convention).
      2. Add an `## Install` section if one doesn't exist. Content:
         "See [INSTALL.md](INSTALL.md) for the WHOOP developer-app setup checklist, quickstart, per-client MCP wiring (Claude Code, Claude Desktop, Cursor), troubleshooting map, and the launchd daily-sync template."
      3. If an `## Install` section ALREADY exists, replace it with the single-line link above (avoiding duplication with INSTALL.md).
      4. Preserve all OTHER sections in README.md unchanged (Status, Stack, Architecture, etc.).

    Tone: no banned words; no emoji.
  </action>
  <verify>
    <automated>grep -q "INSTALL.md" README.md && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    README.md references INSTALL.md. Gate A green. Other README sections untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Install docs → user's filesystem (launchd.md sed + cp) | The doc instructs the user to run sed + cp into `~/Library/LaunchAgents/`. The user is the trusting agent. No automated install. |
| Install docs → external client docs (Claude Code, Claude Desktop, Cursor) | Each per-client doc references the canonical client docs URL so a user landing in 2027 can verify against current shape. RESEARCH §Pitfall 3 is the drift mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-T7 | Tampering | A user could blindly copy the plist + run launchctl load before substituting placeholders | accept | The plist with literal `${RECOVERY_LEDGER_BIN}` would fail launchctl load (launchd does not expand `$( )`). The failure surfaces immediately; the user re-reads the doc. |
| T-05-I8 | Information Disclosure | launchd.log path is `~/.recovery-ledger/launchd.log` — could leak sync output | mitigate | Logs route through Pino (stderr), which sanitizes Bearer/JWT material per ADR-0001 + Phase 4 D-30. The log file is in the user's home directory, accessible only to that user. |
| T-05-R2 | Repudiation | No doctor probe for "is launchd loaded?" per D-15 | mitigate | `last_sync_recency` check surfaces whether yesterday's scheduled run fired. The signal IS in the data, per D-15 design choice. |
</threat_model>

<verification>
After all 5 tasks complete:

```sh
# All install files exist
test -f INSTALL.md
test -f docs/install/whoop-app.md
test -f docs/install/claude-code.md
test -f docs/install/claude-desktop.md
test -f docs/install/cursor.md
test -f docs/install/launchd.md
test -f templates/com.recovery-ledger.daily-sync.plist
grep -q "INSTALL.md" README.md

# plist is valid XML
plutil -lint templates/com.recovery-ledger.daily-sync.plist

# Tone gate green
bash scripts/ci-grep-gates.sh

# All cross-links resolve (light check — actual broken-link tests are in Plan 05-09's troubleshooting contract test)
for f in INSTALL.md docs/install/*.md; do echo "=== $f ==="; grep -oE '\]\([^)]+\)' "$f" | head; done
```
</verification>

<success_criteria>
- INSTALL.md exists at repo root with all 10 sections from RESEARCH §Finding 4.2.
- 5 docs in `docs/install/`: whoop-app.md, claude-code.md, claude-desktop.md, cursor.md, launchd.md.
- `templates/com.recovery-ledger.daily-sync.plist` is valid (plutil -lint passes).
- README.md references INSTALL.md.
- All 10 grep gates green (Gate A is the load-bearing tone check for this plan).
- Per-client docs have the "verified as of 2026-05-26" datestamp.
- launchd doc includes the sed + launchctl load command sequence per Specifics line 265.
- No emoji anywhere; no banned tone words anywhere.
- troubleshooting.md is NOT created in this plan (Plan 05-09 owns it).
</success_criteria>

<output>
Create `.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-08-SUMMARY.md` when done summarizing: 5 install docs + plist template + README link. All Gate A green; all cross-links to plan-05-08-and-later files documented.
</output>
