---
phase: 05-doctor-polish-install-guide-20-minute-setup-validation
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/services/doctor/checks/db-open.ts
  - src/services/doctor/checks/db-open.test.ts
  - src/services/doctor/checks/db-integrity.ts
  - src/services/doctor/checks/db-integrity.test.ts
  - src/services/doctor/checks/db-schema-version.ts
  - src/services/doctor/checks/db-schema-version.test.ts
  - src/services/doctor/checks/db-wal-size.ts
  - src/services/doctor/checks/db-wal-size.test.ts
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "probeDbOpen returns pass when an injected sqlite handle responds to PRAGMA journal_mode (the no-op pragma proxy for 'DB layer is alive'); returns fail with 'no DB handle injected' when deps.sqlite is undefined per RESEARCH §Open Questions §1 recommendation"
    - "probeDbIntegrity returns pass iff `sqlite.pragma('integrity_check')` returns exactly one row with `{integrity_check: 'ok'}`; fail otherwise (multi-row OR non-ok content)"
    - "probeDbSchemaVersion returns pass when `SELECT COUNT(*) FROM __drizzle_migrations` equals the count of `.sql` files in src/infrastructure/db/migrations/; fail otherwise with the backup-path remediation hint"
    - "probeDbWalSize reads `<dbFile>-wal` size via node:fs.statSync; pass at <=32MB (33554432 bytes), warn at <=64MB (67108864 bytes), fail above the journal_size_limit cap (Phase 3 D-30)"
    - "All 4 probes never throw — every error path is caught and returned as a structured fail check"
    - "All 4 probes are read-only — none mutate the DB or filesystem outside of the read-side pragmas / statSync"
    - "Tests use createInMemoryDb() helper for DB-touching probes (ADR-0006); db-wal-size test uses a tmp directory + writeFileSync to construct a fake WAL file"
  artifacts:
    - path: "src/services/doctor/checks/db-open.ts"
      provides: "probeDbOpen({sqlite?}) → DoctorCheck"
      contains: "probeDbOpen"
    - path: "src/services/doctor/checks/db-integrity.ts"
      provides: "probeDbIntegrity({sqlite?}) → DoctorCheck"
      contains: "probeDbIntegrity"
    - path: "src/services/doctor/checks/db-schema-version.ts"
      provides: "probeDbSchemaVersion({sqlite?, migrationsDir?}) → DoctorCheck"
      contains: "probeDbSchemaVersion"
    - path: "src/services/doctor/checks/db-wal-size.ts"
      provides: "probeDbWalSize({dbFile?}) → DoctorCheck"
      contains: "probeDbWalSize"
  key_links:
    - from: "src/services/doctor/checks/db-integrity.ts"
      to: "better-sqlite3"
      via: "sqlite.pragma('integrity_check') — the canonical built-in DB integrity check"
      pattern: "integrity_check"
    - from: "src/services/doctor/checks/db-schema-version.ts"
      to: "src/infrastructure/db/migrate.ts"
      via: "Reads the __drizzle_migrations table the hand-rolled migrator writes (line 169 + 232 + 262)"
      pattern: "__drizzle_migrations"
    - from: "src/services/doctor/checks/db-wal-size.ts"
      to: "src/infrastructure/db/connection.ts"
      via: "Reads the <dbFile>-wal companion file whose ceiling is journal_size_limit=67108864"
      pattern: "wal"
---

<objective>
Ship the 4 DB-touching doctor probes: `db_open`, `db_integrity`, `db_schema_version`, `db_wal_size`. Each is an independent file under `src/services/doctor/checks/` with its own unit test. All 4 consume the optional `RunDoctorOptions.sqlite` handle (added in Plan 05-01) per RESEARCH §Open Questions §1 — the bootstrap layer (Plan 05-06) passes the production handle; absent the handle, probes return a structured fail.

Purpose: surface DB-layer health as 4 atomic signals per D-01 split-per-signal. A composite check would defeat DOC-02's 1:1 troubleshooting-map keying. The 4 probes together cover Phase 3 D-08 (mid-flight migration recovery), D-30 (pragma + WAL ceiling), and the canonical SQLite `PRAGMA integrity_check`.

Output: 4 probe files + 4 test files. Plan 05-06 wires them into runDoctor() alongside the other 5 Wave 1 probes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md
@agent_docs/decisions/0001-mcp-stdout-purity.md
@src/services/doctor/checks/native-modules.ts
@src/services/doctor/checks/auth.ts
@src/services/doctor/checks/check-names.ts
@src/services/doctor/index.ts
@src/infrastructure/db/connection.ts
@src/infrastructure/db/migrate.ts
@src/infrastructure/config/paths.ts
@tests/helpers/in-memory-db.ts

<interfaces>
<!-- Key types each probe consumes. Read the files above; do not invent shapes. -->

From src/services/doctor/index.ts:
  interface DoctorCheck { name: string; status: 'pass'|'warn'|'fail'; detail: string }

From src/services/doctor/checks/check-names.ts (post-Plan 05-01):
  CHECK_NAMES.DB_OPEN === 'db_open'
  CHECK_NAMES.DB_INTEGRITY === 'db_integrity'
  CHECK_NAMES.DB_SCHEMA_VERSION === 'db_schema_version'
  CHECK_NAMES.DB_WAL_SIZE === 'db_wal_size'

From src/infrastructure/db/connection.ts:
  openDb(opts?: {dbFile?: string}): Database.Database
  Pragma: journal_mode=WAL, journal_size_limit=67108864 (= 64 MiB)

From src/infrastructure/db/migrate.ts:
  Migrator writes/reads __drizzle_migrations table (hash, created_at) — lines 139 + 169 + 223 + 232 + 262
  Migrations dir layout: src/infrastructure/db/migrations/0000_initial.sql + future 0001_*.sql etc.

From src/infrastructure/config/paths.ts:
  paths.dbFile  — absolute path to the on-disk database

From tests/helpers/in-memory-db.ts:
  createInMemoryDb()  — returns a real better-sqlite3 handle backed by :memory:, with the migrator already run

WAL thresholds (per D-02 #5, RESEARCH §3.5, agent_docs/research STACK):
  WAL_WARN_BYTES   = 32 * 1024 * 1024   (33554432)
  WAL_FAIL_BYTES   = 64 * 1024 * 1024   (67108864)   matches Phase 3 D-30 journal_size_limit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create probeDbOpen + tests</name>
  <files>src/services/doctor/checks/db-open.ts, src/services/doctor/checks/db-open.test.ts</files>
  <behavior>
    - Exports `interface DbOpenProbeDeps { sqlite?: Database.Database }`.
    - Exports `probeDbOpen(deps?: DbOpenProbeDeps): Promise<DoctorCheck>`.
    - When `deps?.sqlite` is undefined: returns `{name: 'db_open', status: 'fail', detail: 'no DB handle injected — run from CLI to exercise db checks'}`.
    - When the handle is present + `sqlite.pragma('journal_mode')` returns `[{journal_mode: 'wal'}]` (or similar — assert the call succeeds and returns ≥1 row): returns `{name: 'db_open', status: 'pass', detail: 'WAL journal mode confirmed'}`.
    - When the pragma call throws: returns fail with `'pragma probe threw: <err.message>'`.
  </behavior>
  <read_first>
    - @src/services/doctor/checks/auth.ts (probe shape precedent — function signature, try/catch, structured return)
    - @src/infrastructure/db/connection.ts (the WAL pragma — line ~75 sets `journal_mode = WAL`)
    - @tests/helpers/in-memory-db.ts (the in-memory DB helper for tests)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.2 (the probe responsibility — "loud DB layer is alive signal")
    - @src/services/doctor/checks/check-names.ts (post-05-01 — CHECK_NAMES.DB_OPEN)
  </read_first>
  <action>
    Create `src/services/doctor/checks/db-open.ts`:
      Imports: `import type Database from 'better-sqlite3';` + `import type { DoctorCheck } from '../index.js';` + `import { CHECK_NAMES } from './check-names.js';`.
      Export `interface DbOpenProbeDeps { sqlite?: Database.Database }`.
      Export `async function probeDbOpen(deps?: DbOpenProbeDeps): Promise<DoctorCheck>`:
        1. If `!deps?.sqlite`: return `{name: CHECK_NAMES.DB_OPEN, status: 'fail', detail: 'no DB handle injected — run from CLI to exercise db checks'}`.
        2. Try: `const rows = deps.sqlite.pragma('journal_mode') as Array<{ journal_mode?: string }>;`
           - If `rows.length === 1 && rows[0]?.journal_mode?.toLowerCase() === 'wal'`: return pass with detail `'WAL journal mode confirmed'`.
           - Else: return pass with detail `'DB open, journal_mode=<rows[0]?.journal_mode ?? "unknown">'` (non-WAL is unexpected but not load-bearing for this probe — db_integrity is the corruption check; db_open is the "handle is alive" signal).
        3. Catch err: return `{name: CHECK_NAMES.DB_OPEN, status: 'fail', detail: 'pragma probe threw: <err.message>'}`.

    File-header doc-comment in the auth.ts voice; references Phase 3 D-30 pragma (the why this probe exists). No `console.*`. No bare `fetch(`. No `drizzle-orm` import (Gate G — this probe uses the raw better-sqlite3 handle via the pragma() API, NOT the Drizzle query builder; this is consistent with native-modules.ts which also bypasses Drizzle for native-level probes).

    Create `src/services/doctor/checks/db-open.test.ts` with 3 cases:
      1. `'returns fail when no handle injected'` — call with `{}` and assert `status === 'fail'`.
      2. `'returns pass when handle responds to pragma'` — use `createInMemoryDb()` from tests/helpers/in-memory-db.ts, pass `{sqlite: db}`, assert `status === 'pass'` and detail mentions WAL or the actual journal_mode. The in-memory DB defaults to `journal_mode=memory` per SQLite — so the test must accept either WAL or memory; the production handle from openDb() is WAL. Use the test's value to assert the detail string format, not the specific journal_mode string.
      3. `'returns fail when pragma throws'` — pass `{sqlite: {pragma: () => { throw new Error('test-error'); }} as unknown as Database.Database}` and assert status fail + detail includes 'pragma probe threw'.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/db-open.test.ts</automated>
  </verify>
  <done>
    db-open.ts + db-open.test.ts exist; 3 tests green; no `console.*`, no `drizzle-orm` import.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create probeDbIntegrity + tests</name>
  <files>src/services/doctor/checks/db-integrity.ts, src/services/doctor/checks/db-integrity.test.ts</files>
  <behavior>
    - Exports `interface DbIntegrityProbeDeps { sqlite?: Database.Database }`.
    - Exports `probeDbIntegrity(deps?: DbIntegrityProbeDeps): Promise<DoctorCheck>`.
    - When `deps?.sqlite` undefined: fail with `'no DB handle injected'`.
    - When `PRAGMA integrity_check` returns `[{integrity_check: 'ok'}]` (exactly 1 row): pass with detail `'PRAGMA integrity_check ok'`.
    - When it returns multiple rows OR a single non-ok row: fail with detail `'PRAGMA integrity_check returned <N> row(s); first: <rows[0]?.integrity_check ?? "(empty)">'`.
    - When the pragma throws: fail with detail `'probe threw: <message>'`.
  </behavior>
  <read_first>
    - @src/services/doctor/checks/db-open.ts (Task 1 sibling — same dep-injection shape)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.3 + §Pattern 1 §db-integrity.ts (the verbatim code example — reproduce its body)
    - @tests/helpers/in-memory-db.ts (for the pass case)
    - @src/services/doctor/checks/check-names.ts (CHECK_NAMES.DB_INTEGRITY)
  </read_first>
  <action>
    Create `src/services/doctor/checks/db-integrity.ts` reproducing RESEARCH §Pattern 1 §db-integrity.ts verbatim (the executor copies the body shown there). Verify the function signature uses `DbIntegrityProbeDeps` (not the unscoped `deps` parameter name). The `sqlite.pragma('integrity_check')` return-type cast is `as Array<{ integrity_check: string }>`. The 3-arm structured return: 1-row-ok pass, otherwise-fail, throw-fail.

    Create `src/services/doctor/checks/db-integrity.test.ts` with 4 cases:
      1. `'returns fail when no handle injected'`.
      2. `'returns pass on healthy DB'` — `createInMemoryDb()` + assert pass.
      3. `'returns fail when pragma returns multiple rows'` — mock sqlite handle where `pragma('integrity_check')` returns `[{integrity_check: 'database disk image is malformed'}, {integrity_check: 'other'}]`; assert fail + detail mentions '2 row(s)'.
      4. `'returns fail when pragma throws'` — mock handle whose `pragma` throws; assert fail + detail starts with 'probe threw:'.

    No `console.*`. No `drizzle-orm` import. File-header doc-comment cites SQLite `PRAGMA integrity_check` as the canonical built-in approach (per RESEARCH §Don't Hand-Roll).
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/db-integrity.test.ts</automated>
  </verify>
  <done>
    db-integrity.ts + db-integrity.test.ts exist; 4 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create probeDbSchemaVersion + tests</name>
  <files>src/services/doctor/checks/db-schema-version.ts, src/services/doctor/checks/db-schema-version.test.ts</files>
  <behavior>
    - Exports `interface DbSchemaVersionProbeDeps { sqlite?: Database.Database; migrationsDir?: string }`.
    - Exports `probeDbSchemaVersion(deps?: DbSchemaVersionProbeDeps): Promise<DoctorCheck>`.
    - When deps.sqlite undefined: fail with `'no DB handle injected'`.
    - Reads `SELECT COUNT(*) AS c FROM __drizzle_migrations` from the handle and counts the `.sql` files in `deps.migrationsDir` (defaulting to the resolved path of `src/infrastructure/db/migrations/` relative to the running process — same resolution `bootstrap.ts` uses).
    - When `dbCount === fileCount`: pass with detail `'schema at migration <N>/<N>'`.
    - When `dbCount < fileCount`: fail with detail `'schema at migration <dbCount>/<fileCount> — restore from <paths.backupsDir> if available: cp <latest-backup> <paths.dbFile>'`. Backup path is the most-recent file under `paths.backupsDir` (resolved via `node:fs.readdirSync` sorted desc); when no backup exists, fall back to `(no backup found)`.
    - When `dbCount > fileCount`: fail with detail `'schema at migration <dbCount>/<fileCount> — extra rows in __drizzle_migrations (orphaned migration record); see docs/install/troubleshooting.md#db_schema_version'`.
    - When the SELECT throws (e.g., the table doesn't exist): fail with detail `'probe threw: <message>'`.
  </behavior>
  <read_first>
    - @src/infrastructure/db/migrate.ts lines 139-280 (the __drizzle_migrations table schema + the journal entries pattern; line 169 SELECT precedent; line 262 final consistency check — load-bearing for understanding the count discipline)
    - @src/infrastructure/db/migrations/ (the directory with `.sql` files; `0000_initial.sql` + `meta/` — the probe COUNTS only `.sql` files at the directory's top level, NOT recursively, NOT the `meta/` files)
    - @src/infrastructure/config/paths.ts (paths.backupsDir + paths.dbFile)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.4 + Assumption A4 (the orphaned-row case + remediation hint)
    - @.planning/phases/03-data-model-db-layer-sync-loop/03-CONTEXT.md (Phase 3 D-06/D-07/D-08 migrator posture — fails-closed, never auto-restores; the doctor probe SURFACES the remediation but does NOT auto-execute it)
    - @src/services/doctor/checks/check-names.ts (CHECK_NAMES.DB_SCHEMA_VERSION)
  </read_first>
  <action>
    Create `src/services/doctor/checks/db-schema-version.ts`:
      Imports: `import { readdirSync } from 'node:fs';`, `import { resolve, dirname } from 'node:path';`, `import { fileURLToPath } from 'node:url';`, `import type Database from 'better-sqlite3';`, `import type { DoctorCheck } from '../index.js';`, `import { CHECK_NAMES } from './check-names.js';`, `import { paths } from '../../../infrastructure/config/paths.js';`.

      Helper inline: `function resolveDefaultMigrationsDir(): string { return resolve(dirname(fileURLToPath(import.meta.url)), '..', '..', '..', 'infrastructure', 'db', 'migrations'); }` — mirrors the path resolution `bootstrap.ts` uses for the migrator. The resolved path must work both in dev (src/ via tsx + vitest) AND in built dist/ — the relative depth from `src/services/doctor/checks/db-schema-version.ts` to `src/infrastructure/db/migrations/` is `../../../infrastructure/db/migrations`. Verify with `test -d` before relying on it; the executor checks the relative-path math against the live src tree before committing.

      Helper inline: `function findLatestBackup(): string | null { try { const files = readdirSync(paths.backupsDir).filter(f => f.endsWith('.sqlite')).sort().reverse(); return files[0] ? resolve(paths.backupsDir, files[0]) : null; } catch { return null; } }`.

      Export `async function probeDbSchemaVersion(deps?: DbSchemaVersionProbeDeps): Promise<DoctorCheck>`:
        1. If `!deps?.sqlite`: fail with 'no DB handle injected'.
        2. Try block:
           const dir = deps.migrationsDir ?? resolveDefaultMigrationsDir();
           const fileCount = readdirSync(dir).filter(f => f.endsWith('.sql')).length;
           const row = deps.sqlite.prepare('SELECT COUNT(*) AS c FROM __drizzle_migrations').get() as {c: number} | undefined;
           const dbCount = row?.c ?? 0;
           if (dbCount === fileCount) → pass with detail `'schema at migration ${dbCount}/${fileCount}'`.
           else if (dbCount < fileCount):
             const backup = findLatestBackup();
             const hint = backup ? `restore from ${backup}: cp ${backup} ${paths.dbFile}` : '(no backup found)';
             → fail with detail `'schema at migration ${dbCount}/${fileCount} — ${hint}'`.
           else (dbCount > fileCount):
             → fail with detail `'schema at migration ${dbCount}/${fileCount} — extra rows in __drizzle_migrations (orphaned migration record); see docs/install/troubleshooting.md#db_schema_version'`.
        3. Catch err: fail with `'probe threw: <err.message>'`.

      The literal string `__drizzle_migrations` is the source of truth (matches src/infrastructure/db/migrate.ts). The Phase 5 troubleshooting doc (Plan 05-09) will key off the literal anchor `db_schema_version` in the detail string.

    Create `src/services/doctor/checks/db-schema-version.test.ts` with 5 cases:
      1. `'returns fail when no handle injected'`.
      2. `'returns pass when counts match'` — `createInMemoryDb()` (which runs the migrator and so populates __drizzle_migrations with N rows matching the N .sql files). Assert pass + detail matches `/^schema at migration \d+\/\d+$/`. The exact N depends on how many migrations exist; assert the two numbers in the string are EQUAL via regex capture group.
      3. `'returns fail when dbCount < fileCount (missing migration)'` — open createInMemoryDb(), then `db.prepare('DELETE FROM __drizzle_migrations').run()`. Assert fail + detail mentions 'schema at migration 0/'.
      4. `'returns fail when dbCount > fileCount (orphaned row)'` — open createInMemoryDb(), insert a synthetic row: `db.prepare("INSERT INTO __drizzle_migrations (hash, created_at) VALUES ('synthetic-hash', '2026-01-01')").run()`. Assert fail + detail mentions 'extra rows' AND 'docs/install/troubleshooting.md#db_schema_version'.
      5. `'returns fail when table does not exist'` — open a fresh better-sqlite3 ':memory:' DB without running the migrator (use `new (await import('better-sqlite3')).default(':memory:')` directly OR pass a mock handle that throws). Assert fail + detail starts with 'probe threw:'.

    No `console.*`. No `drizzle-orm` import (the probe uses raw better-sqlite3 prepare/get for the COUNT query, paralleling migrate.ts line 169 + 262). The migrator file at src/infrastructure/db/migrate.ts itself uses raw prepare(); the probe matches this style.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/db-schema-version.test.ts</automated>
  </verify>
  <done>
    db-schema-version.ts + db-schema-version.test.ts exist; 5 tests green. The detail string includes the canonical `__drizzle_migrations` row count vs the on-disk `.sql` file count. The orphaned-row hint includes the troubleshooting-doc anchor `db_schema_version` (matches the H2 Plan 05-09 ships).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create probeDbWalSize + tests</name>
  <files>src/services/doctor/checks/db-wal-size.ts, src/services/doctor/checks/db-wal-size.test.ts</files>
  <behavior>
    - Exports `interface DbWalSizeProbeDeps { dbFile?: string }`.
    - Exports `probeDbWalSize(deps?: DbWalSizeProbeDeps): Promise<DoctorCheck>`.
    - Resolves `walPath = (deps?.dbFile ?? paths.dbFile) + '-wal'`.
    - Reads file size via `statSync(walPath, {throwIfNoEntry: false})`. If undefined (file missing → no WAL yet): pass with detail `'no -wal file (no WAL writes since last checkpoint)'`.
    - When size <= 33554432 (32 MiB): pass with detail `'WAL <Math.round(size/1024)>KB (<32MB threshold)'`.
    - When 33554432 < size <= 67108864 (64 MiB): warn with detail `'WAL <Math.round(size/1024/1024)>MB (>32MB; checkpoint is lagging)'`.
    - When size > 67108864: fail with detail `'WAL <Math.round(size/1024/1024)>MB exceeds journal_size_limit=64MB; run `recovery-ledger sync` to force a wal_checkpoint(TRUNCATE)'`.
    - When statSync throws (other than ENOENT which `throwIfNoEntry: false` suppresses): fail with `'wal size probe threw: <message>'`.
  </behavior>
  <read_first>
    - @src/infrastructure/db/connection.ts line ~77 (`journal_size_limit = 67108864` — the upper bound that fails the probe)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.5 + §Don't Hand-Roll (`node:fs.statSync(`${dbFile}-wal`)` — the cheapest read)
    - @src/infrastructure/config/paths.ts (paths.dbFile)
    - @src/services/doctor/checks/check-names.ts (CHECK_NAMES.DB_WAL_SIZE)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Pitfall §12 (unbounded WAL — what this probe surfaces)
  </read_first>
  <action>
    Create `src/services/doctor/checks/db-wal-size.ts`:
      Imports: `import { statSync } from 'node:fs';`, `import type { DoctorCheck } from '../index.js';`, `import { CHECK_NAMES } from './check-names.js';`, `import { paths } from '../../../infrastructure/config/paths.js';`.
      File-level consts (NOT inside the function — easier to find/tweak per Specifics line 267 pattern):
        const WAL_WARN_BYTES = 32 * 1024 * 1024;   // 32 MiB
        const WAL_FAIL_BYTES = 64 * 1024 * 1024;   // 64 MiB (matches journal_size_limit = 67108864)
      Export `interface DbWalSizeProbeDeps { dbFile?: string }`.
      Export `async function probeDbWalSize(deps?: DbWalSizeProbeDeps): Promise<DoctorCheck>`:
        Try block:
          const walPath = (deps?.dbFile ?? paths.dbFile) + '-wal';
          const stat = statSync(walPath, { throwIfNoEntry: false });
          if (stat === undefined) → pass with detail 'no -wal file (no WAL writes since last checkpoint)'.
          const sizeMB = Math.round(stat.size / 1024 / 1024);
          if (stat.size <= WAL_WARN_BYTES) → pass with detail `'WAL ${Math.round(stat.size/1024)}KB (<32MB threshold)'`.
          else if (stat.size <= WAL_FAIL_BYTES) → warn with detail `'WAL ${sizeMB}MB (>32MB; checkpoint is lagging)'`.
          else → fail with detail `'WAL ${sizeMB}MB exceeds journal_size_limit=64MB; run `recovery-ledger sync` to force a wal_checkpoint(TRUNCATE)'`.
        Catch err: fail with `'wal size probe threw: <err.message>'`.

    No `console.*`. No `drizzle-orm`. No bare `fetch(`. The use of `node:fs.statSync` mirrors Phase 3's existing patterns (e.g., migrator's backup-existence checks).

    Create `src/services/doctor/checks/db-wal-size.test.ts` with 4 cases:
      1. `'returns pass when no WAL file present'` — use a tmp directory + a `deps.dbFile` that does NOT have a `<file>-wal` companion. Assert pass + detail mentions 'no -wal file'.
      2. `'returns pass for small WAL (<32MB)'` — `mkdtempSync` from os.tmpdir, write a small file at `<tmp>/db.sqlite-wal` (e.g., `writeFileSync(walPath, Buffer.alloc(100))`), call probe with `dbFile: <tmp>/db.sqlite`. Assert pass + detail mentions 'KB' and '<32MB'.
      3. `'returns warn for WAL in 32MB-64MB range'` — write a 40MB buffer (`Buffer.alloc(40 * 1024 * 1024)`) at the wal path. Assert warn + detail mentions 'MB' and 'checkpoint is lagging'.
      4. `'returns fail for WAL above 64MB'` — write a 70MB buffer. Assert fail + detail mentions 'exceeds journal_size_limit=64MB' and 'wal_checkpoint(TRUNCATE)'.

    Tests use `mkdtempSync(join(tmpdir(), 'rl-walsize-'))` for isolation; cleanup via `rmSync(tmp, {recursive: true})` in afterAll.

    Note: writing a 70MB buffer in a unit test is heavier than ideal but unavoidable for honest threshold coverage. Vitest's default timeout (5s) is enough on any modern disk. If CI flakes, the executor adds `it.concurrent: false` to this specific block or uses `vi.spyOn(fs, 'statSync')` to inject a fake `{size: 70*1024*1024}` — the spy alternative is acceptable and arguably more deterministic. The executor picks one approach and documents the choice in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/db-wal-size.test.ts</automated>
  </verify>
  <done>
    db-wal-size.ts + db-wal-size.test.ts exist; 4 tests green. Thresholds are file-level consts at 32MB / 64MB matching Phase 3 D-30. No `console.*`, no `drizzle-orm`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe → injected sqlite handle | The handle is constructed in `bootstrap()` (Phase 3 chokepoint) and threaded via `RunDoctorOptions.sqlite`. Probes do not open their own connections. |
| Probe → filesystem (db_schema_version, db_wal_size) | Reads only — `readdirSync` for migration files, `statSync` for WAL file, no writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-T3 | Tampering | A future probe that mutates DB state | mitigate | Convention enforced via review: every Phase 5 probe is read-only. `db_integrity` uses `pragma('integrity_check')` (read-only). `db_schema_version` uses `SELECT` only. Tests cover the canonical pass + fail arms — a mutation-introducing change would break the in-memory-db tests. |
| T-05-I3 | Information Disclosure | `db_schema_version` detail string includes the backup-path absolute path | accept | Single-user local tool; the backup path is on the user's own machine. The Phase 4 D-30 sanitizer pipeline does NOT alter file paths (only Bearer/JWT/Authorization patterns) so the path surfaces verbatim — which is the intended remediation hint. |
| T-05-D2 | DoS | `probeDbWalSize` reads a potentially-large WAL file via statSync | accept | `statSync` reads inode metadata only, not the file body. O(1) regardless of WAL size. The 70MB buffer is only allocated in tests, which run in CI/local on machines with abundant memory. |
</threat_model>

<verification>
After all 4 tasks complete:

```sh
# All 4 probe tests green
npx vitest run src/services/doctor/checks/db-open.test.ts src/services/doctor/checks/db-integrity.test.ts src/services/doctor/checks/db-schema-version.test.ts src/services/doctor/checks/db-wal-size.test.ts

# All 10 grep gates green
bash scripts/ci-grep-gates.sh

# Type system clean
npx tsc --noEmit

# Build still works
npm run build
```
</verification>

<success_criteria>
- 4 probe files exist at `src/services/doctor/checks/db-{open,integrity,schema-version,wal-size}.ts`.
- 4 test files exist alongside with the documented per-arm coverage (3+4+5+4 = 16 test cases total).
- Each probe is independently testable; no probe imports from another probe.
- No `console.*` in any new file (Gate B).
- No `drizzle-orm` import in any new probe file (Gate G — the probes use raw better-sqlite3 pragma/prepare APIs, not the Drizzle builder).
- All 10 grep gates green.
- `db_schema_version` detail strings include the literal anchor `db_schema_version` (for the Plan 05-09 troubleshooting H2 anchor).
- `db_wal_size` thresholds are file-level consts matching Phase 3 D-30's `journal_size_limit=67108864`.
</success_criteria>

<output>
Create `.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-03-SUMMARY.md` when done summarizing: 4 DB probes + 4 test files (16 cases total), thresholds + remediation hints documented. Plan 05-06 wires all 4 into runDoctor() alongside the other Wave 1 probes.
</output>
