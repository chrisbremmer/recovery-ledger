---
phase: 05-doctor-polish-install-guide-20-minute-setup-validation
plan: 11
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
  - 05-06
  - 05-07
  - 05-08
  - 05-09
  - 05-10
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06

must_haves:
  truths:
    - "Full Vitest suite green; total runtime under the 90s D-33 budget per Phase 4 precedent (stopwatch test excluded per D-13)"
    - "All 10 grep gates green (A through J — no new gates added in Phase 5)"
    - "D-29 attestation matrix preserved: tools.length === 8 + resources.length === 6 + prompts.length === 4 (Phase 5 added zero MCP surface)"
    - "D-21 attestation matrix verified: src/mcp/sanitize.ts + src/mcp/register.ts + src/mcp/register-resource.ts + src/mcp/register-prompt.ts UNMODIFIED across Phases 4→5 (git diff vs Phase 4 close commit returns empty)"
    - "DOC-01..DOC-06 (6 IDs) flipped to Complete in REQUIREMENTS.md with VALIDATION.md test-file references"
    - "ROADMAP.md Phase 5 row flipped to [x] with completion date + 11-plan count; v1.0 milestone complete (44 + 6 = 50/50 requirements)"
    - "STATE.md records phase-5 close: 6 new REQ-IDs done, test count + runtime, plan count (11), key learnings, milestone v1.0 complete"
    - "05-VALIDATION.md frontmatter flipped to nyquist_compliant: true + wave_0_complete: true + closed: <date>; per-task verification map populated with the test files that landed across Plans 05-01..05-10"
    - "Stopwatch workflow's most-recent run is green on both macos-latest + ubuntu-latest (attestation evidence — link in STATE.md)"
    - "README.md references INSTALL.md (carried forward from Plan 05-08)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 5 close entry mirroring Phase 4 04-12 precedent"
      contains: "Phase 5"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 [x] + completion date + 11 plans listed; coverage table at 50/50"
      contains: "[x] **Phase 5"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOC-01..06 flipped to Complete with Verified-by references"
      contains: "DOC-01"
    - path: ".planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md"
      provides: "Frontmatter nyquist_compliant: true; per-task map populated"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md"
      via: "Each Complete REQ-ID has a Verified-by reference from VALIDATION.md per-task map"
      pattern: "DOC-"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "STATE references the ROADMAP Phase 5 completion line + the v1.0 milestone"
      pattern: "Phase 5"
---

<objective>
Phase-close plan mirroring Phase 4's `04-12-phase-close-PLAN.md` precedent. After 10 prior plans land, this plan:

1. Runs the full Vitest suite + asserts green within the 90s budget per Phase 4 D-33 (stopwatch EXCLUDED per D-13 — it has its own workflow).
2. Runs all 10 grep gates (A through J) + asserts exit 0.
3. Verifies D-29 attestation: tools.length === 8 + resources.length === 6 + prompts.length === 4 (unchanged from Phase 4).
4. Verifies D-21 attestation: sanitize.ts + register.ts + register-resource.ts + register-prompt.ts UNMODIFIED across Phases 4→5 (git diff against Phase 4 close commit).
5. Flips 6 REQ-IDs to Complete in REQUIREMENTS.md (DOC-01..06) with VALIDATION.md test-file references.
6. Flips Phase 5 row in ROADMAP.md to `[x]` with completion date + 11-plan count; updates Coverage table to 50/50.
7. Records the phase-5 close + v1.0 milestone in STATE.md per Phase 4 precedent.
8. Flips 05-VALIDATION.md frontmatter to nyquist_compliant: true + wave_0_complete: true; populates the per-task verification map.
9. Confirms the stopwatch workflow's latest run is green on both macos-latest + ubuntu-latest (attestation only — the workflow exists from Plan 05-10).
10. Optional: `agent_docs/learnings.md` update if the comment-vs-grep-gate convention deviated in Phase 5 (per the deferred-ideas list).

This plan has ONE checkpoint (Task 5 — the user reviews final state before merge). All other tasks are autonomous attestation + doc-flip mechanics.

Output: 4 updated planning docs. NO source code changes (the source landed in 05-01..05-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-12-PLAN.md
@.planning/phases/03-data-model-db-layer-sync-loop/03-13-phase-close-PLAN.md
@agent_docs/learnings.md
@scripts/ci-grep-gates.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite attestation + D-29 + D-21 verification</name>
  <files>(no files modified — verification-only)</files>
  <read_first>
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-20 + D-21 + D-22 (zero new MCP surface; sanitize+register unmodified; phase-close mirrors Plan 04-12)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-12-PLAN.md (the precedent close plan — the attestation matrix shape)
    - @scripts/ci-grep-gates.sh (the 10 gates A-J — Phase 5 adds no new gates per D-22)
  </read_first>
  <action>
    Run, in order, recording the results in the Plan 05-11 SUMMARY:

      1. `npm test` — full Vitest suite. Assert exit 0. Record total runtime; assert under 90s per D-33 (Phase 4 baseline ≈ 80s post-Plan 04-12; Phase 5 adds ~9 new probe test files + 2 contract tests + 1 generator test ≈ ~5-10s additional; comfortable headroom).
      2. `bash scripts/ci-grep-gates.sh` — all 10 gates. Assert exit 0.
      3. `npx tsc --noEmit` — strict TS green.
      4. `npm run build` — tsup builds dist/ cleanly. Assert exit 0.
      5. **D-29 attestation** (carried forward from Phase 4): `npx vitest run tests/integration/mcp-runtime.test.ts`. Confirm `tools.length === 8`, `resources.length === 6`, `prompts.length === 4` assertions still pass — Phase 5 added zero new MCP surface.
      6. **D-21 attestation matrix**: `git diff <phase-4-close-commit>..HEAD -- src/mcp/sanitize.ts src/mcp/register.ts src/mcp/register-resource.ts src/mcp/register-prompt.ts` MUST return empty. Phase 4 close commit per recent commits is the merge commit of Plan 04-12 (audit at task time via `git log --oneline | grep '04-12\|Phase 4 close'`; the exact SHA is the last commit on the Phase 4 close PR). If diff is non-empty: STOP and investigate — D-21 has been violated.
      7. **14-check smoke**: `npm run build && node dist/cli.mjs doctor --offline 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['checks'])==14, f'expected 14, got {len(d[\"checks\"])}'"`. Confirms Plan 05-06 wiring still works end-to-end.
      8. **Troubleshooting + api-gap parity tests**: `npx vitest run tests/contract/troubleshooting-coverage.test.ts tests/contract/api-gap-md-parity.test.ts`. Both must be green (forcing functions in place).
      9. **Stopwatch workflow latest run**: `gh workflow view setup-stopwatch.yml --json` OR check via the GitHub web UI; confirm the most-recent successful run includes both `macos-latest` and `ubuntu-latest`. Record the SHA + run URL in the SUMMARY for traceability. If no successful run exists yet (the workflow may not have triggered on a PR touching its path filter), trigger one manually: open a PR touching `package.json` (e.g., a trivial script reorder + revert) and observe the workflow. Document the trigger in the SUMMARY.

    If ANY step fails: STOP, fix the underlying issue, re-run. The phase does NOT close on warnings — only on green.
  </action>
  <verify>
    <automated>npm test && bash scripts/ci-grep-gates.sh && npx tsc --noEmit && npm run build && npx vitest run tests/integration/mcp-runtime.test.ts tests/contract/troubleshooting-coverage.test.ts tests/contract/api-gap-md-parity.test.ts</automated>
  </verify>
  <done>
    All 9 verification steps pass. Phase 5 is technically ready to close pending the doc flips in Tasks 2-4. Stopwatch workflow's latest run is documented (URL + SHA).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 6 REQ-IDs to Complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - @.planning/REQUIREMENTS.md (current state — DOC-01..06 are still `[ ]`)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md (the per-task map populated by Task 4 below — the canonical test-file mapping per REQ-ID)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-12-PLAN.md Task 2 (the precedent flip shape — REQ-ID format + Verified-by line)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`:

      1. For each of the 6 DOC-* REQ-IDs:
         a. Change the leading checkbox from `[ ]` to `[x]` in the §Diagnostics & Setup section.
         b. In the §Traceability table at the bottom, change `| DOC-XX | Phase 5 | Pending |` to `| DOC-XX | Phase 5 | Complete (Plan 05-NN..05-NN, YYYY-MM-DD — Verified by <test_file_refs>) |`.

      2. Test-file mapping for each DOC-ID (sourced from VALIDATION.md per-task map after Task 4):
         - DOC-01 (recovery-ledger doctor checks) → Plans 05-02..05-06; Verified by `src/services/doctor/index.test.ts` (14-check assertion) + per-probe test files (whoop-roundtrip.test.ts + db-{open,integrity,schema-version,wal-size}.test.ts + last-sync-recency.test.ts + most-recent-scored-day.test.ts + data-quality-counts.test.ts + concurrent-writers-stress.test.ts)
         - DOC-02 (structured exit codes + troubleshooting map) → Plans 05-01 (DOCTOR_EXIT_CODES preserved per D-04) + 05-09; Verified by `tests/contract/troubleshooting-coverage.test.ts` + `src/cli/commands/doctor.ts` DOCTOR_EXIT_CODES const + `src/cli/commands/doctor.test.ts` (if exists)
         - DOC-03 (API-gap documentation) → Plan 05-07; Verified by `tests/contract/api-gap-md-parity.test.ts` + `scripts/generate-api-gap-md.test.ts`
         - DOC-04 (Install guide) → Plans 05-08 + 05-09; Verified by `tests/contract/troubleshooting-coverage.test.ts` + manual file-existence checks (`INSTALL.md`, `docs/install/{claude-code,claude-desktop,cursor,launchd,whoop-app}.md`)
         - DOC-05 (launchd template) → Plan 05-08; Verified by `templates/com.recovery-ledger.daily-sync.plist` (plutil -lint passes) + `docs/install/launchd.md` existence
         - DOC-06 (<20-min stopwatch) → Plan 05-10; Verified by `tests/integration/setup-stopwatch.test.ts` (env-gated; latest CI run green on macos-latest + ubuntu-latest — record the workflow run URL)

      3. Update the Coverage summary section at the bottom: change `Complete: 44 / 50` → `Complete: 50 / 50`. Update `Remaining: 6 / 50 (DOC-01..06 → Phase 5)` → `Remaining: 0 / 50 (v1.0 complete)`.

      4. Add a v1.0 completion marker at the bottom of the §v1 Requirements section: a one-line `> *v1.0 complete: <date> — 50 / 50 requirements done across Phases 1+2+3+4+5.*`.

      5. The §v2 Requirements section is UNCHANGED (V2-01..V2-10 stay as-is).
      6. The §Out of Scope table is UNCHANGED.

    Use today's date for completion dates (resolve at task time — the executor stamps the actual current date, not a placeholder).
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*DOC-" .planning/REQUIREMENTS.md | grep -q "^6$" && grep -q "v1.0 complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    All 6 DOC-* REQ-IDs flipped to Complete with Verified-by references. Coverage table shows 50/50. v1.0 completion marker present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip ROADMAP.md Phase 5 to [x] + update Progress + Coverage</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - @.planning/ROADMAP.md (current — Phase 5 row is `[ ]`, details section ends with "Plans: TBD")
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-22 (phase-close mirrors 04-12)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-12-PLAN.md Task 3 (the precedent ROADMAP flip)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`:

      1. Line 14 (Phase 5 row in §Phases): change `- [ ] **Phase 5: ...` → `- [x] **Phase 5: ...` and append ` — completed YYYY-MM-DD` (today's date).

      2. §Phase 5 details (lines 108-118):
         - Change `**Plans**: TBD` → `**Plans**: 11 plans`.
         - Add an enumerated Plans list (per Phase 4 precedent shape):
           ```
           Plans:
           - [x] 05-01-PLAN.md — Wave 0 scaffolding: CHECK_NAMES + RunDoctorOptions + CLI flags + MCP inputSchema + repo helpers + reserved directories (DOC-01, DOC-02)
           - [x] 05-02-PLAN.md — whoop_roundtrip probe + 5 test cases (DOC-01)
           - [x] 05-03-PLAN.md — 4 DB probes (db_open, db_integrity, db_schema_version, db_wal_size) + 16 test cases (DOC-01)
           - [x] 05-04-PLAN.md — 3 sync-recency/data-quality probes + 14 test cases (DOC-01)
           - [x] 05-05-PLAN.md — concurrent_writers_stress probe + worker + 3 test cases (DOC-01)
           - [x] 05-06-PLAN.md — runDoctor() extended 5→14 probes + bootstrap composition wiring + 14-check smoke (DOC-01, DOC-02)
           - [x] 05-07-PLAN.md — API-gap markdown generator + parity contract test + npm script (DOC-03)
           - [x] 05-08-PLAN.md — INSTALL.md + 5 docs/install/*.md + launchd .plist template + README link (DOC-04, DOC-05)
           - [x] 05-09-PLAN.md — troubleshooting.md (14 sections) + 3-test contract enforcement (DOC-02, DOC-04)
           - [x] 05-10-PLAN.md — env-gated setup-stopwatch.test.ts + dedicated CI workflow (DOC-06)
           - [x] 05-11-PLAN.md — phase close (full-suite green + 10 gates + REQ flips + STATE/ROADMAP/VALIDATION close) (all 6 REQ-IDs + v1.0 milestone)
           ```

      3. §Progress table: update the Phase 5 row to `| 5. Doctor Polish, Install Guide & <20-Minute Setup Validation | 11/11 | Complete | YYYY-MM-DD |`.

      4. §Coverage section: update the line about complete/remaining requirements to reflect 50/50 + v1.0 milestone complete. Update §Coverage's `Complete: 44 / 50` to `Complete: 50 / 50`.

      5. Last-updated line at the bottom (currently dated 2026-05-20): update to today's date + plan totals (e.g., `50 / 50 v1 requirements complete across Phases 1+2+3+4+5 — milestone v1.0 complete`).

    Use today's date for completion dates. No placeholders.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*Phase 5" .planning/ROADMAP.md | grep -q "^1$" && grep -q "50 / 50" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    Phase 5 row is `[x]`. Details section lists all 11 plans. Progress table updated. Coverage at 50/50. Last-updated line reflects v1.0 completion.
  </done>
</task>

<task type="auto">
  <name>Task 4: Record phase-5 close in STATE.md + flip 05-VALIDATION.md frontmatter</name>
  <files>.planning/STATE.md, .planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md</files>
  <read_first>
    - @.planning/STATE.md (current state — find the Phase 4 close section + the appropriate position to append Phase 5 close)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md (current draft frontmatter — Plan 05-11 flips it)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-12-PLAN.md Task 3 (the STATE entry shape precedent)
  </read_first>
  <action>
    ### STATE.md edits

    Append a new "Phase 5 close" section after the Phase 4 close section. Mirror Phase 4 04-12's close-entry shape:

      ```
      ## Phase 5 close — YYYY-MM-DD

      Phase 5 (Doctor Polish, Install Guide & <20-Minute Setup Validation) closed with all 6 REQ-IDs Complete:
      - DOC-01 (14 doctor checks across 5 existing + 9 new)
      - DOC-02 (structured 0/1/2 exit codes + troubleshooting.md 1:1 mapping)
      - DOC-03 (API-gap markdown generated from src/services/api-gap/data.ts)
      - DOC-04 (install guide: INSTALL.md + docs/install/{claude-code,claude-desktop,cursor,launchd,whoop-app,troubleshooting,api-gap}.md)
      - DOC-05 (launchd .plist template + sed+launchctl docs)
      - DOC-06 (env-gated <20-min stopwatch test + dedicated CI workflow on macos-latest + ubuntu-latest)

      Plans: 11 across 5 waves (0: scaffolding ×1; 1: 9 probes across 4 plans; 2: doctor orchestration + 3 docs/test plans; 3: stopwatch; 4: close).
      Tests: <N> total, <T>s runtime (budget 90s). Stopwatch test excluded from default suite per D-13.
      Gates: 10 of 10 green (A through J — no new gates needed per D-22; Phase 5 reuses Gates A/B/C/F/G).
      Attestation matrices:
        D-29 (carried from Phase 4): tools.length === 8 + resources.length === 6 + prompts.length === 4.
        D-21 (new): src/mcp/sanitize.ts + src/mcp/register.ts + src/mcp/register-resource.ts + src/mcp/register-prompt.ts UNMODIFIED across Phases 4→5.
      Stopwatch workflow: latest run green on macos-latest + ubuntu-latest at <commit-sha> (<run-url>).

      Key learnings:
      - The dep-injection seam for DB-touching probes (RunDoctorOptions.{sqlite, repos}) paid off — same probe contract surfaces in unit tests (mocked deps), CLI doctor (bootstrap-supplied deps), and MCP whoop_doctor (the same bootstrap-supplied deps through the Phase 4 04-10 chokepoint).
      - In-process command invocation in the stopwatch test (vs spawned children) sidestepped the MSW interception gap (D-14) while still preserving the npm install + tsup build cost in the timed boundary — the dominant friction the test measures.
      - The parity contract test pattern from api-gap-md-parity.test.ts and troubleshooting-coverage.test.ts can generalize for any future docs-mirror-source artifact (e.g., a future schema reference doc generated from drizzle schema).
      - D-04's lock of the 0/1/2 exit-code floor + the JSON `checks[].name` structure is the right call — no consumer of the doctor exit code has emerged that wanted finer structure, and the troubleshooting.md keyed by name gives the lookup affordance per-check anyway.

      Status: v1.0 complete. ready-for-real-use.
      ```

      Replace `<N>`, `<T>s`, `<commit-sha>`, `<run-url>`, and the date with the actual values captured from Task 1's verification + Task 1 step 9's stopwatch-workflow lookup.

    ### 05-VALIDATION.md edits

    1. Frontmatter:
       - `status: draft` → `status: complete`.
       - `nyquist_compliant: false` → `nyquist_compliant: true`.
       - `wave_0_complete: false` → `wave_0_complete: true`.
       - Add `closed: YYYY-MM-DD`.

    2. Per-Task Verification Map — populate the table with entries for every task that landed across Plans 05-01..05-10. Use the precedent Vitest command from each plan's `<verify>` block. Example rows:
       - `05-01-T1 | 05-01 | 0 | DOC-01 | unit (type-system) | npx vitest run src/services/doctor | (multiple) | ✅ green`
       - `05-02-T1 | 05-02 | 1 | DOC-01 | unit | npx vitest run src/services/doctor/checks/whoop-roundtrip.test.ts | yes | ✅ green`
       - ... (continue for all ~20 task entries across the 10 plans)

       Use today's date for the green-status timestamp.

    3. Validation Sign-Off section: flip each `[ ]` to `[x]`. Conditions met:
       - All tasks have automated verify
       - Sampling continuity preserved
       - Wave 0 covers all MISSING references (Plan 05-01 added repo helpers; no MISSING remaining)
       - No watch-mode flags
       - Feedback latency under ~10s for default suite
       - nyquist_compliant: true

    4. Bottom line: `**Approval:** pending` → `**Approval:** approved on YYYY-MM-DD; phase closed per 05-11-PLAN.md.`
  </action>
  <verify>
    <automated>grep -c "Phase 5 close" .planning/STATE.md | grep -qE "^[1-9]+$" && grep -q "nyquist_compliant: true" .planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md</automated>
  </verify>
  <done>
    STATE.md has Phase 5 close section with all attestation matrices + key learnings + v1.0 marker. 05-VALIDATION.md frontmatter flipped to complete + nyquist_compliant + wave_0_complete. Per-task map populated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User review of v1.0 close state</name>
  <what-built>
    All 11 Phase 5 plans complete. 6 DOC-* REQ-IDs flipped. ROADMAP.md / STATE.md / VALIDATION.md updated. 10 grep gates green. D-29 + D-21 attestations verified. Stopwatch workflow green on both platforms. v1.0 milestone is COMPLETE (50/50 v1 requirements done).
  </what-built>
  <how-to-verify>
    1. `git status` — clean working tree (no unstaged changes after the doc flips committed).
    2. `git log --oneline -30` — recent commits show the Phase 5 plan close history.
    3. `cat .planning/STATE.md | tail -50` — confirm the Phase 5 close section is present + lists all 6 DOC-IDs + records the v1.0 marker.
    4. `cat .planning/ROADMAP.md | head -30` — Phase 5 row is `[x]` with the correct completion date; coverage table at 50/50.
    5. `cat .planning/REQUIREMENTS.md | grep -A1 "DOC-0"` — all 6 DOC-IDs are `[x]`.
    6. `npm test` — final sanity check (full suite green).
    7. `bash scripts/ci-grep-gates.sh` — final gate green.
    8. **Hands-on 14-check smoke**: `node dist/cli.mjs doctor --offline --text` — see all 14 checks render. Most will be `fail` on a developer machine that hasn't init/auth/sync'd; that's expected. Confirm the 14 names and the troubleshooting H2 anchors match.
    9. **Hands-on install dry-run**: read `INSTALL.md` start-to-finish. Try the quickstart from a temp directory (using a fresh `RECOVERY_LEDGER_HOME`) up through `recovery-ledger doctor` — if all steps work and the doctor output makes sense, the install guide is operational.
    10. **MCP Inspector verification** (optional but listed in 05-VALIDATION.md §Manual-Only): `npx @modelcontextprotocol/inspector node dist/mcp.mjs` — click `whoop_doctor`, confirm the response includes 14 checks + the dual `structuredContent` + `content` shape per MCP-02.
    11. **Stopwatch workflow URL** captured in STATE.md — open it; confirm green on both `macos-latest` and `ubuntu-latest`; record the actual elapsed times in the SUMMARY (typically 3-6 min on macOS, 5-8 min on Ubuntu cold; budget 20 min).

    Approve to close Phase 5 + v1.0 OR call out issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-close documentation → future agents (post-v1.0) | The STATE.md + ROADMAP.md + VALIDATION.md entries are the canonical source of truth for any future v1.1+ work or for the V2 backlog. Misrecording the REQ-ID flip or attestation matrix would mislead future planning. |
| Phase-close attestations → CI gate | Tasks 1+5 (Task 1's automated checks + Task 5's human review) are the load-bearing layers. The git diff against Phase 4 close commit is the cryptographic-style attestation that sanitize.ts + register*.ts didn't drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-T10 | Tampering | Phase-close attestations could be skipped or fudged | mitigate | Task 1 runs the full attestation matrix BEFORE the doc flips. D-21 git-diff verification is the binary check (any non-empty diff aborts). The human checkpoint (Task 5) gates the final close — no merge without explicit approval. |
| T-05-R3 | Repudiation | Future contributor could claim "Phase 5 was supposed to have more X" | mitigate | The 6 DOC-IDs in REQUIREMENTS.md are the canonical contract; v1.0 closing at 50/50 v1 requirements is the immutable milestone. v2.0 work goes through a new planning cycle. |
| T-05-I11 | Information Disclosure | STATE.md key-learnings include implementation details | accept | Same posture as Phase 4 close — the planning history is internal / repository-local; not exposed externally. |
</threat_model>

<verification>
After Tasks 1-4 complete (Task 5 is the human gate):

```sh
# Final attestation matrix
npm test
bash scripts/ci-grep-gates.sh
npx tsc --noEmit
npm run build
git diff <phase-4-close-sha>..HEAD -- src/mcp/sanitize.ts src/mcp/register.ts src/mcp/register-resource.ts src/mcp/register-prompt.ts | wc -l   # MUST be 0 (D-21)

# MCP attestation (D-29 carried forward)
npx vitest run tests/integration/mcp-runtime.test.ts

# 14-check smoke + parity tests + troubleshooting coverage
npm run build && node dist/cli.mjs doctor --offline | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['checks'])==14"
npx vitest run tests/contract/api-gap-md-parity.test.ts tests/contract/troubleshooting-coverage.test.ts

# Doc state
grep "\[x\] \*\*Phase 5" .planning/ROADMAP.md
grep "Phase 5 close" .planning/STATE.md
grep "nyquist_compliant: true" .planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-VALIDATION.md
grep -c "\[x\] \*\*DOC-" .planning/REQUIREMENTS.md   # 6
```
</verification>

<success_criteria>
- Full Vitest suite green; runtime under 90s.
- All 10 grep gates green (A through J).
- D-29 attestation: tools=8 + resources=6 + prompts=4 (unchanged from Phase 4).
- D-21 attestation: sanitize.ts + register*.ts UNMODIFIED via git diff vs Phase 4 close commit.
- 6 DOC-* REQ-IDs flipped to Complete in REQUIREMENTS.md with Verified-by references.
- ROADMAP.md Phase 5 row `[x]` + 11 plans listed + Coverage at 50/50 + v1.0 milestone recorded.
- STATE.md Phase 5 close + v1.0 milestone marker.
- 05-VALIDATION.md frontmatter: complete + nyquist_compliant: true + wave_0_complete: true.
- Stopwatch workflow's most-recent run green on macos-latest + ubuntu-latest.
- User approval recorded via Task 5 checkpoint.
- v1.0 ships: 50 / 50 v1 requirements complete.
</success_criteria>

<output>
Create `.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-11-SUMMARY.md` when done summarizing: 6 REQ-IDs complete, 10 gates green, D-29 + D-21 attested, Phase 5 closed, v1.0 milestone shipped (50/50). Document the stopwatch workflow URL + per-platform elapsed times. Note any final key-learnings from the v1.0 close.
</output>
