---
phase: 05-doctor-polish-install-guide-20-minute-setup-validation
plan: 04
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/services/doctor/checks/last-sync-recency.ts
  - src/services/doctor/checks/last-sync-recency.test.ts
  - src/services/doctor/checks/most-recent-scored-day.ts
  - src/services/doctor/checks/most-recent-scored-day.test.ts
  - src/services/doctor/checks/data-quality-counts.ts
  - src/services/doctor/checks/data-quality-counts.test.ts
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "probeLastSyncRecency reads repos.syncRuns.latestFinished(); pass <=36h, warn <=7d, fail >7d OR no syncs yet"
    - "probeMostRecentScoredDay computes max across cycles+recoveries+sleeps latestScoredDate() (added in Plan 05-01); same 36h/7d/fail thresholds; null across all 3 → fail with 'no SCORED data yet' detail"
    - "probeDataQualityCounts ALWAYS returns status: 'pass' per D-02 #8 (informational visibility per Pitfall 19); detail string lists per-resource counts via countByScoreState() (added in Plan 05-01)"
    - "All 3 probes accept an optional `deps.repos` injection seam (per Open Questions §1 — bootstrap composition root passes the full Repos; absent the repos, return fail)"
    - "Thresholds are file-level consts named RECENCY_PASS_MS (36h) and RECENCY_WARN_MS (7d) — discoverable + tunable per Specifics line 267 pattern"
    - "Tests use in-memory repository mocks (deterministic per-resource return values); no real DB required for these 3 probes (the dep-injection seam keeps them pure)"
  artifacts:
    - path: "src/services/doctor/checks/last-sync-recency.ts"
      provides: "probeLastSyncRecency({repos?}) → DoctorCheck"
      contains: "probeLastSyncRecency"
    - path: "src/services/doctor/checks/most-recent-scored-day.ts"
      provides: "probeMostRecentScoredDay({repos?}) → DoctorCheck"
      contains: "probeMostRecentScoredDay"
    - path: "src/services/doctor/checks/data-quality-counts.ts"
      provides: "probeDataQualityCounts({repos?}) → DoctorCheck (always pass)"
      contains: "probeDataQualityCounts"
  key_links:
    - from: "src/services/doctor/checks/last-sync-recency.ts"
      to: "src/infrastructure/db/repositories/sync-runs.repo.ts"
      via: "repos.syncRuns.latestFinished() returns {finished_at, status} | null"
      pattern: "latestFinished"
    - from: "src/services/doctor/checks/most-recent-scored-day.ts"
      to: "src/infrastructure/db/repositories/{cycles,recovery,sleep}.repo.ts"
      via: "latestScoredDate() added in Plan 05-01 across all 3 repos"
      pattern: "latestScoredDate"
    - from: "src/services/doctor/checks/data-quality-counts.ts"
      to: "src/infrastructure/db/repositories/{cycles,recovery,sleep}.repo.ts"
      via: "countByScoreState() added in Plan 05-01 across all 3 repos"
      pattern: "countByScoreState"
---

<objective>
Ship 3 sync-recency and data-quality probes consumed by `runDoctor()`. All 3 read through the repository layer added/extended in Plan 05-01:

- `last_sync_recency` — surfaces "did sync run recently?" (D-02 #6); 36h pass / 7d warn / >7d fail.
- `most_recent_scored_day` — surfaces "do we have recent SCORED data?" (D-02 #7); same thresholds, distinct signal because sync can succeed with all-PENDING data.
- `data_quality_counts` — always-pass informational surface (D-02 #8) showing per-resource SCORED / PENDING_SCORE / UNSCORABLE / excluded counts. The Pitfall 19 visibility surface.

Purpose: these 3 probes are the "your data is healthy" cross-section. Together they make missing days proactively visible (RESEARCH §Pitfall §19) instead of waiting for the user to notice a stale review.

Output: 3 probe files + 3 test files (12 test cases total). Plan 05-06 wires all 3 into runDoctor() with the injected `repos` from the bootstrap composition root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md
@agent_docs/decisions/0001-mcp-stdout-purity.md
@agent_docs/decisions/0003-score-state-discipline.md
@src/services/doctor/checks/auth.ts
@src/services/doctor/checks/token-freshness.ts
@src/services/doctor/checks/check-names.ts
@src/services/doctor/index.ts
@src/infrastructure/db/repositories/sync-runs.repo.ts
@src/infrastructure/db/repositories/cycles.repo.ts
@src/infrastructure/db/repositories/recovery.repo.ts
@src/infrastructure/db/repositories/sleep.repo.ts

<interfaces>
<!-- The Repos shape these probes consume (subset). Read sync-runs.repo.ts line 65 for the exact latestFinished() return type. -->

From src/infrastructure/db/repositories/sync-runs.repo.ts:
  SyncRunsRepo.latestFinished(): { finished_at: string; status: 'ok'|'partial'|'failed' } | null

From src/infrastructure/db/repositories/cycles.repo.ts (post-Plan 05-01 additions):
  CyclesRepo.latestScoredDate(): string | null               (already exists at line 75)
  CyclesRepo.countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number}   (NEW in 05-01)

From src/infrastructure/db/repositories/recovery.repo.ts (post-Plan 05-01):
  RecoveryRepo.latestScoredDate(): string | null            (NEW in 05-01)
  RecoveryRepo.countByScoreState(): {...}                    (NEW in 05-01)

From src/infrastructure/db/repositories/sleep.repo.ts (post-Plan 05-01):
  SleepRepo.latestScoredDate(): string | null               (NEW in 05-01)
  SleepRepo.countByScoreState(): {...}                       (NEW in 05-01)

Probe-side shape (subset of the full Repos type from bootstrap.ts — the probe only requires the specific methods it calls):
  interface RecencyRepos {
    syncRuns: { latestFinished(): { finished_at: string; status: 'ok'|'partial'|'failed' } | null };
  }
  interface ScoredDayRepos {
    cycles: { latestScoredDate(): string | null };
    recovery: { latestScoredDate(): string | null };
    sleep: { latestScoredDate(): string | null };
  }
  interface DataQualityRepos {
    cycles: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} };
    recovery: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} };
    sleep: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} };
  }

Thresholds (per D-02 #6/#7):
  const RECENCY_PASS_MS = 36 * 60 * 60 * 1000    // 36h
  const RECENCY_WARN_MS = 7 * 24 * 60 * 60 * 1000 // 7d

Probe shape: every probe takes `(deps?: {<sub-repos>}, opts?: {clock?: () => Date})`. The `clock` injection seam keeps the threshold math deterministic in tests; production omits it and falls back to `new Date()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create probeLastSyncRecency + tests</name>
  <files>src/services/doctor/checks/last-sync-recency.ts, src/services/doctor/checks/last-sync-recency.test.ts</files>
  <behavior>
    - Exports `interface LastSyncRecencyDeps { repos?: { syncRuns: { latestFinished(): { finished_at: string; status: 'ok'|'partial'|'failed' } | null } } }`.
    - Exports `probeLastSyncRecency(deps?: LastSyncRecencyDeps, opts?: { clock?: () => Date }): Promise<DoctorCheck>`.
    - When `deps?.repos` undefined: fail with `'no repos injected — run from CLI to exercise sync-recency check'`.
    - When `latestFinished()` returns null: fail with `'no syncs yet — run `recovery-ledger sync`'`.
    - Computes `ageMs = (opts?.clock?.() ?? new Date()).getTime() - new Date(row.finished_at).getTime()`.
    - `ageMs <= RECENCY_PASS_MS` (36h): pass with detail `'last sync <formatDuration(ageMs)> ago (status: <row.status>)'`.
    - `ageMs <= RECENCY_WARN_MS` (7d): warn with detail `'last sync <formatDuration(ageMs)> ago — run `recovery-ledger sync` (status: <row.status>)'`.
    - `ageMs > RECENCY_WARN_MS`: fail with detail `'last sync <formatDuration(ageMs)> ago — exceeds 7d threshold; run `recovery-ledger sync`'`.
    - When the row's `status === 'failed'`: regardless of age, downgrade pass to warn (a recent failed sync is not a clean signal) — detail string mentions 'last sync failed <ago>'.
    - `formatDuration` inlined per the token-freshness.ts precedent (humanize ms → `Xd`, `Xh`, `Xm`).
  </behavior>
  <read_first>
    - @src/services/doctor/checks/token-freshness.ts (formatDuration helper precedent — copy the inline function OR factor it to a shared helper if convenient; the executor decides based on whether token-freshness already exports it; if not exported, inline a fresh copy here)
    - @src/infrastructure/db/repositories/sync-runs.repo.ts line 65-ish (latestFinished signature; the {finished_at, status} | null shape — Assumption A9 [VERIFIED])
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.6 (the verbatim threshold table)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-02 #6 (the 36h/7d thresholds + the "no syncs yet → fail" semantics)
    - @src/services/doctor/checks/check-names.ts (CHECK_NAMES.LAST_SYNC_RECENCY)
  </read_first>
  <action>
    Create `src/services/doctor/checks/last-sync-recency.ts`:
      Imports: `import type { DoctorCheck } from '../index.js';`, `import { CHECK_NAMES } from './check-names.js';`.
      File-level consts:
        const RECENCY_PASS_MS = 36 * 60 * 60 * 1000;            // 36h
        const RECENCY_WARN_MS = 7 * 24 * 60 * 60 * 1000;        // 7d
      Helper (inline): `function formatDuration(ms: number): string { ... }` — copy the shape from token-freshness.ts. Produce strings like `'2d 3h'`, `'5h 12m'`, `'18m'`, `'30s'`. If token-freshness.ts already exports formatDuration, IMPORT it instead of duplicating — verify before duplicating.

      Export `interface LastSyncRecencyDeps { repos?: { syncRuns: { latestFinished(): { finished_at: string; status: 'ok'|'partial'|'failed' } | null } } }`.

      Export `async function probeLastSyncRecency(deps?: LastSyncRecencyDeps, opts?: { clock?: () => Date }): Promise<DoctorCheck>`:
        1. If `!deps?.repos`: fail with 'no repos injected — run from CLI to exercise sync-recency check'.
        2. Try block:
           const row = deps.repos.syncRuns.latestFinished();
           if (row === null) → fail with 'no syncs yet — run `recovery-ledger sync`'.
           const now = opts?.clock?.() ?? new Date();
           const ageMs = now.getTime() - new Date(row.finished_at).getTime();
           const ago = formatDuration(ageMs);
           if (row.status === 'failed' && ageMs <= RECENCY_PASS_MS):
             → warn with detail `'last sync failed ${ago} ago — run `recovery-ledger sync` to retry'`.
           else if (ageMs <= RECENCY_PASS_MS):
             → pass with detail `'last sync ${ago} ago (status: ${row.status})'`.
           else if (ageMs <= RECENCY_WARN_MS):
             → warn with detail `'last sync ${ago} ago — run `recovery-ledger sync` (status: ${row.status})'`.
           else:
             → fail with detail `'last sync ${ago} ago — exceeds 7d threshold; run `recovery-ledger sync`'`.
        3. Catch err: fail with 'probe threw: <err.message>'.

      No `console.*`. No `drizzle-orm`. No `fetch(`. The probe is a pure consumer of the injected repo method.

    Create `src/services/doctor/checks/last-sync-recency.test.ts` with 6 cases (use `clock` injection to make all of them deterministic):
      Helper inline: `const NOW = new Date('2026-05-26T12:00:00Z'); const clock = () => NOW;`
      Helper inline: `function reposFor(latest: { finished_at: string; status: 'ok'|'partial'|'failed' } | null) { return { syncRuns: { latestFinished: () => latest } }; }`

      1. `'returns fail when no repos injected'` — assert fail + 'no repos injected'.
      2. `'returns fail when latestFinished returns null'` — `reposFor(null)`; assert fail + 'no syncs yet'.
      3. `'returns pass when sync is 12h old'` — `reposFor({finished_at: '2026-05-26T00:00:00Z', status: 'ok'})`; assert pass + detail mentions '12h' and 'status: ok'.
      4. `'returns warn when sync is 3d old'` — `reposFor({finished_at: '2026-05-23T12:00:00Z', status: 'ok'})`; assert warn + detail mentions '3d' and 'run `recovery-ledger sync`'.
      5. `'returns fail when sync is 10d old'` — `reposFor({finished_at: '2026-05-16T12:00:00Z', status: 'ok'})`; assert fail + detail mentions '10d' and 'exceeds 7d threshold'.
      6. `'returns warn when recent sync failed'` — `reposFor({finished_at: '2026-05-26T06:00:00Z', status: 'failed'})`; assert warn (NOT pass — failure overrides) + detail mentions 'last sync failed' and '6h'.

    No `console.*`. Tests pass deterministic `clock` for repeatability across timezones / CI runs.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/last-sync-recency.test.ts</automated>
  </verify>
  <done>
    last-sync-recency.ts + last-sync-recency.test.ts exist; 6 tests green. Thresholds are file-level consts. Detail strings include the verbatim remediation hints.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create probeMostRecentScoredDay + tests</name>
  <files>src/services/doctor/checks/most-recent-scored-day.ts, src/services/doctor/checks/most-recent-scored-day.test.ts</files>
  <behavior>
    - Exports `interface MostRecentScoredDayDeps { repos?: { cycles: { latestScoredDate(): string|null }; recovery: { latestScoredDate(): string|null }; sleep: { latestScoredDate(): string|null } } }`.
    - Exports `probeMostRecentScoredDay(deps?, opts?: {clock?: () => Date}): Promise<DoctorCheck>`.
    - When `deps?.repos` undefined: fail with 'no repos injected'.
    - Computes `latestPerResource = {cycles, recovery, sleep}` from each repo's latestScoredDate().
    - When all 3 return null: fail with 'no SCORED data yet — run `recovery-ledger sync`'.
    - Otherwise picks the MAX date string across the 3 (string compare works for yyyy-mm-dd because lexicographic and chronological order coincide), converts to an age vs the clock (`now.getTime() - new Date(maxDate + 'T00:00:00Z').getTime()`), applies the same 36h/7d thresholds as Task 1.
    - Detail string includes which resource(s) tied for the max (e.g., `'most recent SCORED day 2026-05-25 (cycles, recovery; sleep at 2026-05-24)'`). Use a concise format; the formatter prints multi-line detail strings OK per existing precedent.
    - Same threshold consts as Task 1 — reuse them by importing or duplicating; researcher's recommendation is "duplicate is fine for two-file scope" — choose either; document the choice.
  </behavior>
  <read_first>
    - @src/services/doctor/checks/last-sync-recency.ts (sibling probe — same threshold + dep-injection + clock pattern)
    - @src/infrastructure/db/repositories/cycles.repo.ts (latestScoredDate signature — existing; line 75)
    - @src/infrastructure/db/repositories/recovery.repo.ts (latestScoredDate added by Plan 05-01)
    - @src/infrastructure/db/repositories/sleep.repo.ts (latestScoredDate added by Plan 05-01)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.7 (the cross-resource MAX semantics; Assumption A2 resolved by Plan 05-01)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-02 #7 (distinct from #6 because sync can succeed with all-PENDING)
    - @agent_docs/decisions/0003-score-state-discipline.md (the SCORED-only filter is enforced in the repo layer per Phase 3 D-04)
    - @src/services/doctor/checks/check-names.ts (CHECK_NAMES.MOST_RECENT_SCORED_DAY)
  </read_first>
  <action>
    Create `src/services/doctor/checks/most-recent-scored-day.ts`:
      Imports: `import type { DoctorCheck } from '../index.js';`, `import { CHECK_NAMES } from './check-names.js';`.
      File-level consts (duplicated from last-sync-recency.ts — small enough that DRY-vs-locality favors duplication; the threshold tunability lives in BOTH files for findability):
        const RECENCY_PASS_MS = 36 * 60 * 60 * 1000;
        const RECENCY_WARN_MS = 7 * 24 * 60 * 60 * 1000;
      Optionally import `formatDuration` from `./token-freshness.js` if exported; if not, duplicate the inline helper.

      Export `interface MostRecentScoredDayDeps { repos?: { cycles: { latestScoredDate(): string|null }; recovery: { latestScoredDate(): string|null }; sleep: { latestScoredDate(): string|null } } }`.

      Export `async function probeMostRecentScoredDay(deps?: MostRecentScoredDayDeps, opts?: { clock?: () => Date }): Promise<DoctorCheck>`:
        1. If `!deps?.repos`: fail with 'no repos injected — run from CLI to exercise scored-day check'.
        2. Try block:
           const dates = {
             cycles: deps.repos.cycles.latestScoredDate(),
             recovery: deps.repos.recovery.latestScoredDate(),
             sleep: deps.repos.sleep.latestScoredDate(),
           };
           const nonNull = Object.entries(dates).filter(([, d]) => d !== null) as Array<[string, string]>;
           if (nonNull.length === 0) → fail with 'no SCORED data yet — run `recovery-ledger sync`'.
           const maxDate = nonNull.reduce((acc, [, d]) => (d > acc ? d : acc), '0000-00-00');
           const leaders = nonNull.filter(([, d]) => d === maxDate).map(([k]) => k);
           const trailers = nonNull.filter(([, d]) => d !== maxDate).map(([k, d]) => `${k} at ${d}`);
           const trailersStr = trailers.length > 0 ? `; ${trailers.join(', ')}` : '';

           const now = opts?.clock?.() ?? new Date();
           const ageMs = now.getTime() - new Date(maxDate + 'T00:00:00Z').getTime();
           const ago = formatDuration(ageMs);

           if (ageMs <= RECENCY_PASS_MS):
             → pass with detail `'most recent SCORED day ${maxDate} (${leaders.join(', ')}${trailersStr})'`.
           else if (ageMs <= RECENCY_WARN_MS):
             → warn with detail `'most recent SCORED day ${maxDate}, ${ago} ago — run `recovery-ledger sync` (${leaders.join(', ')}${trailersStr})'`.
           else:
             → fail with detail `'most recent SCORED day ${maxDate}, ${ago} ago — exceeds 7d threshold (${leaders.join(', ')}${trailersStr})'`.
        3. Catch err: fail with 'probe threw: <err.message>'.

    Create `src/services/doctor/checks/most-recent-scored-day.test.ts` with 5 cases:
      Helper: `const NOW = new Date('2026-05-26T12:00:00Z'); const clock = () => NOW;`
      Helper: `function reposFor(c: string|null, r: string|null, s: string|null) { return { cycles: { latestScoredDate: () => c }, recovery: { latestScoredDate: () => r }, sleep: { latestScoredDate: () => s } }; }`

      1. `'returns fail when no repos injected'`.
      2. `'returns fail when all three repos return null'` — `reposFor(null, null, null)`; assert fail + 'no SCORED data yet'.
      3. `'returns pass when max date is within 36h of clock'` — `reposFor('2026-05-25', '2026-05-25', '2026-05-24')`; clock NOW is 2026-05-26T12:00:00Z. Max date is 2026-05-25; age from 2026-05-25T00 to 2026-05-26T12 is 36h exactly OR slightly over — adjust the test fixture to clearly be ≤36h (use `reposFor('2026-05-26', '2026-05-26', '2026-05-25')` so age ≈ 12h). Assert pass + detail mentions '2026-05-26' and both 'cycles' and 'recovery' as leaders.
      4. `'returns warn when max date is 3d old'` — `reposFor('2026-05-23', '2026-05-23', '2026-05-22')`; clock NOW 2026-05-26T12:00:00Z. Age ≈ 3.5d → warn. Detail mentions '2026-05-23' + 'run `recovery-ledger sync`'.
      5. `'returns fail when max date is 10d old'` — `reposFor('2026-05-16', null, null)`; clock NOW 2026-05-26T12:00:00Z. Age ≈ 10d → fail. Detail mentions '2026-05-16' + 'exceeds 7d threshold' + 'cycles'.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/most-recent-scored-day.test.ts</automated>
  </verify>
  <done>
    most-recent-scored-day.ts + most-recent-scored-day.test.ts exist; 5 tests green. Probe computes the max across 3 resources, distinguishes leaders/trailers in the detail string, applies the same 36h/7d threshold ladder.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create probeDataQualityCounts + tests</name>
  <files>src/services/doctor/checks/data-quality-counts.ts, src/services/doctor/checks/data-quality-counts.test.ts</files>
  <behavior>
    - Exports `interface DataQualityCountsDeps { repos?: { cycles: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} }; recovery: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} }; sleep: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} } } }`.
    - Exports `probeDataQualityCounts(deps?: DataQualityCountsDeps): Promise<DoctorCheck>`.
    - When `deps?.repos` undefined: fail with `'no repos injected — run from CLI to exercise data-quality check'`. (Even though the probe is "always pass" when repos exist, a missing-repos invocation is a degenerate error — we surface it as fail so the user knows the check did not run.)
    - When repos injected: ALWAYS returns `status: 'pass'` per D-02 #8 (informational only). Detail string format:
      `'cycles: <c.scored> scored, <c.pending> pending, <c.unscorable> unscorable, <c.excluded> excluded; recovery: <r.scored> scored, ...; sleep: <s.scored> scored, ...'` (one segment per resource, semicolon-separated).
    - On throw inside the count gathering: still return `status: 'pass'` with detail `'count gathering partially failed: <err.message>'` — the pitfall-19 visibility goal trumps a strict fail; the user sees something rather than nothing. (Researcher-allowed deviation; the alternative is to fail closed — the executor MAY choose to return fail-on-throw if it better matches the convention of the other probes. Either is defensible; document the choice in the SUMMARY.)
  </behavior>
  <read_first>
    - @src/services/doctor/checks/last-sync-recency.ts (sibling probe — same dep-injection shape)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.8 (the always-pass posture; the format string)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-02 #8 + §Specifics line 273 (the verbatim detail-format spec)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Pitfall §19 (silent missing days — the why this probe exists)
    - @src/services/doctor/checks/check-names.ts (CHECK_NAMES.DATA_QUALITY_COUNTS)
  </read_first>
  <action>
    Create `src/services/doctor/checks/data-quality-counts.ts`:
      Imports: `import type { DoctorCheck } from '../index.js';`, `import { CHECK_NAMES } from './check-names.js';`.
      Export `interface DataQualityCountsDeps { repos?: { cycles: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} }; recovery: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} }; sleep: { countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} } } }`.

      Helper inline: `function formatResource(label: string, c: {scored:number; pending:number; unscorable:number; excluded:number}): string { return `${label}: ${c.scored} scored, ${c.pending} pending, ${c.unscorable} unscorable, ${c.excluded} excluded`; }`.

      Export `async function probeDataQualityCounts(deps?: DataQualityCountsDeps): Promise<DoctorCheck>`:
        1. If `!deps?.repos`: fail with 'no repos injected — run from CLI to exercise data-quality check'.
        2. Try block:
           const c = deps.repos.cycles.countByScoreState();
           const r = deps.repos.recovery.countByScoreState();
           const s = deps.repos.sleep.countByScoreState();
           const detail = [formatResource('cycles', c), formatResource('recovery', r), formatResource('sleep', s)].join('; ');
           return {name: CHECK_NAMES.DATA_QUALITY_COUNTS, status: 'pass', detail};
        3. Catch err: return {name, status: 'pass' (informational discipline), detail: 'count gathering partially failed: <err.message>'}. (See behavior note above; the executor MAY choose fail-on-throw — document the choice.)

    Create `src/services/doctor/checks/data-quality-counts.test.ts` with 3 cases:
      Helper: `function reposFor(c, r, s) { return { cycles: { countByScoreState: () => c }, recovery: { countByScoreState: () => r }, sleep: { countByScoreState: () => s } }; }`

      1. `'returns fail when no repos injected'` — assert fail + 'no repos injected'.
      2. `'returns pass with concatenated per-resource counts'` — `reposFor({scored:142,pending:3,unscorable:0,excluded:2}, {scored:140,pending:2,unscorable:1,excluded:0}, {scored:138,pending:5,unscorable:0,excluded:1})`. Assert status === 'pass' + detail equals exactly `'cycles: 142 scored, 3 pending, 0 unscorable, 2 excluded; recovery: 140 scored, 2 pending, 1 unscorable, 0 excluded; sleep: 138 scored, 5 pending, 0 unscorable, 1 excluded'`.
      3. `'returns pass with empty zeros when all repos are empty'` — `reposFor({scored:0,pending:0,unscorable:0,excluded:0}, ..., ...)`. Assert pass + detail mentions '0 scored' across all three resources.

    No `console.*`. No `drizzle-orm`. The detail string is the literal format documented in 05-CONTEXT.md §Specifics line 273.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/data-quality-counts.test.ts</automated>
  </verify>
  <done>
    data-quality-counts.ts + data-quality-counts.test.ts exist; 3 tests green. The always-pass posture is honored when repos are injected. The detail string format matches 05-CONTEXT.md §Specifics line 273 byte-for-byte.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe → injected repos | All 3 probes consume the repository layer via the dep-injection seam. Bootstrap composition root passes the production Repos; unit tests pass per-method mocks. |
| Probe → time source | `last_sync_recency` + `most_recent_scored_day` rely on a `clock` injection seam for deterministic tests. Production omits the seam and uses `new Date()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-I4 | Information Disclosure | `data_quality_counts` detail string emits per-resource row counts | accept | Counts are non-sensitive aggregate metrics; nothing about WHOOP data uniqueness leaks. The sanitizer pipeline already covers Bearer/JWT — these strings contain none. |
| T-05-T4 | Tampering | Repos are read-only methods (no side effects) | mitigate | The probes use `latestFinished()`, `latestScoredDate()`, `countByScoreState()` — all SELECT queries; no UPDATE/INSERT/DELETE. Tests assert pass + detail content but never verify mutation behavior because there is none to verify. |
| T-05-R1 | Repudiation | `last_sync_recency` reads sync_runs as authority | accept | The sync_runs table IS the audit trail (Phase 3 SYNC-05). If a sync row is fraudulent/missing the probe simply mirrors reality — the discrepancy surfaces in the doctor output. |
</threat_model>

<verification>
After all 3 tasks complete:

```sh
# 3 probe test files green
npx vitest run src/services/doctor/checks/last-sync-recency.test.ts src/services/doctor/checks/most-recent-scored-day.test.ts src/services/doctor/checks/data-quality-counts.test.ts

# Grep gates clean
bash scripts/ci-grep-gates.sh

# Type system clean
npx tsc --noEmit
```
</verification>

<success_criteria>
- 3 probe files + 3 test files (14 test cases total: 6 + 5 + 3).
- All probes accept an optional `deps.repos` dep-injection seam and a `deps.clock` (where applicable).
- `last_sync_recency` thresholds at 36h pass / 7d warn / >7d fail; downgrades pass→warn when the most-recent sync's status is 'failed'.
- `most_recent_scored_day` reads across cycles+recovery+sleep latestScoredDate(), picks the MAX (string-compare yyyy-mm-dd), applies the same thresholds.
- `data_quality_counts` is always pass when repos are injected; detail string format byte-matches 05-CONTEXT.md §Specifics line 273.
- No `console.*`, no `drizzle-orm` import, no bare `fetch(` in any new file.
- All 10 grep gates green.
</success_criteria>

<output>
Create `.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-04-SUMMARY.md` when done summarizing: 3 sync-recency / data-quality probes ship, 14 test cases covering the threshold ladder + the always-pass discipline of data_quality_counts. Plan 05-06 wires all 3 into runDoctor() with the bootstrap-injected Repos.
</output>
