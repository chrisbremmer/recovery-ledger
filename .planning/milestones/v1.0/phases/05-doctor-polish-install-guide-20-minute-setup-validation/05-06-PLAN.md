---
phase: 05-doctor-polish-install-guide-20-minute-setup-validation
plan: 06
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - src/services/doctor/index.ts
  - src/services/doctor/index.test.ts
  - src/services/bootstrap.ts
  - src/services/index.ts
  - src/cli/commands/doctor.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "runDoctor() runs all 14 checks: 5 existing (better_sqlite3_load, napi_keyring_load, mcp_stdout_purity, auth, token_freshness) + 9 new (whoop_roundtrip, db_open, db_integrity, db_schema_version, db_wal_size, last_sync_recency, most_recent_scored_day, data_quality_counts, concurrent_writers_stress)"
    - "PROBE_NAMES tuple extended to 14 entries in the dependency-aware order from RESEARCH §Finding 2 (load → db → auth → online → recency → quality → stress)"
    - "bootstrap() exposes `services.runDoctor` that pre-binds the production deps: sqlite handle from openDb(), repos from the existing factories, refreshOrchestrator from the default singleton, fetcher that wraps httpGet('/v2/user/profile/basic')"
    - "createServices() (lightweight, no DB) still works — runDoctor() called from the no-DB context surfaces the db_* + recency probes as fail with 'no DB handle injected' / 'no repos injected' detail strings (consistent with the per-probe contracts from 05-03 + 05-04)"
    - "RunDoctorOptions.offline + .stress + .sqlite + .skipSubprocessChecks all flow through to the right probes"
    - "whoop_roundtrip's production fetcher wraps httpGet through callWithAuth — Gate F satisfied (the wrapper lives in src/services/doctor/index.ts which is allowed to call services.refreshOrchestrator.callWithAuth, which goes through src/infrastructure/whoop/client.ts httpGet, which is the Gate-F-allowlisted chokepoint)"
    - "When a probe THROWS rather than returning a structured DoctorCheck, the MR-07 Promise.allSettled fallback synthesizes a {name, status:'fail', detail:'probe threw: ...'} entry using PROBE_NAMES[i] — all 14 names mapped correctly"
    - "Existing 5-check tests still pass (no regression in Phase 1+2 check behavior)"
  artifacts:
    - path: "src/services/doctor/index.ts"
      provides: "runDoctor() with 14 PROBE_NAMES + Promise.allSettled over 14 probe invocations + dep-injection wiring"
      contains: "PROBE_NAMES"
    - path: "src/services/bootstrap.ts"
      provides: "bootstrap().services.runDoctor pre-bound to production deps (sqlite, repos, refreshOrchestrator, fetcher)"
      contains: "runDoctor"
    - path: "src/services/doctor/index.test.ts"
      provides: "Updated PROBE_NAMES assertion (14 entries); 14-check end-to-end test"
      contains: "14"
  key_links:
    - from: "src/services/doctor/index.ts"
      to: "src/services/doctor/checks/whoop-roundtrip.ts"
      via: "probeWhoopRoundtrip imported + invoked with {refreshOrchestrator, fetcher} from runDoctorImpl"
      pattern: "probeWhoopRoundtrip"
    - from: "src/services/bootstrap.ts"
      to: "src/services/doctor/index.ts"
      via: "bootstrap returns Services interface with runDoctor pre-bound to {sqlite, repos, refreshOrchestrator, fetcher}"
      pattern: "runDoctor"
    - from: "src/services/doctor/index.ts"
      to: "src/infrastructure/whoop/resources/profile.ts"
      via: "Production fetcher constructed in runDoctorImpl wraps httpGet('/v2/user/profile/basic', {}, WhoopRawProfile) with performance.now timing"
      pattern: "profile/basic"
---

<objective>
Wire all 9 new probes from Wave 1 into `runDoctor()` + extend `bootstrap()` to pre-bind the production deps each probe needs. This is the orchestration plan that turns 9 isolated probe files into a working 14-check doctor surface.

Purpose: end-to-end correctness requires the orchestrator to know which probe consumes which dep. The lightweight `createServices()` path is preserved (no DB) — when called from a context without bootstrap (theoretically only test isolation OR an external `services.runDoctor()` caller that bypasses bootstrap), the db_* + recency probes return their "no handle injected" / "no repos injected" structured fail per Wave 1's dep-injection seam.

Output: extended `src/services/doctor/index.ts` (now 14 PROBE_NAMES + 14 probe invocations); extended `src/services/bootstrap.ts` (wires sqlite + repos + refreshOrchestrator + fetcher into runDoctor); updated test suite. After this plan, `recovery-ledger doctor` produces 14-check JSON output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md
@agent_docs/decisions/0001-mcp-stdout-purity.md
@agent_docs/decisions/0002-single-flight-oauth-refresh.md
@src/services/doctor/index.ts
@src/services/doctor/checks/check-names.ts
@src/services/doctor/checks/whoop-roundtrip.ts
@src/services/doctor/checks/db-open.ts
@src/services/doctor/checks/db-integrity.ts
@src/services/doctor/checks/db-schema-version.ts
@src/services/doctor/checks/db-wal-size.ts
@src/services/doctor/checks/last-sync-recency.ts
@src/services/doctor/checks/most-recent-scored-day.ts
@src/services/doctor/checks/data-quality-counts.ts
@src/services/doctor/checks/concurrent-writers-stress.ts
@src/services/bootstrap.ts
@src/services/index.ts
@src/services/refresh-orchestrator.ts
@src/infrastructure/whoop/client.ts
@src/infrastructure/whoop/resources/profile.ts
@src/infrastructure/config/paths.ts

<interfaces>
<!-- The current runDoctor + bootstrap shapes Plan 05-06 extends. -->

From src/services/doctor/index.ts (current, post-Plan 05-01):
  RunDoctorOptions { skipSubprocessChecks?, offline?, stress?, sqlite? }   (extended in 05-01)
  PROBE_NAMES tuple: 5 entries today (extended to 14 here)
  runDoctor(opts) calls Promise.allSettled over 5 probes (extended to 14 here)

Each Wave 1 probe's dep signature (recap):
  probeWhoopRoundtrip({refreshOrchestrator, fetcher}, {offline?})
  probeDbOpen({sqlite?})
  probeDbIntegrity({sqlite?})
  probeDbSchemaVersion({sqlite?, migrationsDir?})
  probeDbWalSize({dbFile?})
  probeLastSyncRecency({repos?}, {clock?})
  probeMostRecentScoredDay({repos?}, {clock?})
  probeDataQualityCounts({repos?})
  probeConcurrentWritersStress({skipSubprocess?, enabled?})

From src/services/bootstrap.ts:
  bootstrap(): Promise<Bootstrapped>
  Bootstrapped.services: Services    — pre-bound surface; Phase 5 adds runDoctor pre-binding
  Bootstrapped.close(): void

Production fetcher construction for whoop_roundtrip (research-validated):
  async (accessToken: string) => {
    const start = performance.now();
    const raw = await httpGet('/v2/user/profile/basic', {}, WhoopRawProfile);
    return { status: 200, durationMs: performance.now() - start };
  }
  — but httpGet THROWS on non-200, so the fetcher's catch arm needs to surface WhoopApiError.status if available. See task body for the full shape.

Recommended PROBE_NAMES order (RESEARCH §Finding 2 — visual ordering for first-fail-wins UX):
  1. BETTER_SQLITE3_LOAD
  2. NAPI_KEYRING_LOAD
  3. MCP_STDOUT_PURITY
  4. DB_OPEN
  5. DB_INTEGRITY
  6. DB_SCHEMA_VERSION
  7. DB_WAL_SIZE
  8. AUTH
  9. TOKEN_FRESHNESS
  10. WHOOP_ROUNDTRIP
  11. LAST_SYNC_RECENCY
  12. MOST_RECENT_SCORED_DAY
  13. DATA_QUALITY_COUNTS
  14. CONCURRENT_WRITERS_STRESS
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runDoctor() to invoke all 14 probes with dep wiring</name>
  <files>src/services/doctor/index.ts, src/services/doctor/index.test.ts</files>
  <behavior>
    - `runDoctor(opts)` invokes all 14 probes via `Promise.allSettled` in the documented order.
    - `RunDoctorOptions` accepts a new optional `repos?: Repos` field for the recency/data-quality probes. The Repos type is imported from `bootstrap.ts` (or a narrow interface declared locally — see action).
    - The whoop_roundtrip probe also receives a `refreshOrchestrator` + `fetcher` deps pair; these come from a new field `RunDoctorOptions.whoopFetcher?: (accessToken: string) => Promise<{status: number; durationMs: number}>`. When omitted, the probe receives the dep-injection-missing fallback (probe returns `{status: 'pass', detail: 'skipped (no fetcher)'}` — same semantics as `--offline`). The fetcher is constructed in `bootstrap()` (Task 2 below).
    - PROBE_NAMES tuple extended to 14 entries in the documented order.
    - The MR-07 Promise.allSettled fallback (synthesize fail from probe throws) maps `PROBE_NAMES[i]` correctly across all 14 slots.
    - Test extended to assert: `result.checks.length === 14`, all 14 checks have a `name` field equal to the expected CHECK_NAMES.* value, deriveOverall still works.
  </behavior>
  <read_first>
    - @src/services/doctor/index.ts (current 5-probe orchestrator — read line-by-line; the Promise.allSettled block, the PROBE_NAMES tuple, the deriveOverall function — extends additively)
    - @src/services/doctor/index.test.ts (current test suite — extend the 5-check assertion to 14)
    - All 9 Wave 1 probe files (read each one to confirm the dep signature is what's documented above)
    - @src/services/bootstrap.ts (the Repos type definition — line where it's declared; the runDoctor wiring will reference this type)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §2 (PROBE_NAMES recommended order)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-02 + D-03 (the 14-check inventory + --offline/--stress flag semantics)
  </read_first>
  <action>
    Edit `src/services/doctor/index.ts`:

      1. Add 9 new imports at the top of the probe-imports block (alongside the existing `probeAuth`, `probeMcpStdoutPurity`, etc.):
        import { probeWhoopRoundtrip } from './checks/whoop-roundtrip.js';
        import { probeDbOpen } from './checks/db-open.js';
        import { probeDbIntegrity } from './checks/db-integrity.js';
        import { probeDbSchemaVersion } from './checks/db-schema-version.js';
        import { probeDbWalSize } from './checks/db-wal-size.js';
        import { probeLastSyncRecency } from './checks/last-sync-recency.js';
        import { probeMostRecentScoredDay } from './checks/most-recent-scored-day.js';
        import { probeDataQualityCounts } from './checks/data-quality-counts.js';
        import { probeConcurrentWritersStress } from './checks/concurrent-writers-stress.js';
        import type { RefreshOrchestrator } from '../refresh-orchestrator.js';

      2. Extend `RunDoctorOptions` (added in Plan 05-01) with two more optional fields. The full interface becomes:
        export interface RunDoctorOptions {
          skipSubprocessChecks?: boolean;
          offline?: boolean;
          stress?: boolean;
          sqlite?: Database.Database;
          repos?: {
            syncRuns: { latestFinished(): {finished_at: string; status: 'ok'|'partial'|'failed'} | null };
            cycles: { latestScoredDate(): string|null; countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} };
            recovery: { latestScoredDate(): string|null; countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} };
            sleep: { latestScoredDate(): string|null; countByScoreState(): {scored:number; pending:number; unscorable:number; excluded:number} };
          };
          refreshOrchestrator?: RefreshOrchestrator;
          whoopFetcher?: (accessToken: string) => Promise<{status: number; durationMs: number}>;
        }
        The `repos` inline shape is the union of LastSyncRecencyDeps['repos'] + MostRecentScoredDayDeps['repos'] + DataQualityCountsDeps['repos']. Inline is fine for one place; the bootstrap layer's Repos type will satisfy this structurally.

      3. Extend the `PROBE_NAMES` tuple from 5 entries to 14, in the order documented in interfaces above (matching RESEARCH §Finding 2):
        const PROBE_NAMES = [
          CHECK_NAMES.BETTER_SQLITE3_LOAD,
          CHECK_NAMES.NAPI_KEYRING_LOAD,
          CHECK_NAMES.MCP_STDOUT_PURITY,
          CHECK_NAMES.DB_OPEN,
          CHECK_NAMES.DB_INTEGRITY,
          CHECK_NAMES.DB_SCHEMA_VERSION,
          CHECK_NAMES.DB_WAL_SIZE,
          CHECK_NAMES.AUTH,
          CHECK_NAMES.TOKEN_FRESHNESS,
          CHECK_NAMES.WHOOP_ROUNDTRIP,
          CHECK_NAMES.LAST_SYNC_RECENCY,
          CHECK_NAMES.MOST_RECENT_SCORED_DAY,
          CHECK_NAMES.DATA_QUALITY_COUNTS,
          CHECK_NAMES.CONCURRENT_WRITERS_STRESS,
        ] as const;

      4. Extend the `Promise.allSettled` invocation to call all 14 probes in the SAME order as PROBE_NAMES. The new entries' dep wiring:
        const settled = await Promise.allSettled([
          probeBetterSqlite3(),
          probeKeyring(),
          probeMcpStdoutPurity({ skipSubprocess }),
          probeDbOpen({ sqlite: opts.sqlite }),
          probeDbIntegrity({ sqlite: opts.sqlite }),
          probeDbSchemaVersion({ sqlite: opts.sqlite }),
          probeDbWalSize({}),                            // reads paths.dbFile by default
          probeAuth(),
          probeTokenFreshness(),
          probeWhoopRoundtrip(
            // The probe requires deps + opts. When refreshOrchestrator/fetcher are missing,
            // fall through to a synthesized "pass — skipped" via the offline arm:
            opts.refreshOrchestrator && opts.whoopFetcher
              ? { refreshOrchestrator: opts.refreshOrchestrator, fetcher: opts.whoopFetcher }
              : { refreshOrchestrator: { callWithAuth: async () => { throw new Error('no orchestrator'); } } as unknown as RefreshOrchestrator, fetcher: async () => ({ status: 0, durationMs: 0 }) },
            { offline: opts.offline === true || !opts.refreshOrchestrator || !opts.whoopFetcher },
          ),
          probeLastSyncRecency({ repos: opts.repos }),
          probeMostRecentScoredDay({ repos: opts.repos }),
          probeDataQualityCounts({ repos: opts.repos }),
          probeConcurrentWritersStress({ skipSubprocess, enabled: opts.stress === true }),
        ]);

        Note the whoop_roundtrip dep-or-offline degenerate-pass: when called without deps, the probe receives `{offline: true}` so it short-circuits with 'skipped (--offline)' detail; the user invoking `recovery-ledger doctor` from a context without a refresh orchestrator should see the same skipped behavior as `--offline`. The dummy deps satisfy the type contract; the offline arm guarantees they're never actually called.

      5. The settled.map() block that synthesizes fail-from-throw is UNCHANGED in structure but its PROBE_NAMES[i] lookup now maps to 14 positions correctly.

    File-header doc-comment: amend the existing comment block (the one that explains MR-07 + MR-14 + the PROBE_NAMES discipline) to add a final paragraph: "Plan 05-06: extended from 5 to 14 probes. Phase 5 D-02 + Finding 2 dependency ordering — load → db → auth → online → recency → quality → stress. The whoop_roundtrip probe degrades to 'skipped' when refreshOrchestrator/whoopFetcher deps are absent (createServices() lightweight path); production bootstrap() supplies both." Per agent_docs/learnings.md, do NOT inline grep-gate substrings (no `console.log` literal, etc.).

    Edit `src/services/doctor/index.test.ts`:
      - Find the existing test that asserts `result.checks.length === 5` (or similar) and update to `=== 14`.
      - Add a new test `runDoctor returns 14 checks in the documented order` that asserts `result.checks.map(c => c.name)` equals exactly the PROBE_NAMES tuple values.
      - Add a new test `runDoctor degrades whoop_roundtrip to skipped when no orchestrator deps are passed` that calls `runDoctor({})` and asserts `result.checks.find(c => c.name === 'whoop_roundtrip')?.detail` equals `'skipped (--offline)'`.
      - Add a new test `runDoctor degrades concurrent_writers_stress to skipped by default` that calls `runDoctor({})` and asserts the stress probe detail equals `'skipped — run with --stress to enable'`.

    Do NOT modify the existing 5-check probe test assertions; just extend the count. The Phase 1+2 tests stay green because the new probes are additive and the existing 5 still appear at their original positions in the PROBE_NAMES tuple's first 3 + 8 + 9 slots (BETTER_SQLITE3_LOAD, NAPI_KEYRING_LOAD, MCP_STDOUT_PURITY, AUTH, TOKEN_FRESHNESS).

    No `console.*`. Strict TS. The wide RunDoctorOptions union may need a small re-export adjustment in `src/services/index.ts` — verify the existing `export type { ... RunDoctorOptions }` line still works (it should — the type's wider structure flows transparently through the re-export).
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/index.test.ts && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    runDoctor invokes all 14 probes. Test asserts result.checks.length === 14 + name-tuple matches PROBE_NAMES. Degenerate-pass behavior for whoop_roundtrip + concurrent_writers_stress verified via test. All 10 grep gates green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend bootstrap() to pre-bind production deps into services.runDoctor</name>
  <files>src/services/bootstrap.ts, src/services/index.ts</files>
  <behavior>
    - `bootstrap().services.runDoctor` is now a pre-bound zero-arg method (i.e., the existing `(opts?: RunDoctorOptions) => Promise<DoctorResult>` shape STAYS — but the bootstrap layer's wrapper supplies `{sqlite, repos, refreshOrchestrator, whoopFetcher}` defaults so callers don't need to pass them).
    - When the bootstrap-supplied wrapper is invoked with `{offline: true, stress: false}`, those flags propagate through; `{sqlite, repos, refreshOrchestrator, whoopFetcher}` defaults are still supplied so the db_* + recency probes work and only whoop_roundtrip is skipped.
    - The production whoopFetcher is constructed in `bootstrap()` and wraps `httpGet('/v2/user/profile/basic', {}, WhoopRawProfile)` with `performance.now()` start/end. Errors from httpGet (which throws on non-200) are caught and converted to `{status: <WhoopApiError.status ?? 0>, durationMs: <elapsed>}` so the probe sees the status code in its branch logic rather than a thrown error.
    - The lightweight `createServices()` path is unchanged — it still returns a no-DB `ServicesBase` whose `runDoctor` does NOT pre-bind sqlite/repos/whoopFetcher (so the affected probes return their "no handle injected" structured fail when invoked through that path). CLI doctor command continues to flow through createServices (its existing line `const services = createServices();`) — but the MCP `whoop_doctor` tool already goes through bootstrap() per Phase 4 Plan 04-10. Verify by reading both call sites.

    BUT: this creates an asymmetry — the CLI invocation of `doctor` would not exercise the db_* probes because `createServices()` lacks bootstrap. The executor must EITHER:
      (a) Switch `src/cli/commands/doctor.ts` to call `bootstrap()` instead of `createServices()` so the CLI sees the full surface (RECOMMENDED — matches RESEARCH §Open Questions §1 recommendation), OR
      (b) Leave the CLI on createServices() and accept that the new db_* + recency probes only run via MCP (rejected — defeats DOC-01 which requires `recovery-ledger doctor` to run all checks).

    Choose (a). This requires src/cli/commands/doctor.ts edit (NOT in this plan's files_modified — add it). Update files_modified to include it.
  </behavior>
  <read_first>
    - @src/services/bootstrap.ts (the full file — read the existing composition root; the `Bootstrapped.services` interface; the existing runSync wiring; the close() lifecycle)
    - @src/services/index.ts (the createServices / bootstrap split; the Services / ServicesBase type relationship)
    - @src/services/refresh-orchestrator.ts (the default singleton + the bound callWithAuth re-export)
    - @src/infrastructure/whoop/client.ts (httpGet signature + throw-on-non-200 behavior — feeds the production whoopFetcher)
    - @src/infrastructure/whoop/resources/profile.ts (the WhoopRawProfile Zod schema used by getProfile)
    - @src/infrastructure/whoop/errors.ts (WhoopApiError shape — `.status` field on caught errors)
    - @src/cli/commands/doctor.ts (the createServices() invocation — line 31-ish; this is the call site to switch to bootstrap())
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Open Questions §1 (the dep-injection seam recommendation)
  </read_first>
  <action>
    Edit `src/services/bootstrap.ts`:
      1. Locate the `Bootstrapped.services` object construction (the function that returns `{services: {runSync, runDoctor, getDailyReview, ...}, close}` or similar). Identify the existing `runDoctor` re-export — Phase 1's `services.runDoctor` may currently be a bare re-export of `import { runDoctor } from './doctor/index.js';`.

      2. Replace the bare re-export with a pre-bound wrapper. Imports needed at the top of bootstrap.ts (add if missing):
        - `import { httpGet } from '../infrastructure/whoop/client.js';`
        - `import { performance } from 'node:perf_hooks';`
        - `import { refreshOrchestrator } from './refresh-orchestrator.js';`
        - `import { WhoopRawProfile } from '../domain/schemas/whoop-api.js';`
        - `import { WhoopApiError } from '../infrastructure/whoop/errors.js';`
        - `import { runDoctor as runDoctorImpl, type RunDoctorOptions, type DoctorResult } from './doctor/index.js';`

      3. Inside the bootstrap composition, after the sqlite handle is opened and the repositories are constructed, build the production fetcher:
        const productionWhoopFetcher = async (_accessToken: string): Promise<{status: number; durationMs: number}> => {
          const start = performance.now();
          try {
            await httpGet('/v2/user/profile/basic', {}, WhoopRawProfile);
            return { status: 200, durationMs: performance.now() - start };
          } catch (err) {
            const status = (err instanceof WhoopApiError && typeof err.status === 'number') ? err.status : 0;
            return { status, durationMs: performance.now() - start };
          }
        };

      4. Build the production runDoctor wrapper:
        const services_runDoctor = (opts: RunDoctorOptions = {}): Promise<DoctorResult> => runDoctorImpl({
          ...opts,
          sqlite: opts.sqlite ?? sqlite,            // user override wins; default to the bootstrapped handle
          repos: opts.repos ?? { syncRuns, cycles, recovery, sleep },   // narrow Repos subset matching RunDoctorOptions.repos
          refreshOrchestrator: opts.refreshOrchestrator ?? refreshOrchestrator,
          whoopFetcher: opts.whoopFetcher ?? productionWhoopFetcher,
        });

        The `syncRuns, cycles, recovery, sleep` names come from the existing repo factory invocations in the bootstrap body — read the file to confirm the local variable names; adapt if different. The `Repos` narrow union built here must structurally match `RunDoctorOptions.repos` declared in src/services/doctor/index.ts (Task 1).

      5. Replace the existing `runDoctor` field in the returned services object with `services_runDoctor`. Field name in the returned object stays `runDoctor` (no API surface change to consumers of `bootstrap().services.runDoctor`).

    Edit `src/services/index.ts`:
      The createServices() function continues to return ServicesBase with the bare `runDoctor` from `./doctor/index.js`. No edits needed if the existing line is `import { runDoctor } from './doctor/index.js';` + re-export — this remains the no-DB / no-repos / no-fetcher path. Document the choice in a brief comment update: "Plan 05-06: createServices() runDoctor is the lightweight no-DB no-repos no-fetcher path; the db_* + recency + whoop_roundtrip probes report 'no <X> injected' / 'skipped (--offline)' in this path. The CLI command now flows through bootstrap() (src/cli/commands/doctor.ts) so the full 14-check surface runs end-to-end."

    Edit `src/cli/commands/doctor.ts` (add to files_modified):
      Replace the `const services = createServices();` line with:
        const { services, close } = await bootstrap();
        // Ensure the bootstrap handle is released after exit; the existing process.exit callback runs before the close, so call close synchronously before the write.
      And inside the existing `process.stdout.write(`${body}\n`, () => { ... })` callback (which calls `process.exit(...)`), insert `close();` before `process.exit(...)`. Same shape for the catch branch.

      Imports adjustment at the top: replace `import { createServices, type DoctorResult } from '../../services/index.js';` with `import { bootstrap, type DoctorResult } from '../../services/index.js';`.

      Verify the existing `--text`, `--offline`, `--stress` flag wiring from Plan 05-01 Task 3 still threads through correctly — the call becomes `services.runDoctor({ offline: opts.offline === true, stress: opts.stress === true })` (no change vs Plan 05-01's wiring; bootstrap() supplies the dep defaults).

      Gate C still requires the process.stdout.write to be the only allowed site here; no changes to that.

    Add tests via integration: `src/services/doctor/index.test.ts` already covers the no-deps path. The full-deps path is implicitly exercised by `tests/integration/mcp-runtime.test.ts` whose whoop_doctor invocation through bootstrap() should now return 14 checks (most as fail or pass; the test does not need to assert on each — it asserts `tools.length === 8` which is unaffected). The executor confirms tests/integration/mcp-runtime.test.ts still passes after this edit (i.e., the whoop_doctor invocation doesn't crash due to the new 14-check expansion).
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/services/doctor tests/integration/mcp-runtime.test.ts && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    bootstrap().services.runDoctor pre-binds sqlite + repos + refreshOrchestrator + whoopFetcher. CLI doctor command switched to bootstrap() so all 14 checks run end-to-end. MCP whoop_doctor inherits the wiring through its existing bootstrap() path (no changes to src/mcp/tools/whoop-doctor.ts). All 10 grep gates green. Build clean. The Phase 4 D-29 tools=8 attestation still passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke test the 14-check surface end-to-end via the CLI</name>
  <files>(no files modified — verification-only)</files>
  <read_first>
    - The four files modified by Tasks 1+2 (re-read to confirm completeness)
    - @src/formatters/doctor.txt.ts (the text renderer — verify it still handles 14 rows; no changes expected per RESEARCH §Finding 1)
  </read_first>
  <action>
    Run, in order:
      1. `npm run build` — should compile clean.
      2. `node dist/cli.mjs doctor --text 2>&1 | head -40` — should print 14 `[<status>] <name> — <detail>` lines followed by `overall: <status>`. Confirm the 14 names appear in the documented PROBE_NAMES order. Most checks will be `fail` on a developer machine that hasn't run `init` / `auth` / `sync`; the smoke test just verifies the surface runs end-to-end and the renderer produces 14 rows. (The 14 names + the overall line = 15 lines minimum.)
      3. `node dist/cli.mjs doctor --offline --text 2>&1 | grep -E 'whoop_roundtrip|concurrent_writers_stress'` — should show whoop_roundtrip as 'pass' with 'skipped (--offline)' AND concurrent_writers_stress as 'pass' with 'skipped — run with --stress to enable'.
      4. `node dist/cli.mjs doctor --stress --offline --text 2>&1 | grep concurrent_writers_stress` — should show concurrent_writers_stress as 'pass' (with the 4-worker timing detail). 800ms+ runtime acknowledged.
      5. `node dist/cli.mjs doctor 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print('checks:', len(d['checks']), 'overall:', d['overall'])"` — assert `checks: 14`.

    Record the actual run-times + exit codes in the Plan 05-06 SUMMARY. If any step fails: STOP, investigate, fix. Do not advance to Plan 05-07 until the surface is end-to-end green.
  </action>
  <verify>
    <automated>npm run build && node dist/cli.mjs doctor --offline 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['checks'])==14, f'expected 14, got {len(d[\"checks\"])}'; assert all('name' in c and 'status' in c and 'detail' in c for c in d['checks']), 'malformed check'; print('OK')"</automated>
  </verify>
  <done>
    `recovery-ledger doctor --offline` prints 14 checks via JSON; all have {name, status, detail}; overall is one of pass/warn/fail. CLI exit code is one of 0/1/2 (DOCTOR_EXIT_CODES). End-to-end smoke verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap() → runDoctor | The composition root passes the production sqlite handle + repos + refreshOrchestrator + whoopFetcher. Every probe consumes through the dep-injection seam, never bypassing the boundary. |
| CLI doctor command → bootstrap | Plan 05-06 switches the CLI from createServices() to bootstrap() so the full 14-check surface runs. The bootstrap close() lifecycle is invoked before process.exit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-S4 | Spoofing | A probe-level dep-injection could be subverted via an opts override | accept | Single-user local tool; the override path exists only for tests + manual debugging. The bootstrap-provided defaults are the production wiring. |
| T-05-D4 | DoS | bootstrap() opens DB + runs migrator on every CLI doctor invocation (was previously a lightweight createServices) | mitigate | bootstrap is idempotent (the migrator is a no-op when up to date); the close() lifecycle releases the handle. CLI startup cost adds ~50-100ms vs the lightweight path — acceptable given DOC-01 requires the full surface. |
| T-05-I6 | Information Disclosure | whoopFetcher catches WhoopApiError + reads `.status` | mitigate | The fetcher returns ONLY `{status, durationMs}` to the probe — no Bearer/JWT material flows through. The probe's catch-arm 'roundtrip failed:' detail goes through the sanitizer pipeline before being surfaced via MCP. |
| T-05-SC | Tampering | No new npm installs in this plan | accept | Phase 5 ships zero new dependencies per RESEARCH §Standard Stack confirmation. |
</threat_model>

<verification>
After all 3 tasks complete:

```sh
# Doctor index test green
npx vitest run src/services/doctor/index.test.ts

# Full suite green
npm test

# Grep gates clean
bash scripts/ci-grep-gates.sh

# 14-check smoke
npm run build && node dist/cli.mjs doctor --offline 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['checks'])==14"

# MCP attestation unchanged
npx vitest run tests/integration/mcp-runtime.test.ts
```
</verification>

<success_criteria>
- `runDoctor()` invokes all 14 probes via Promise.allSettled in the documented dependency-aware order.
- `RunDoctorOptions` exposes all 7 optional fields: skipSubprocessChecks, offline, stress, sqlite, repos, refreshOrchestrator, whoopFetcher.
- `bootstrap()` pre-binds sqlite + repos + refreshOrchestrator + whoopFetcher into `services.runDoctor`.
- `recovery-ledger doctor` (CLI) flows through bootstrap() and produces 14 checks + overall in JSON output.
- `--offline` flag skips whoop_roundtrip (pass with skipped detail).
- `--stress` flag enables concurrent_writers_stress (pass with timing detail when 4 workers complete).
- `whoop_doctor` MCP tool unchanged in this plan — inherits new behavior via existing bootstrap() flow.
- D-29 attestation (tools=8/resources=6/prompts=4) still passes.
- All 10 grep gates green.
- No `console.*`, no bare `fetch(` outside the existing Gate F allowlist.
</success_criteria>

<output>
Create `.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-06-SUMMARY.md` when done summarizing: runDoctor 5→14 probes, bootstrap pre-binds production deps, CLI switched from createServices to bootstrap, smoke test confirms 14 checks render via both --text and JSON.
</output>
