---
phase: 04-domain-math-reviews-decision-ledger-mcp-surface
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/domain/baselines/types.ts
  - src/domain/anomalies/types.ts
  - src/domain/patterns/types.ts
  - src/domain/confidence/types.ts
  - src/domain/review/types.ts
  - src/services/decision/types.ts
  - src/services/cache/types.ts
  - src/services/api-gap/types.ts
  - src/domain/baselines/types.test.ts
  - src/domain/anomalies/types.test.ts
  - src/domain/patterns/types.test.ts
  - src/domain/confidence/types.test.ts
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-06
  - REV-07
  - DEC-01
  - DEC-02
  - DEC-03

must_haves:
  truths:
    - "All Phase 4 cross-cutting type contracts exist (BaselineStats, ZAnalysis, Anomaly, WeeklyPattern, ConfidenceGate, DailyReviewResult, WeeklyReviewResult, TodayMetrics, DataStatus, WeekSummary, DecisionPrompt, AddDecisionInput, ReviewDecisionsInput, ReviewDecisionsResult, UpdateDecisionInput, QueryCacheInput, QueryCacheResult, ApiGapEntry, ApiGapResult) so Wave 1 + Wave 2 plans implement against compile-checked contracts"
    - "ZAnalysis is a discriminated union with three variants: computed | refused(insufficient_days) | refused(baseline_mad_zero) (D-05 + RESEARCH §1 MAD=0 extension)"
    - "WeeklyPattern is a discriminated union: detected (with optional pattern_confidence: 'weak'|'strong' per D-34) | no_pattern (insufficient_window_days | no_factor_cleared_fdr | all_candidates_refused)"
    - "DecisionPrompt is a discriminated union: silent | none_this_week(suggested_text) per D-22"
    - "QueryCacheInput is a discriminated union per resource with 8 arms (cycles, recoveries, sleeps, workouts, profile, body_measurements, sync_runs, decisions) per D-24"
    - "METRIC_NAMES closed-tuple defines exactly 9 metrics per D-04 + RESEARCH §Statistical Engine §2 direction map (hrv_rmssd_milli, recovery_score, sleep_duration_minutes, sleep_efficiency_percent, resting_heart_rate, respiratory_rate, day_strain, sleep_duration_minutes wait — see action for exact list)"
    - "CANDIDATE_FACTORS closed-tuple defines exactly 5 candidates per D-11 (no module-load constant change here — that lives in candidates.ts in Wave 1; this plan defines the CandidateName type)"
  artifacts:
    - path: "src/domain/baselines/types.ts"
      provides: "BaselineStats, METRIC_NAMES tuple, MetricName"
      contains: "METRIC_NAMES"
    - path: "src/domain/anomalies/types.ts"
      provides: "ZAnalysis discriminated union, Anomaly"
      contains: "ZAnalysis"
    - path: "src/domain/patterns/types.ts"
      provides: "WeeklyPattern discriminated union, CandidateResult, WorstDay, CandidateName"
      contains: "WeeklyPattern"
    - path: "src/domain/confidence/types.ts"
      provides: "ConfidenceTier, ConfidenceGate"
      contains: "ConfidenceTier"
    - path: "src/domain/review/types.ts"
      provides: "DailyReviewResult (D-03), WeeklyReviewResult (D-16), TodayMetrics, DataStatus, WeekSummary, DecisionPrompt, SuggestedAction"
      contains: "DailyReviewResult"
    - path: "src/services/decision/types.ts"
      provides: "AddDecisionInput, ReviewDecisionsInput, ReviewDecisionsResult, UpdateDecisionInput"
      contains: "AddDecisionInput"
    - path: "src/services/cache/types.ts"
      provides: "QueryCacheInput (D-24 8-arm discriminated union), QueryCacheResult"
      contains: "QueryCacheInput"
    - path: "src/services/api-gap/types.ts"
      provides: "ApiGapEntry, ApiGapResult"
      contains: "ApiGapEntry"
  key_links:
    - from: "src/domain/review/types.ts"
      to: "src/domain/anomalies/types.ts, src/domain/patterns/types.ts, src/domain/confidence/types.ts"
      via: "DailyReviewResult composes Anomaly[], confidence: ConfidenceGate; WeeklyReviewResult composes WeeklyPattern, ConfidenceGate, DecisionPrompt"
      pattern: "import.*Anomaly|import.*WeeklyPattern|import.*ConfidenceGate"
    - from: "src/services/cache/types.ts"
      to: "src/domain/types/sync.ts, src/domain/types/entities.js (Phase 3)"
      via: "Reuses ResourceName tuple + entity types from Phase 3 — Phase 4 adds the typed-discriminated-union QueryCacheInput shape on top"
      pattern: "ResourceName|Cycle|Recovery|Sleep|Workout|Decision"
---

<objective>
Define every cross-cutting type contract Phase 4 plans will implement against. No business logic ships in this plan — only `*types*.ts` files plus a few `*.types.test.ts` files that compile-check the discriminated-union narrowing (per ADR-0004's forcing-function discipline + Phase 3 D-03 ScoreState union precedent).

Purpose: contracts ship in Wave 0 so Wave 1 (pure domain) implements against compile-checked shapes; Wave 2 (services) consumes the result types; Wave 3 (formatters) and Wave 4 (MCP/CLI) consume the same types verbatim. Without this scaffolding, downstream plans either (a) re-invent types and clash at merge, or (b) read each other's outputs and create implicit ordering coupling. Phase 3 D-03 (Score union) is the precedent — types ship before logic, narrowing is enforced at the type-system level.

Output: 8 type files + 4 type-narrowing test files. Every Phase 4 cross-cutting type contract is now compile-checked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md
@agent_docs/decisions/0003-score-state-discipline.md
@agent_docs/decisions/0004-no-reliable-pattern-positive-output.md
@src/domain/types/score.ts
@src/domain/types/sync.ts
@src/domain/types/entities.ts

<interfaces>
<!-- Phase 3's typed-result + discriminated-union precedents Phase 4 must mirror. -->

From src/domain/types/sync.ts (closed-tuple + derived type + runtime Set pattern):
- `RESOURCES = [...] as const`
- `ResourceName = (typeof RESOURCES)[number]`
- `ResourceSyncStatus = 'success' | 'partial_429' | 'partial_5xx' | ...`
- `ResourceSyncOutcome = { status: ResourceSyncStatus; ...per-status fields }`
- `RunSyncResult = { status: RunSyncStatus; perResource: Record<ResourceName, ResourceSyncOutcome>; syncRunId: string; gapsDetected: number }`

From src/domain/types/score.ts (ADR-0003 discriminated union):
- `Score = { score_state: 'SCORED', ...numeric fields } | { score_state: 'PENDING_SCORE' } | { score_state: 'UNSCORABLE' }`
- `SCORE_STATES` tuple + `SCORE_STATES_SET` runtime Set

From src/domain/types/entities.ts:
- `Decision = { id, createdAt, category, decision, rationale, confidence, expectedEffect, followUpDate, status, outcomeNotes }`
- `Cycle`, `Recovery`, `Sleep`, `Workout`, `Profile`, `BodyMeasurement`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain stats/baseline/confidence type files</name>
  <files>src/domain/baselines/types.ts, src/domain/baselines/types.test.ts, src/domain/confidence/types.ts, src/domain/confidence/types.test.ts</files>
  <behavior>
    - Test: `METRIC_NAMES` is a 9-tuple (the D-04 raw measurement list).
    - Test: `BaselineStats` has fields `metric: MetricName`, `median: number`, `mad: number` (raw, not scaled), `n: number` (count of SCORED days that contributed), `coverage_pct: number` (n / 30 × 100).
    - Test: `ConfidenceTier` literal union has exactly `'insufficient' | 'weak' | 'strong'` (per D-13).
    - Test: `ConfidenceGate` has fields `tier`, `coveragePct`, `minRequired: 10 | 20`, `sampleSize: number` (= scoredDays).
  </behavior>
  <read_first>
    - @src/domain/types/score.ts (closed-tuple + ReadonlySet precedent — Shared Pattern 2)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-04 (the 9 metric names) + D-13 (tier thresholds)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Statistical Engine §3 Confidence-Tier Gating
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §Shared Pattern 2 (closed-tuple discriminator)
    - @agent_docs/conventions.md (no default exports; comments only when why isn't obvious)
  </read_first>
  <action>
    Create `src/domain/baselines/types.ts` exporting (per D-04 + Shared Pattern 2):
    - `METRIC_NAMES = ['recovery_score', 'hrv_rmssd_milli', 'resting_heart_rate', 'spo2_percentage', 'skin_temp_celsius', 'day_strain', 'sleep_duration_minutes', 'sleep_efficiency_percent', 'respiratory_rate'] as const` (9 entries — verbatim from D-04 `TodayMetrics`).
    - `type MetricName = (typeof METRIC_NAMES)[number]`.
    - `METRIC_NAMES_SET: ReadonlySet<MetricName>` built from the tuple.
    - `interface BaselineStats { metric: MetricName; median: number; mad: number; n: number; coverage_pct: number }` — `mad` field is RAW (not yet scaled by 1.4826 — scaling happens in `domain/stats/mad.ts` `robustSigma()`; the field name documents "raw MAD" via the JSDoc).
    Create `src/domain/confidence/types.ts` exporting (per D-13 + RESEARCH §3):
    - `type ConfidenceTier = 'insufficient' | 'weak' | 'strong'`.
    - `interface ConfidenceGate { tier: ConfidenceTier; coveragePct: number; minRequired: 10 | 20; sampleSize: number }` — `minRequired` is `10` for insufficient/weak, `20` for strong (the literal-tuple-as-doc pattern; documents the thresholds without a comment).
    Create sibling test files asserting tuple-length, set-size, and basic shape (with `expectTypeOf` if vitest's type assertions help, or just runtime narrowing tests). No `console.*`, no I/O. Files live under `src/domain/` so they are pure (Shared Pattern 1).
  </action>
  <verify>
    <automated>npx vitest run src/domain/baselines/types.test.ts src/domain/confidence/types.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Both source files exist with the exports above. Both test files pass. `tsc --noEmit` returns 0 (the new files compile under strict TS). No `console.*`. The 9-metric tuple matches D-04 verbatim.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain anomalies + patterns type files with discriminated unions</name>
  <files>src/domain/anomalies/types.ts, src/domain/anomalies/types.test.ts, src/domain/patterns/types.ts, src/domain/patterns/types.test.ts</files>
  <behavior>
    - Test: `ZAnalysis` discriminated union has three variants — `computed` (with `value, baseline_median, baseline_mad, tier: 'weak'|'strong'`), `refused` with `reason: 'insufficient_days'` (carrying `days_available: number`, `days_required: 14`), and `refused` with `reason: 'baseline_mad_zero'` (carrying `days_available: number`). The narrowing test reads each variant via exhaustive switch — a fourth `kind` triggers a type error at the exhaustive site.
    - Test: `Anomaly` interface has `metric: MetricName`, `z: number`, `direction: 'low' | 'high'` (per D-06; `bidirectional` is never an Anomaly, only informational).
    - Test: `WeeklyPattern` discriminated union has `detected` arm and `no_pattern` arm. `detected` carries `factor: CandidateName`, `statistic: { U: number; p_raw: number; p_adjusted: number }`, `direction: 'worst_days_had_lower' | 'worst_days_had_higher'`, and the D-34 ADDITIVE field `pattern_confidence: 'weak' | 'strong'`. `no_pattern` carries `reason: 'insufficient_window_days' | 'no_factor_cleared_fdr' | 'all_candidates_refused'`.
    - Test: `CANDIDATE_FACTORS_TYPE_TUPLE` is referenced only as a type guide — the actual `as const` tuple ships in Wave 1's `candidates.ts`. THIS file just exports `type CandidateName` either by importing from candidates.ts (forward-declares a circular dep — avoid) OR by inlining the 5 candidate literal strings as a union type. Use the inline-union approach to keep this file fully self-contained (Wave 1's candidates.ts re-exports the type or asserts equivalence at compile time).
    - Test: `CandidateResult` interface has `factor: CandidateName`, `p_raw: number`, `p_adjusted: number`, `cleared: boolean`, `refused: boolean` (when sample-size-refused before MW ran).
    - Test: `WorstDay` interface has `date: string`, `recovery_score: number`.
  </behavior>
  <read_first>
    - @src/domain/types/score.ts (the canonical ADR-0003 discriminated-union narrowing precedent + the discriminator-field-via-switch exhaustive pattern)
    - @agent_docs/decisions/0004-no-reliable-pattern-positive-output.md (THE Phase 4 ADR governing every "absent" path)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-05 (ZAnalysis variants) + D-06 (Anomaly firing rule + direction map) + D-11 (5 candidates + dropped 2 with rationale) + D-16 (WeeklyPattern schema) + D-34 (pattern_confidence ADDITIVE field on the detected arm; not a top-level slot)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Statistical Engine §1 MAD=0 edge case (the third refused variant)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/domain/patterns/pattern.ts + §src/domain/anomalies/types.ts file map entries
  </read_first>
  <action>
    Create `src/domain/anomalies/types.ts`:
    - `import type { MetricName } from '../baselines/types.js';`
    - `export type ZAnalysis = | { kind: 'computed'; value: number; baseline_median: number; baseline_mad: number; tier: 'weak' | 'strong' } | { kind: 'refused'; reason: 'insufficient_days'; days_available: number; days_required: 14 } | { kind: 'refused'; reason: 'baseline_mad_zero'; days_available: number };` (D-05 + RESEARCH §1 MAD=0 extension — the third variant has no `days_required` field because the issue isn't sample size).
    - `export interface Anomaly { metric: MetricName; z: number; direction: 'low' | 'high'; baseline_median: number; baseline_mad_scaled: number; tier: 'weak' | 'strong'; }` (carries enough context for the formatter to render "HRV 35 (median 50, robust σ 8, z = -1.9, tier: strong) — low")
    Create `src/domain/patterns/types.ts`:
    - `export const CANDIDATE_FACTORS_TYPE_ONLY = ['sleep_duration_prior_night','sleep_debt_3d_rolling','day_strain_prior_day','workout_timing_late_evening','hrv_delta_prior_day'] as const;` — name-suffixed `_TYPE_ONLY` to flag that this is the type-anchor; Wave 1's `candidates.ts` will host the load-bearing module-load constant + the comment block on dropped candidates per D-11. Both must stay textually in sync — the candidates.ts test in Plan 04-05 will assert deep-equal.
    - `export type CandidateName = (typeof CANDIDATE_FACTORS_TYPE_ONLY)[number];`
    - `export type WeeklyPattern = | { kind: 'detected'; factor: CandidateName; statistic: { U: number; p_raw: number; p_adjusted: number }; direction: 'worst_days_had_lower' | 'worst_days_had_higher'; pattern_confidence: 'weak' | 'strong'; } | { kind: 'no_pattern'; reason: 'insufficient_window_days' | 'no_factor_cleared_fdr' | 'all_candidates_refused'; };` (D-16 + D-34 — the `pattern_confidence` field is on the `detected` arm only, not on `no_pattern`).
    - `export interface CandidateResult { factor: CandidateName; p_raw: number; p_adjusted: number; cleared: boolean; refused: boolean; refusal_reason?: 'sample_too_small'; }` (carries the per-candidate context per ADR-0004 §If FDR set empty — unranked context, not recommendation).
    - `export interface WorstDay { date: string; recovery_score: number }`.
    Add the two sibling test files. Each MUST include an exhaustive `switch (x.kind)` over the union variants that returns `string` from each branch — adding a fourth variant (e.g., `kind: 'tampered'`) would fail to compile, anchoring the ADR-0004 forcing function at the type level. Test files run green; the negative-case (fourth variant) is verified by static reasoning only (Vitest doesn't compile-fail by default; we rely on `tsc --noEmit` from the verify step).
  </action>
  <verify>
    <automated>npx vitest run src/domain/anomalies/types.test.ts src/domain/patterns/types.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Both source files exist with all unions + interfaces above. Both test files pass. `tsc --noEmit` returns 0. `ZAnalysis` has exactly 3 variants. `WeeklyPattern` has exactly 2 top-level variants. `pattern_confidence` lives on the `detected` arm only (D-34). `CANDIDATE_FACTORS_TYPE_ONLY` has exactly 5 entries (D-11).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Domain review types — DailyReviewResult, WeeklyReviewResult, supporting shapes</name>
  <files>src/domain/review/types.ts</files>
  <behavior>
    - `DailyReviewResult` shape per D-03 — all 7 slots (`data_status`, `today_state`, `anomalies`, `patterns`, `actions`, `confidence`, `insufficient_reason`). `patterns` typed as `Pattern[]` (empty in v1 per D-07; reserved slot).
    - `WeeklyReviewResult` shape per D-16 — all 6 slots (`data_status`, `week_summary`, `pattern`, `candidate_results`, `decision_prompt`, `confidence`).
    - `DecisionPrompt` discriminated union: `silent` | `none_this_week(suggested_text)` per D-22.
    - `TodayMetrics` carries the 9 D-04 fields as `number | null`.
    - `DataStatus`, `WeekSummary`, `SuggestedAction`, `Pattern` (placeholder, never populated in v1) interfaces.
  </behavior>
  <read_first>
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-03 (DailyReviewResult schema), D-04 (TodayMetrics), D-07 (empty patterns slot), D-08 (SuggestedAction shape), D-10 (insufficient_reason), D-16 (WeeklyReviewResult schema), D-17 (trailing-7 vs trailing-28 windows), D-22 (decision_prompt slot)
    - @src/domain/types/sync.ts (typed-result precedent — `RunSyncResult` is the Phase 3 analog)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/domain/review/types.ts (file map entry — points to the analog)
  </read_first>
  <action>
    Create `src/domain/review/types.ts` with:
    - `import type { Anomaly } from '../anomalies/types.js'; import type { WeeklyPattern, CandidateResult, WorstDay } from '../patterns/types.js'; import type { ConfidenceGate } from '../confidence/types.js'; import type { MetricName } from '../baselines/types.js'; import type { ResourceName } from '../types/sync.js';`
    - `interface TodayMetrics { recovery_score: number | null; hrv_rmssd_milli: number | null; resting_heart_rate: number | null; spo2_percentage: number | null; skin_temp_celsius: number | null; day_strain: number | null; sleep_duration_minutes: number | null; sleep_efficiency_percent: number | null; respiratory_rate: number | null; }` (per D-04; each field is `number | null` because WHOOP may not surface every metric on every cycle).
    - `interface DataStatus { reviewed_date: string; latest_sync_at: string | null; latest_sync_status: 'ok' | 'partial' | 'failed' | null; staleness_days: number; baseline_window: { start: string; end: string; scored_day_count: number; coverage_pct: number }; missing_resources: ResourceName[]; }` (per D-03 — note `latest_sync_status` values match Phase 3's `RunSyncStatus`, but Phase 4 narrows to only the three user-facing values; the Phase 3 `'running'` state is filtered out by `latestFinished()`).
    - `interface SuggestedAction { id: string; text: string; metric: MetricName; direction: 'low' | 'high'; }` (per D-08; carries the catalog `id` so contract tests assert specific catalog entries fired).
    - `interface Pattern { kind: 'placeholder_v1_empty'; }` — placeholder per D-07; reserved slot, never populated in v1. The discriminator-with-one-arm pattern documents "V2 expansion" without polluting the v1 type surface.
    - `interface DailyReviewResult { data_status: DataStatus; today_state: TodayMetrics; anomalies: Anomaly[]; patterns: Pattern[]; actions: SuggestedAction[]; confidence: ConfidenceGate; insufficient_reason: string | null; }` (verbatim D-03).
    - `interface WeekSummary { scored_day_count: number; worst_days: WorstDay[]; best_day: { date: string; recovery_score: number } | null; avg_strain: number | null; total_sleep_hours: number | null; }` (per D-16).
    - `type DecisionPrompt = | { kind: 'silent' } | { kind: 'none_this_week'; suggested_text: string };` (D-22 — discriminated union, ADR-0004 typed positive output).
    - `interface WeeklyReviewResult { data_status: DataStatus & { week_start: string; week_end: string; pattern_test_window: { start: string; end: string; scored_day_count: number } }; week_summary: WeekSummary; pattern: WeeklyPattern; candidate_results: CandidateResult[]; decision_prompt: DecisionPrompt; confidence: ConfidenceGate; }` (D-16 — the `data_status` is the daily shape extended with weekly-only fields).
    No tests in this task — the import-graph tests in subsequent tasks indirectly verify these compile. `tsc --noEmit` in this task's verify step is the contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `src/domain/review/types.ts` exists with all interfaces + unions above. `tsc --noEmit` returns 0. Every D-03 + D-16 slot is present. `Pattern[]` slot exists but `Pattern` has only a `placeholder_v1_empty` discriminator (D-07 anchor for future V2 expansion). `DecisionPrompt` is a discriminated union, NOT optional (D-22).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Service-layer type files — decision, cache, api-gap</name>
  <files>src/services/decision/types.ts, src/services/cache/types.ts, src/services/api-gap/types.ts</files>
  <behavior>
    - `AddDecisionInput`: shape matches D-19 CLI flag map (`decision: string`, `category?`, `rationale?`, `confidence?`, `expectedEffect?`, `followUpDate?` — all optional except `decision`).
    - `ReviewDecisionsInput`: D-21 dual-mode discriminated union — `{ mode: 'list'; includeAll?: boolean }` | `{ mode: 'update'; id: string; status: 'open'|'followed_up'|'abandoned'; notes?: string | null }`.
    - `ReviewDecisionsResult`: shape per D-21 — `{ mode: 'list'; decisions: Decision[] }` | `{ mode: 'update'; decision: Decision }`.
    - `QueryCacheInput`: per D-24 — typed discriminated union over 8 resource arms (cycles, recoveries, sleeps, workouts, profile, body_measurements, sync_runs, decisions). Each arm carries only the filters valid for that resource.
    - `QueryCacheResult`: `{ resource: ResourceName | 'sync_runs' | 'decisions'; rows: unknown[]; count: number; truncated: boolean }` — `rows` typed as `unknown[]` here because per-resource narrowing happens at the formatter / service dispatch site (D-24).
    - `ApiGapEntry`: per D-28 — `{ feature, whoop_consumer_path, available_via_v2_api: false, alternative_via_v2, notes }`.
    - `ApiGapResult`: `{ entries: ApiGapEntry[] }`.
  </behavior>
  <read_first>
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-19 (decision add fields), D-20 (decision review/update surfaces), D-21 (whoop_review_decisions dual-mode), D-24 (QueryCacheInput typed-union per resource), D-28 (ApiGapEntry shape)
    - @src/domain/types/sync.ts (ResourceName tuple reuse for QueryCacheInput arms)
    - @src/domain/types/entities.ts (Decision, Cycle, Recovery, Sleep, Workout, Profile, BodyMeasurement — Phase 3 entity types)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/services/cache/types.ts + §src/services/decision/types.ts + §src/services/api-gap/data.ts entries
  </read_first>
  <action>
    Create `src/services/decision/types.ts`:
    - `import type { Decision } from '../../domain/types/entities.js';`
    - `interface AddDecisionInput { decision: string; category?: string; rationale?: string | null; confidence?: 'low' | 'medium' | 'high' | null; expectedEffect?: string | null; followUpDate?: string; }` — D-19 verbatim; `followUpDate` is already-resolved ISO yyyy-mm-dd (CLI `--follow-up "in 7d"` parsing happens in the CLI shim per RESEARCH §CLI Surface).
    - `type ReviewDecisionsInput = | { mode: 'list'; includeAll?: boolean } | { mode: 'update'; id: string; status: 'open' | 'followed_up' | 'abandoned'; notes?: string | null };` (D-21 dual-mode).
    - `type ReviewDecisionsResult = | { mode: 'list'; decisions: Decision[] } | { mode: 'update'; decision: Decision };`
    - `interface UpdateDecisionInput { id: string; status: 'open' | 'followed_up' | 'abandoned'; notes?: string | null }` (CLI-layer convenience type — the CLI shim resolves prefix-to-id then constructs ReviewDecisionsInput).
    Create `src/services/cache/types.ts`:
    - `import type { ResourceName } from '../../domain/types/sync.js';`
    - `import type { Cycle, Recovery, Sleep, Workout, Profile, BodyMeasurement, Decision } from '../../domain/types/entities.js';`
    - The discriminated union per D-24 (8 arms):
      ```ts
      export type QueryCacheInput =
        | { resource: 'cycles'; since?: string; until?: string; includeUnscored?: boolean; includeExcluded?: boolean; limit?: number }
        | { resource: 'recoveries'; since?: string; until?: string; includeUnscored?: boolean; minRecoveryScore?: number; maxRecoveryScore?: number; limit?: number }
        | { resource: 'sleeps'; since?: string; until?: string; includeUnscored?: boolean; limit?: number }
        | { resource: 'workouts'; since?: string; until?: string; includeUnscored?: boolean; sportId?: number; limit?: number }
        | { resource: 'profile' }
        | { resource: 'body_measurements'; since?: string; until?: string; limit?: number }
        | { resource: 'sync_runs'; status?: 'ok' | 'partial' | 'failed' | 'running'; since?: string; limit?: number }
        | { resource: 'decisions'; status?: 'open' | 'followed_up' | 'abandoned'; category?: string; limit?: number };
      ```
    - `export type QueryCacheResource = QueryCacheInput['resource'];` (derived type for switch exhaustiveness).
    - `export interface QueryCacheResult { resource: QueryCacheResource; rows: unknown[]; count: number; truncated: boolean }` — `rows` is `unknown[]` at the service-layer boundary; per-resource narrowing happens at the formatter dispatch site (Wave 3 Plan 04-09) via a switch on `result.resource`.
    - Document via JSDoc: `limit` default = 100, hard cap = 500 (D-24 §last paragraph).
    Create `src/services/api-gap/types.ts`:
    - `interface ApiGapEntry { feature: string; whoop_consumer_path: string; available_via_v2_api: false; alternative_via_v2: string | null; notes: string }` (D-28 verbatim; `available_via_v2_api: false` is a literal type — every v1 entry is unavailable; documents the contract at the type level).
    - `interface ApiGapResult { entries: ApiGapEntry[] }`.
    No business logic. No `console.*`. No I/O imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    All three files exist. `tsc --noEmit` returns 0. `QueryCacheInput` has exactly 8 discriminated arms matching D-24 verbatim. `ReviewDecisionsInput` is a 2-arm union per D-21. `ApiGapEntry.available_via_v2_api` is the literal `false` type.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type-layer | This plan ships zero runtime code. Threats land in plans that implement against these types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-S1 | Tampering | `ZAnalysis` discriminated union | mitigate | The third `baseline_mad_zero` variant prevents the "MAD = 0 → divide by zero → NaN" cascade. Wave 1 stats plan asserts the variant is returned when MAD = 0. |
| T-04-S4 | Information Disclosure | `QueryCacheInput` discriminated union | mitigate | Typed-union-per-resource refuses free-form SQL at the type system. Wave 4 MCP tool plan asserts the Zod schema mirrors this shape so untrusted MCP input cannot widen the contract at runtime. |
</threat_model>

<verification>
After all 4 tasks complete:

```sh
# All type files compile under strict TS
npx tsc --noEmit

# Type-narrowing tests pass
npx vitest run src/domain/baselines/types.test.ts src/domain/anomalies/types.test.ts src/domain/patterns/types.test.ts src/domain/confidence/types.test.ts

# Full suite still green (no regressions)
npm test
```
</verification>

<success_criteria>
- 8 source `*types*.ts` files exist across `src/domain/{baselines,anomalies,patterns,confidence,review}/` and `src/services/{decision,cache,api-gap}/`.
- 4 sibling `*.test.ts` files green.
- `tsc --noEmit` returns 0.
- Every discriminated union from CONTEXT (ZAnalysis, WeeklyPattern, DecisionPrompt, QueryCacheInput, ReviewDecisionsInput) is exported with the exact variants specified by the locked decisions.
- D-04's 9-metric `METRIC_NAMES` tuple, D-11's 5-candidate type union (via `CANDIDATE_FACTORS_TYPE_ONLY`), and D-13's tier thresholds are encoded at the type level.
- D-34's `pattern_confidence: 'weak' | 'strong'` field lives on the `detected` arm of `WeeklyPattern`, not as a top-level slot.
- No business logic in this plan (zero non-type exports outside the type narrowing helpers).
</success_criteria>

<output>
Create `.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-02-SUMMARY.md` when done summarizing: types created, discriminated-union variant counts, and Wave 1+2+3+4 plan readiness against the contracts.
</output>
