---
phase: 04-domain-math-reviews-decision-ledger-mcp-surface
plan: 09
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-05
  - 04-06
  - 04-07
  - 04-08
files_modified:
  - src/formatters/daily-review.txt.ts
  - src/formatters/daily-review.txt.test.ts
  - src/formatters/weekly-review.txt.ts
  - src/formatters/weekly-review.txt.test.ts
  - src/formatters/decision.txt.ts
  - src/formatters/decision.txt.test.ts
  - src/formatters/query-cache.txt.ts
  - src/formatters/query-cache.txt.test.ts
  - src/formatters/api-gap.txt.ts
  - src/formatters/api-gap.txt.test.ts
  - tests/contract/formatter-tone.test.ts
  - tests/contract/daily-review-shape.test.ts
  - tests/fixtures/decisions/decision-add-happy-path.json
  - tests/fixtures/decisions/decision-review-list.json
  - tests/fixtures/decisions/decision-review-interactive-update.json
autonomous: true
requirements:
  - REV-03
  - REV-04
  - REV-08
  - DEC-03
  - MCP-04

must_haves:
  truths:
    - "renderDailyReview leads with 'Data status:' section per REV-04 (data-freshness first); REV-03 D-03 slot coverage verified by tests/contract/daily-review-shape.test.ts"
    - "renderWeeklyReview dispatches on pattern.kind via exhaustive switch (ADR-0004 forcing function); renders 'no reliable pattern detected' with the typed reason when kind === 'no_pattern'"
    - "renderWeeklyReview surfaces decision_prompt.suggested_text as the FINAL line when decision_prompt.kind === 'none_this_week' per D-22"
    - "renderWeeklyReview renders pattern_confidence annotation when pattern.kind === 'detected' (D-34: 'small sample — effect estimates imprecise' when weak, no extra annotation when strong)"
    - "renderDecisionList dispatches on input shape: list (column-padded id-prefix | category | decision | elapsed/window | over_window); single (detail block); update-result ('decision <ulid-prefix> updated to <status>')"
    - "tests/contract/formatter-tone.test.ts runs every formatter over every fixture in tests/fixtures/review/ + decisions/ + the catalogs ACTION_CATALOG + DECISION_PROMPT_CATALOG + the 4 prompt instruction constants (Plan 04-10 will register the constants); rendered output passes containsBannedToneToken + EMOJI_RE"
    - "ADR-0005 §Enforcement defence-in-depth complete: Gate A (source-level) + D-26 contract test (rendered-output level)"
  artifacts:
    - path: "src/formatters/daily-review.txt.ts"
      provides: "renderDailyReview(result: DailyReviewResult): string"
      min_lines: 150
    - path: "src/formatters/weekly-review.txt.ts"
      provides: "renderWeeklyReview(result: WeeklyReviewResult): string"
      min_lines: 200
    - path: "src/formatters/decision.txt.ts"
      provides: "renderDecisionList(decisions: Decision[]) + renderDecisionDetail(d: Decision) + renderDecisionUpdate(d: Decision): string"
      min_lines: 150
    - path: "src/formatters/query-cache.txt.ts"
      provides: "renderQueryCache(result: QueryCacheResult): string with per-resource sub-renderers"
      min_lines: 160
    - path: "src/formatters/api-gap.txt.ts"
      provides: "renderApiGap(result: ApiGapResult): string"
      min_lines: 60
    - path: "tests/contract/formatter-tone.test.ts"
      provides: "D-26 banned-word lint over rendered output × every fixture × every formatter (REPLACES the Wave 0 scaffold)"
      contains: "BANNED_TONE_WORDS"
    - path: "tests/contract/daily-review-shape.test.ts"
      provides: "REV-03 schema + REV-04 data-status-leads assertion (REPLACES the Wave 0 scaffold)"
      contains: "data_status"
  key_links:
    - from: "src/formatters/weekly-review.txt.ts"
      to: "src/domain/patterns/types.ts (WeeklyPattern discriminated union)"
      via: "exhaustive switch on pattern.kind per ADR-0004 forcing function"
      pattern: "kind === 'detected'|kind === 'no_pattern'"
    - from: "tests/contract/formatter-tone.test.ts"
      to: "src/domain/banned-words.ts, all 5 new formatters, ACTION_CATALOG, DECISION_PROMPT_CATALOG"
      via: "describe.each iterates every formatter × every fixture; asserts rendered output passes containsBannedToneToken"
      pattern: "BANNED_TONE_WORDS|containsBannedToneToken|EMOJI_RE"
---

<objective>
Ship the 5 new formatters that render Phase 4's typed result objects to compact text, plus the D-26 defence-in-depth contract test that re-checks every rendered output against the banned-word lint. Wave 3 closes the source-level → rendered-output ADR-0005 enforcement loop: Wave 1 lint the catalog source, Wave 3 lint the rendered output.

Purpose: REV-04 (lead with data freshness) is encoded by `renderDailyReview` putting `Data status:` first. REV-08 (banned tone words enforced on output) closes via the contract test. REV-03 (D-03 schema rendered correctly) anchored by `tests/contract/daily-review-shape.test.ts`. ADR-0004's forcing function (typed positive output for absence) lives in the exhaustive `switch (pattern.kind)` — the renderer compiles only when the discriminated union is fully covered.

Wave 4 surfaces (Plans 04-10, 04-11) consume these formatters: every CLI command renders via `process.stdout.write(`${formatXxx(result)}\n`)`; every MCP tool returns `content: [{type: 'text', text: formatXxx(result)}]` per MCP-02 dual-shape contract.

Decision fixtures live in this plan (not Plan 04-06 or 04-07) because their primary consumers are the formatter tests; the decision service tests in Plan 04-06 use in-line synthetic objects.

Output: 5 formatter files + 5 sibling tests + 2 fully-populated contract tests + 3 decision fixture JSONs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md
@.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md
@agent_docs/decisions/0001-mcp-stdout-purity.md
@agent_docs/decisions/0004-no-reliable-pattern-positive-output.md
@agent_docs/decisions/0005-banned-tone-words.md
@src/domain/banned-words.ts
@src/domain/review/types.ts
@src/domain/patterns/types.ts
@src/domain/types/entities.ts
@src/services/cache/types.ts
@src/services/api-gap/types.ts
@src/formatters/sync.txt.ts
@src/formatters/doctor.txt.ts

<interfaces>
Canonical formatter precedent (src/formatters/sync.txt.ts):
- Column-padding constants at module scope (`RESOURCE_COL_WIDTH = 20`, `STATUS_COL_WIDTH = 15`).
- Exhaustive `statusSuffix(status: ResourceSyncStatus)` switch with NO `default:` clause (compile-time forcing function — adding a new status fails to compile).
- Pure function `(typedResult): string`.
- One responsibility per file; comments only on tone-discipline boundaries.

Wave 0 banned-words (Plan 04-01):
- `BANNED_TONE_WORDS` (10-tuple), `containsBannedToneToken(text): {hit, word?, index?}`, `EMOJI_RE` (regex).

Wave 0 scaffolds (Plan 04-01):
- `tests/contract/formatter-tone.test.ts` (it.todo + sanity for BANNED_TONE_WORDS length).
- `tests/contract/daily-review-shape.test.ts` (it.todo placeholder).

Wave 1+2 inputs (Plans 04-05, 04-07):
- `DailyReviewResult` per D-03; `WeeklyReviewResult` per D-16; `Decision` (Phase 3 entity); `QueryCacheResult`; `ApiGapResult`.
- `WeeklyPattern` discriminated union with `kind: 'detected' | 'no_pattern'`.
- `DecisionPrompt` discriminated union with `kind: 'silent' | 'none_this_week'`.

Wave 2 fixtures (Plan 04-07):
- 5 daily fixtures + 5 weekly fixtures under `tests/fixtures/review/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship renderDailyReview + renderWeeklyReview formatters</name>
  <files>src/formatters/daily-review.txt.ts, src/formatters/daily-review.txt.test.ts, src/formatters/weekly-review.txt.ts, src/formatters/weekly-review.txt.test.ts, tests/contract/daily-review-shape.test.ts</files>
  <behavior>

  ### renderDailyReview(result: DailyReviewResult): string
  Multi-section output per D-03 (REV-04 lead-with-data-freshness; data_status FIRST):

  ```
  Data status:
    Reviewed date: 2026-03-15  (source: latest_scored, staleness: 2d)
    Last sync: 2026-03-14T08:33:01Z  (ok)
    Baseline window: 2026-02-14 to 2026-03-15  (22 SCORED days, 73% coverage)
    Missing resources: (none)

  Today's measurements:
    Recovery       52
    HRV (rMSSD)    38ms
    Resting HR     58bpm
    Strain         11.2
    Sleep          7.4h  (89% efficiency)
    Resp. rate     14.8bpm
    SpO2           97%
    Skin temp      -0.2°C

  Anomalies:
    HRV (rMSSD)    -2.1σ (median 50ms, robust σ 5.7ms, tier: strong) — low

  Actions:
    1. Take a recovery day or keep intensity easy.
    2. Hold off on high-strain sessions today.

  Confidence: strong
  ```

  Rules:
  - When `confidence.tier === 'insufficient'`: render data_status + today_state + `Confidence: insufficient — ${insufficient_reason}` only; omit Anomalies + Actions sections (they are empty per REV-05).
  - When `anomalies.length === 0`: omit the Anomalies section entirely (no "Anomalies: none" filler — ADR-0004 typed positive output rendered as section omission).
  - When `actions.length === 0`: omit the Actions section entirely.
  - `Patterns` slot is empty in v1 per D-07 — omit the section entirely.
  - `missing_resources` rendering: when empty, render `(none)`; when populated, comma-join the resource names.
  - The Date formatting uses `date-fns` (`format(parseISO(date), 'yyyy-MM-dd')`).
  - Numbers rounded to 1 decimal for HRV/strain/strength values; integers for HR/recovery/SpO2/sleep efficiency; sleep duration as hours-decimal.

  ### renderWeeklyReview(result: WeeklyReviewResult): string
  Multi-section output per D-16:

  ```
  Data status:
    Reviewed date: 2026-03-15  (source: latest_scored, staleness: 0d)
    Week: 2026-03-09 to 2026-03-15
    Pattern test window: 2026-02-16 to 2026-03-15  (22 SCORED days)
    Last sync: 2026-03-15T11:02:14Z  (ok)

  Week summary:
    Days scored: 7
    Worst days: 2026-03-09 (38), 2026-03-11 (41)
    Best day: 2026-03-13 (78)
    Avg strain: 14.2
    Total sleep: 49.8h

  Pattern over trailing 28 days:
    Detected: sleep_duration_prior_night was lower on worst-recovery days
              (U=24, p_raw=0.008, p_adjusted=0.040)
              Sample: 5 worst-days vs 15 other-days
              Confidence: strong

  Candidate factors (ranked):
    1. sleep_duration_prior_night    p_raw=0.008  p_adj=0.040  cleared
    2. sleep_debt_3d_rolling         p_raw=0.12   p_adj=0.30   not cleared
    3. day_strain_prior_day          p_raw=0.31   p_adj=0.39   not cleared
    4. hrv_delta_prior_day           p_raw=0.45   p_adj=0.56   not cleared
    5. workout_timing_late_evening   p_raw=0.62   p_adj=0.62   not cleared

  Decision prompt:
    Sleep on worst-recovery days was meaningfully shorter. Add a decision: `recovery-ledger decision add "sleep at least 7h on training days" --category sleep`
  ```

  Dispatch on `pattern.kind` via exhaustive switch (ADR-0004 forcing function):
  - `'detected'`: render the section above; include `pattern_confidence` annotation when `weak`: render "  Small sample — effect estimates are imprecise." (D-34 — per RESEARCH §Pitfall 2 recommendation).
  - `'no_pattern'`: render "Pattern over trailing 28 days:\n  No reliable pattern detected. Reason: ${reason}" where reason is one of the three D-16 typed reasons.

  Decision prompt section:
  - When `decision_prompt.kind === 'silent'`: omit the section entirely.
  - When `decision_prompt.kind === 'none_this_week'`: render as the FINAL section, single line with the `suggested_text` (D-22 — agent / user surfaces as follow-up question).

  `candidate_results` always rendered (ADR-0004 §If FDR set empty — unranked context for self-vetting). Sort by `p_adjusted` ascending; render `p_raw`, `p_adjusted`, cleared/not-cleared/refused status per row.

  ### Test files
  - `daily-review.txt.test.ts`: load each of the 5 daily fixtures (Plan 04-07), call `renderDailyReview(fixture.expected)` if fixtures include expected results OR load the in-memory DB + run getDailyReview + render. RECOMMEND: tests load fixtures + run getDailyReview + render so the full pipeline is asserted (the formatter's input must match what the service emits).
  - Assertions: rendered output starts with the literal `Data status:` line (REV-04 anchor). Anomalies section present when fixture has anomalies; absent when empty (ADR-0004 — no `Anomalies: (none)` filler). Insufficient fixture renders `Confidence: insufficient — ` + the reason string. NO banned-word tokens present in any rendered output (Task 3 contract test owns this comprehensively; per-formatter test does a sanity sweep over each output).
  - `weekly-review.txt.test.ts`: load each of the 5 weekly fixtures; assert pattern dispatch arms render correctly. The REV-07 fixture renders `No reliable pattern detected. Reason: no_factor_cleared_fdr`. The D-34 weak-confidence fixture (build inline if Plan 04-07's `weekly-pattern-clears-fdr.json` is N=22 → strong; need a 14-19 SCORED variant for the weak case — add `weekly-pattern-clears-fdr-weak.json` here OR adapt the fixture set) renders the "Small sample — effect estimates are imprecise." annotation.
  - `tests/contract/daily-review-shape.test.ts` (replaces Wave 0 scaffold): load each daily fixture; assert the rendered output:
    - Starts with `Data status:` line (REV-04).
    - Contains every D-03 slot's required label string (`Today's measurements:`, `Confidence:`).
    - For insufficient case: contains `Confidence: insufficient — ` AND does NOT contain `Anomalies:` (omitted per REV-05).
    - For multi-anomaly case: contains `Anomalies:` AND at most 3 numbered actions.
  </behavior>
  <read_first>
    - @src/formatters/sync.txt.ts (canonical formatter precedent + exhaustive-switch idiom)
    - @src/formatters/doctor.txt.ts (multi-section output precedent)
    - @src/domain/review/types.ts (DailyReviewResult + WeeklyReviewResult contracts)
    - @src/domain/patterns/types.ts (WeeklyPattern union — exhaustive switch target)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-03/D-04/D-08/D-10/D-16/D-22/D-34 (every rendering decision)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Formatter Architecture §5 new formatters (the section breakdown per formatter)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/formatters/daily-review.txt.ts + §weekly-review.txt.ts (analog mapping)
    - @tests/fixtures/review/*.json (Plan 04-07 fixtures)
    - @src/domain/banned-words.ts (Plan 04-01 lint utility — used in sanity sweeps + Task 3 contract test)
  </read_first>
  <action>
    Implement both formatters per the behavior above. NO `console.*` (Gate B — formatters are MCP-reachable; Pino-via-stderr is for ops logs not implemented in formatters). Each file is a pure `(typedResult) => string` function (Shared Pattern 5). Comments avoid the literal banned tokens (Phase 1-3 comment-vs-grep convention).

    Helpers per formatter (module-scope constants):
    - `daily-review.txt.ts`: `formatStaleness(days: number): string` (e.g., "0d" / "5d"); `formatMetricLine(label, value, unit?)`; `formatAnomalyLine(anomaly: Anomaly)`; `renderDataStatus(ds: DataStatus): string[]`; `renderTodayMetrics(today: TodayMetrics): string[]`.
    - `weekly-review.txt.ts`: `formatPatternSection(pattern: WeeklyPattern): string[]` with exhaustive switch on `pattern.kind`; `formatCandidateResultsTable(results: CandidateResult[]): string[]` (ranked by p_adjusted ASC); `formatDecisionPrompt(prompt: DecisionPrompt): string[] | null` (returns null when silent → omit section).

    Date formatting uses `date-fns` with explicit `format` patterns (not Date.toString()). ALL string concatenation MUST not contain banned tokens (Gate A scans this source file at CI time).

    For the D-34 weak-confidence test case: if no existing fixture from Plan 04-07 has N_scored ∈ [14, 20], add `tests/fixtures/review/weekly-pattern-clears-fdr-weak.json` to fill the gap (engineering: 16 SCORED cycles with strong sleep-duration effect on worst-quartile so candidate clears at N=16 → pattern_confidence='weak').
  </action>
  <verify>
    <automated>npx vitest run src/formatters/daily-review.txt.test.ts src/formatters/weekly-review.txt.test.ts tests/contract/daily-review-shape.test.ts</automated>
  </verify>
  <done>
    Both formatters ship + green against all 5 daily + 5 weekly fixtures (plus the weak-confidence variant if added). REV-03/04/05 anchored in the rendering. ADR-0004 exhaustive switch on `pattern.kind`. D-34 pattern_confidence annotation when weak. D-22 decision_prompt rendered as final line when none_this_week. No `console.*`. Gate A clean over formatter source.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship renderDecisionList + renderQueryCache + renderApiGap formatters</name>
  <files>src/formatters/decision.txt.ts, src/formatters/decision.txt.test.ts, src/formatters/query-cache.txt.ts, src/formatters/query-cache.txt.test.ts, src/formatters/api-gap.txt.ts, src/formatters/api-gap.txt.test.ts, tests/fixtures/decisions/decision-add-happy-path.json, tests/fixtures/decisions/decision-review-list.json, tests/fixtures/decisions/decision-review-interactive-update.json</files>
  <behavior>

  ### renderDecisionList(arg: Decision[] | Decision | ReviewDecisionsResult): string
  Dispatch on input shape:
  - `Decision[]`: column-padded table per D-20:
    ```
    ID         Category    Decision                                    Elapsed/Window  Status
    01HK7XYZ   sleep       sleep at least 7h on training days          3d/7d           open
    01HK7ABC   training    deload week starting Monday                 12d/14d         open
    ```
    Columns: id-prefix (first 8 chars), category, decision text (truncated to 40 chars with ellipsis if longer), `elapsed_days/expected_window_days` (computed from `created_at` + `followUpDate`), status. `over_window` flag rendered as `*` suffix on the elapsed column when elapsed > expected.
  - Single `Decision`: detail block (multi-line, all fields).
  - `ReviewDecisionsResult` with `mode: 'update'`: single-line `decision <ulid-prefix> updated to <status>` per D-21.

  ### renderQueryCache(result: QueryCacheResult): string
  Per-resource sub-renderers (dispatch on `result.resource`):
  - `cycles`: table of `start | end | day_strain | day_recovery_score | score_state | baseline_excluded`.
  - `recoveries`: table of `cycle_id | recovery_score | hrv_rmssd_milli | resting_heart_rate | score_state`.
  - `sleeps`: table of `id | start | end | duration_minutes | efficiency_percent | score_state`.
  - `workouts`: table of `id | start | end | sport_id | day_strain | score_state`.
  - `profile`: single block with `user_id, first_name, last_name, email`.
  - `body_measurements`: table of `measured_at | height_meters | weight_kilograms | max_heart_rate`.
  - `sync_runs`: reuse Phase 3's `sync.txt.ts` per-resource line format (audit + import or duplicate).
  - `decisions`: reuse `renderDecisionList` for the list path.
  - Trailing line: `count: N (truncated: true|false)`.

  ### renderApiGap(result: ApiGapResult): string
  One line per ApiGapEntry per D-28:
  ```
  Healthspan: WHOOP app → Health Monitor → Healthspan
    Not available via WHOOP v2 API. Closest proxy: long-run trends in recovery_score.
  ECG (electrocardiogram): WHOOP app → Heart → ECG
    Not available via WHOOP v2 API. No closest proxy.
  ...
  ```

  ### Test files
  - `decision.txt.test.ts`: assert column widths, ellipsis truncation, over_window flag, update form, detail form. Use the 3 new decision fixtures.
  - `query-cache.txt.test.ts`: assert per-resource sub-renderer outputs given small fixture arrays; assert truncated suffix in the trailing line.
  - `api-gap.txt.test.ts`: assert every entry from Plan 04-06's API_GAP_ENTRIES renders (call `getApiGap()` then `renderApiGap`).
  </behavior>
  <read_first>
    - @src/formatters/sync.txt.ts (column-padding precedent)
    - @src/domain/types/entities.ts (Decision shape)
    - @src/services/cache/types.ts (QueryCacheResult)
    - @src/services/api-gap/types.ts (ApiGapResult)
    - @src/services/decision/types.ts (ReviewDecisionsResult discriminated union)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-20 (decision review columns), D-21 (update form), D-24 (per-resource shape), D-28 (api-gap rendering)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-PATTERNS.md §src/formatters/decision.txt.ts + §query-cache.txt.ts + §api-gap.txt.ts
  </read_first>
  <action>
    Implement 3 formatters per the behavior above. Each file is pure (no I/O), uses module-scope column-width constants, uses exhaustive switch on the discriminator field (resource for query-cache; shape-dispatch for decision via type-narrowing). The `decision.txt.ts` shape dispatch uses TypeScript narrowing on `Array.isArray(arg) || 'mode' in arg ? ... : ...`.

    Create 3 decision fixture JSON files under `tests/fixtures/decisions/`:
    - `decision-add-happy-path.json`: a single Decision with category='sleep', decision="sleep at least 7h on training days", confidence='medium', expected_effect="+5% HRV", follow_up_date 7 days out.
    - `decision-review-list.json`: array of 3 Decisions (1 open within window, 1 open over window, 1 followed_up). Renders all 3 in list form (when `includeAll: true`).
    - `decision-review-interactive-update.json`: a `ReviewDecisionsResult` with `mode: 'update'` and an updated Decision.

    `query-cache.txt.ts` reuses the Phase 3 sync_runs line format — RECOMMEND importing `formatPerResourceLine` from `src/formatters/sync.txt.ts` if it's exported, else duplicate the format string (small duplication is acceptable to avoid coupling).

    NO `console.*` anywhere. Gate A clean over all 3 source files. All 3 sibling tests green.
  </action>
  <verify>
    <automated>npx vitest run src/formatters/decision.txt.test.ts src/formatters/query-cache.txt.test.ts src/formatters/api-gap.txt.test.ts</automated>
  </verify>
  <done>
    3 formatters shipped with sub-rendering dispatch. 3 decision fixtures in place. Per-formatter tests green. Trailing truncation suffix verified for query-cache.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Populate tests/contract/formatter-tone.test.ts with full D-26 coverage</name>
  <files>tests/contract/formatter-tone.test.ts</files>
  <behavior>
    Replace the Wave 0 `it.todo` scaffold with the FULL D-26 contract test per RESEARCH §Formatter Architecture §D-26 contract test sketch:
    - `describe.each` over the 5 new formatters + the 2 Phase 1-3 formatters (`renderDoctor`, `formatSyncResult` — retroactive coverage).
    - Each formatter × each fixture in its category (review fixtures × daily/weekly formatters; decisions fixtures × decision formatter; etc.).
    - For each rendered output: iterate `BANNED_TONE_WORDS`, assert `containsBannedToneToken(rendered).hit === false`. Plus `EMOJI_RE.test(rendered) === false`.
    - Catalog lint: iterate `ACTION_CATALOG` (Plan 04-05) + `DECISION_PROMPT_CATALOG` (Plan 04-05) + `API_GAP_ENTRIES` (Plan 04-06) entries × their string fields; assert each passes the lint.
    - Prompt instruction lint (placeholder): the test imports the 4 prompt instruction constants — they don't exist until Plan 04-10 lands. RECOMMEND: this task skips the prompt-instruction subset and Plan 04-10 extends the test to include them. OR: emit empty `PROMPT_INSTRUCTIONS = []` constant in Plan 04-10's prompt files so the iteration is empty in this wave + non-empty in Wave 4. RECOMMEND OPTION B — Plan 04-10 will extend the array.
    - Sanity: `BANNED_TONE_WORDS.length === 10` (Plan 04-01 already covers this; keep here for atomic test self-coverage).
    - All assertions table-driven with descriptive test names per fixture × per banned word so a failure clearly identifies WHICH formatter + WHICH fixture + WHICH banned word.
  </behavior>
  <read_first>
    - @tests/contract/formatter-tone.test.ts (Wave 0 scaffold)
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-RESEARCH.md §Formatter Architecture §D-26 contract test sketch
    - @.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-CONTEXT.md D-26 (defence-in-depth two layers)
    - @agent_docs/decisions/0005-banned-tone-words.md (the ADR)
    - @src/domain/banned-words.ts (Plan 04-01 utility)
  </read_first>
  <action>
    Replace `tests/contract/formatter-tone.test.ts` content. Structure:
    ```ts
    import { describe, it, expect } from 'vitest';
    import { glob } from 'glob';
    import { readFileSync } from 'node:fs';
    import { BANNED_TONE_WORDS, containsBannedToneToken, EMOJI_RE } from '../../src/domain/banned-words.js';
    import { renderDailyReview } from '../../src/formatters/daily-review.txt.js';
    // ... 4 more formatter imports
    import { ACTION_CATALOG } from '../../src/domain/actions/catalog.js';
    import { DECISION_PROMPT_CATALOG } from '../../src/domain/actions/decision-prompts.js';
    import { API_GAP_ENTRIES } from '../../src/services/api-gap/data.js';

    const cases = [
      { name: 'daily-review', formatter: renderDailyReview, fixtures: 'tests/fixtures/review/daily-*.json' },
      { name: 'weekly-review', formatter: renderWeeklyReview, fixtures: 'tests/fixtures/review/weekly-*.json' },
      // ... 3 more
    ];

    describe('formatter tone lint (D-26 defence-in-depth layer 2)', () => {
      for (const c of cases) {
        const fixturePaths = glob.sync(c.fixtures);
        describe(c.name, () => {
          for (const fixturePath of fixturePaths) {
            it(`${fixturePath} renders without banned tokens`, () => {
              const input = JSON.parse(readFileSync(fixturePath, 'utf8'));
              // Optional: run the input through the service if the fixture is a DB-state fixture
              // rather than a pre-rendered result; daily/weekly fixtures are DB-state.
              const result = /* run service or use fixture directly */;
              const rendered = c.formatter(result);
              const hit = containsBannedToneToken(rendered);
              expect(hit.hit, hit.hit ? `Banned token '${hit.word}' found at index ${hit.index} in ${c.name} output: ${rendered.slice(Math.max(0, hit.index - 30), hit.index + 30)}` : '').toBe(false);
              expect(EMOJI_RE.test(rendered)).toBe(false);
            });
          }
        });
      }

      describe('catalog source-level lint (ADR-0005 §Enforcement bullet 1)', () => {
        for (const entry of ACTION_CATALOG) {
          it(`ACTION_CATALOG[${entry.id}] text passes tone lint`, () => {
            expect(containsBannedToneToken(entry.text).hit).toBe(false);
            expect(EMOJI_RE.test(entry.text)).toBe(false);
          });
        }
        for (const entry of DECISION_PROMPT_CATALOG) {
          it(`DECISION_PROMPT_CATALOG[${entry.id}] text passes tone lint`, () => {
            expect(containsBannedToneToken(entry.text).hit).toBe(false);
            expect(EMOJI_RE.test(entry.text)).toBe(false);
          });
        }
        for (const entry of API_GAP_ENTRIES) {
          it(`API_GAP_ENTRIES[${entry.feature}] passes tone lint`, () => {
            for (const field of [entry.feature, entry.whoop_consumer_path, entry.notes, entry.alternative_via_v2].filter((v): v is string => v !== null)) {
              expect(containsBannedToneToken(field).hit).toBe(false);
            }
          });
        }
      });
    });
    ```

    The "run service or use fixture directly" choice: review fixtures are DB-state inputs (cycles, recoveries, sleeps, workouts). The contract test loads each fixture into an in-memory DB via the Phase 3 helper, runs the service, then renders. This is heavier than direct-input fixtures but exercises the full pipeline. RECOMMEND: this approach (full pipeline) for the review fixtures; direct-input for decision + api-gap + query-cache (the result objects can be hand-authored).

    NO `console.*`. Test uses `glob` (audit if installed; if not, switch to `fs.readdirSync` + filter pattern — simpler and zero-dep).

    Document at the top of the file: "D-26 defence-in-depth — Layer 2 contract test. Layer 1 is `scripts/ci-grep-gates.sh` Gate A scanning formatter source. This test re-checks RENDERED output across every fixture × every formatter so generated content (catalog strings concatenated into templates) is caught." (No literal banned tokens in comments either.)
  </action>
  <verify>
    <automated>npx vitest run tests/contract/formatter-tone.test.ts</automated>
  </verify>
  <done>
    Contract test populated with full D-26 coverage. Every fixture × every formatter passes the banned-word lint. All catalog entries pass at module-load. ADR-0005 defence-in-depth layer 2 in place. Wave 4 Plan 04-10 will extend with prompt-instruction constants.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service result → formatter → stdout/MCP content | Formatters are pure functions; tone enforcement is the defence-in-depth gate against generated content (catalog strings concatenated into templates) that source-level grep can't see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-S3 | Information Disclosure | formatter source files | mitigate | Formatters never inline secrets — they consume typed result objects from services (which already passed through the Phase 1 sanitizer for MCP returns). Test corpus includes a result with an embedded "Bearer xyz" substring (Wave 4 will inject this via MCP error path); the formatter should render the sanitized form, not the raw substring. |
| T-04-S3 / REV-08 | Information Disclosure (tone leak) | formatter rendered output | mitigate | D-26 contract test re-checks rendered output × every fixture × every formatter. Catalog source already lint-passed at Plan 04-05. Both layers together = ADR-0005 §Enforcement defence-in-depth. |
</threat_model>

<verification>
```sh
npx vitest run src/formatters/ tests/contract/formatter-tone.test.ts tests/contract/daily-review-shape.test.ts
bash scripts/ci-grep-gates.sh
npx tsc --noEmit
```
</verification>

<success_criteria>
- 5 new formatters ship + 5 sibling tests green.
- D-26 contract test (`tests/contract/formatter-tone.test.ts`) covers every fixture × every formatter + every catalog entry.
- `tests/contract/daily-review-shape.test.ts` asserts REV-04 data-status-leads + REV-03 D-03 slot coverage.
- ADR-0005 §Enforcement defence-in-depth complete (Gate A source + D-26 rendered output).
- ADR-0004 exhaustive `switch (pattern.kind)` in `weekly-review.txt.ts` (forcing function for future variants).
- All 10 grep gates green.
- 3 new decision fixtures shipped.
- Phase 1-3 formatters (renderDoctor, formatSyncResult) get retroactive D-26 coverage.
</success_criteria>

<output>
Create `.planning/phases/04-domain-math-reviews-decision-ledger-mcp-surface/04-09-SUMMARY.md` when done summarizing: 5 formatters shipped, D-26 layer 2 in place, REV-04 + REV-08 anchored at rendering layer.
</output>
