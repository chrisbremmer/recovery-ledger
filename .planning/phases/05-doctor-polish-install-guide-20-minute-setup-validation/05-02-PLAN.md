---
phase: 05-doctor-polish-install-guide-20-minute-setup-validation
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/services/doctor/checks/whoop-roundtrip.ts
  - src/services/doctor/checks/whoop-roundtrip.test.ts
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "probeWhoopRoundtrip is the ONE online doctor check; honors --offline by short-circuiting to a 'pass' with 'skipped (--offline)' detail per D-03"
    - "On 200 from /v2/user/profile/basic, the probe returns {status:'pass', detail:'profile fetched in <N>ms'} per RESEARCH §Specifics line 269"
    - "On 401 (post-refresh failure), the probe returns {status:'fail', detail:'WHOOP returned 401 after refresh — run `recovery-ledger auth`'} per RESEARCH §Finding 3.1"
    - "On any non-401 4xx, the probe returns {status:'warn', detail:'WHOOP returned <code> — scopes may have drifted; check developer.whoop.com/dashboard/applications'} per D-02 #1"
    - "On network error / throw, the probe returns {status:'fail', detail:'roundtrip failed: <message>'} (sanitized — never leaks Bearer / JWT material per ADR-0001 + Phase 4 D-30 sanitizer)"
    - "The probe calls through services.refreshOrchestrator.callWithAuth (Phase 2 ADR-0002 chokepoint) so a stale token triggers exactly one refresh through the single-flight gate"
    - "Unit test mocks the fetcher (does NOT make real HTTP calls — ADR-0006 / Gate F enforced; the probe file does not import 'fetch' directly because it goes through callWithAuth)"
  artifacts:
    - path: "src/services/doctor/checks/whoop-roundtrip.ts"
      provides: "probeWhoopRoundtrip async function returning Promise<DoctorCheck>"
      contains: "probeWhoopRoundtrip"
    - path: "src/services/doctor/checks/whoop-roundtrip.test.ts"
      provides: "5 test cases: --offline short-circuit, 200 pass with timing, 401 fail, non-401 4xx warn, network-error fail"
      contains: "probeWhoopRoundtrip"
  key_links:
    - from: "src/services/doctor/checks/whoop-roundtrip.ts"
      to: "src/services/refresh-orchestrator.ts"
      via: "refreshOrchestrator.callWithAuth wraps the GET /v2/user/profile/basic request through the single-flight refresh gate"
      pattern: "callWithAuth"
    - from: "src/services/doctor/checks/whoop-roundtrip.ts"
      to: "src/services/doctor/checks/check-names.ts"
      via: "CHECK_NAMES.WHOOP_ROUNDTRIP referenced as the probe name"
      pattern: "WHOOP_ROUNDTRIP"
---

<objective>
Ship the only online doctor check: `whoop_roundtrip`. This is the D-22 deferred check from Phase 2 — Phase 5's reason to exist for the doctor surface. Calls `GET /v2/user/profile/basic` through the Phase 2 `callWithAuth` orchestrator + Phase 3 `httpGet` chokepoint; returns pass/warn/fail per RESEARCH §Finding 3.1.

Purpose: surface "your WHOOP credentials work and the API responds" as a discrete signal alongside the offline-safe `auth` / `token_freshness` checks. A stale token automatically triggers exactly one refresh via the single-flight gate (ADR-0002). A revoked scope shows up as a non-401 4xx warning.

Output: one probe file + one test file. Plan 05-06 wires the probe into runDoctor()'s PROBE_NAMES tuple alongside the other 8 new probes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md
@.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md
@agent_docs/decisions/0001-mcp-stdout-purity.md
@agent_docs/decisions/0002-single-flight-oauth-refresh.md
@agent_docs/decisions/0006-fixture-only-tests.md
@agent_docs/decisions/0007-whoop-read-only.md
@src/services/doctor/checks/auth.ts
@src/services/doctor/checks/token-freshness.ts
@src/services/doctor/checks/check-names.ts
@src/services/refresh-orchestrator.ts
@src/infrastructure/whoop/resources/profile.ts
@src/infrastructure/whoop/errors.ts
@src/services/doctor/index.ts

<interfaces>
<!-- Key existing types the probe consumes. Read the files above; do not invent shapes. -->

From src/services/doctor/index.ts:
  interface DoctorCheck { name: string; status: 'pass'|'warn'|'fail'; detail: string }

From src/services/refresh-orchestrator.ts:
  interface RefreshOrchestrator {
    callWithAuth<T extends FetchLikeResponse>(operation: AuthedOperation<T>, options?: CallWithAuthOptions): Promise<T>
  }
  // refreshOrchestrator default singleton exports callWithAuth bound

From src/infrastructure/whoop/resources/profile.ts:
  getProfile(): Promise<GetProfileResult>   — uses httpGet('/v2/user/profile/basic', {}, WhoopRawProfile)

From src/services/doctor/checks/check-names.ts (post-Plan 05-01):
  CHECK_NAMES.WHOOP_ROUNDTRIP === 'whoop_roundtrip'

Probe shape precedents (read auth.ts, token-freshness.ts):
  - export async function probe<Name>(deps?: {...}, opts?: {...}): Promise<DoctorCheck>
  - return {name, status, detail} — never throw
  - all error paths caught + converted to {status:'fail', detail: sanitized message}
  - never call console.* (ADR-0001 / Gate B)
  - never bare-fetch (Gate F; this probe goes through callWithAuth which goes through httpGet)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/services/doctor/checks/whoop-roundtrip.ts</name>
  <files>src/services/doctor/checks/whoop-roundtrip.ts</files>
  <behavior>
    - Exports `interface WhoopRoundtripDeps { refreshOrchestrator: RefreshOrchestrator; fetcher: (accessToken: string) => Promise<{status: number; durationMs: number}> }`.
    - Exports `probeWhoopRoundtrip(deps: WhoopRoundtripDeps, opts?: {offline?: boolean}): Promise<DoctorCheck>`.
    - When `opts?.offline === true`: returns `{name: CHECK_NAMES.WHOOP_ROUNDTRIP, status: 'pass', detail: 'skipped (--offline)'}` without invoking the fetcher.
    - When the fetcher returns `{status: 200, durationMs: N}`: returns `{name, status: 'pass', detail: 'profile fetched in <Math.round(N)>ms'}`.
    - When the fetcher returns `{status: 401, durationMs: N}`: returns `{name, status: 'fail', detail: 'WHOOP returned 401 after refresh — run `recovery-ledger auth`'}`.
    - When the fetcher returns any other 4xx/5xx (`status !== 200 && status !== 401`): returns `{name, status: 'warn', detail: 'WHOOP returned <code> — scopes may have drifted; check developer.whoop.com/dashboard/applications'}`.
    - When the fetcher throws or `callWithAuth` rejects: returns `{name, status: 'fail', detail: 'roundtrip failed: <err.message or String(err)>'}`. The error message MUST go through the same sanitize pattern as auth.ts (Phase 1 sanitizer covers Bearer / JWT shapes).
  </behavior>
  <read_first>
    - @src/services/doctor/checks/auth.ts (canonical offline-safe probe shape — copy the file-header comment voice, the import block style, the function-export signature; the try/catch + structured-return discipline)
    - @src/services/doctor/checks/token-freshness.ts (the formatDuration-style helper inlining precedent; the probe's `if (state === X) return Y` cascade)
    - @src/services/refresh-orchestrator.ts (the callWithAuth signature + the FetchLikeResponse generic — the probe's fetcher must satisfy this constraint)
    - @src/infrastructure/whoop/resources/profile.ts (the canonical `/v2/user/profile/basic` call via httpGet — the probe's production fetcher wraps THIS call)
    - @src/infrastructure/whoop/errors.ts (WhoopApiError shape — caught + sanitized in the fail arm; Phase 4 D-30 cause-walker already covers this)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.1 (the verbatim function body from research; the executor reproduces it without modification)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-CONTEXT.md §Decisions D-02 #1 + D-03 (pass/warn/fail status mapping; --offline short-circuit)
    - @agent_docs/decisions/0001-mcp-stdout-purity.md (no `console.*` in MCP-reachable code)
  </read_first>
  <action>
    Create `src/services/doctor/checks/whoop-roundtrip.ts` exporting:
      - `interface WhoopRoundtripDeps` with two required fields: `refreshOrchestrator: RefreshOrchestrator` (imported as `type` from `../../refresh-orchestrator.js`), `fetcher: (accessToken: string) => Promise<{status: number; durationMs: number}>`. Comment block above the interface explains the dependency-injection seam: production wiring (in Plan 05-06's runDoctor extension) constructs the fetcher to call `httpGet('/v2/user/profile/basic', {}, WhoopRawProfile)` wrapped in `performance.now()` start/end. The unit test injects a deterministic mock.

      - `async function probeWhoopRoundtrip(deps: WhoopRoundtripDeps, opts?: { offline?: boolean }): Promise<DoctorCheck>`. The body follows the 05-RESEARCH.md §Finding 3.1 cascade verbatim, modified only for sanitize discipline:
          1. If `opts?.offline === true`: return pass with 'skipped (--offline)' detail. Do NOT call the fetcher.
          2. Wrap a `try` block:
             const result = await deps.refreshOrchestrator.callWithAuth(deps.fetcher);
             Branch on `result.status`:
               === 200 → pass + 'profile fetched in <Math.round(result.durationMs)>ms'
               === 401 → fail + 'WHOOP returned 401 after refresh — run `recovery-ledger auth`'   (the backtick around `recovery-ledger auth` is intentional — markdown-style command rendering when the formatter prints; the formatter passes detail strings through verbatim)
               otherwise → warn + 'WHOOP returned <result.status> — scopes may have drifted; check developer.whoop.com/dashboard/applications'
          3. catch (err): return fail + 'roundtrip failed: <err instanceof Error ? err.message : String(err)>'. The wrapped MCP register() at src/mcp/register.ts will sanitize the detail string before returning over the wire; this probe DOES NOT call sanitize() directly (avoid duplicate sanitization; the chokepoint is at the wrapper).

    Imports at the top:
      import type { RefreshOrchestrator } from '../../refresh-orchestrator.js';
      import type { DoctorCheck } from '../index.js';
      import { CHECK_NAMES } from './check-names.js';
    (Do NOT import from src/infrastructure/whoop/* in this file — the production fetcher is constructed in Plan 05-06's runDoctor, not here. Importing infrastructure here would make the unit test require a mock of httpGet; the deps-injection seam keeps the test pure.)

    Comment style: file-header doc-comment in the auth.ts voice (3-5 lines explaining the why + the D-22 / ADR-0002 references), NO inlined grep-gate substrings (per agent_docs/learnings.md convention). No `console.*` anywhere. No default export. Strict TypeScript: no `any`, no implicit `unknown` returns.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/services/doctor/checks/whoop-roundtrip.ts 2>&1 | grep -v "node_modules" | grep -E "error TS|whoop-roundtrip" | wc -l | grep -q '^[[:space:]]*0$'</automated>
  </verify>
  <done>
    File exists with the exact signature above. `npx tsc --noEmit` is clean. No `console.*`, no `fetch(` (Gate F), no `drizzle-orm` (Gate G). Imports are limited to type-only refs from refresh-orchestrator + doctor/index + the value import of CHECK_NAMES.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/services/doctor/checks/whoop-roundtrip.test.ts with 5 test cases</name>
  <files>src/services/doctor/checks/whoop-roundtrip.test.ts</files>
  <behavior>
    - 5 test cases covering: --offline short-circuit, 200 pass with timing, 401 fail with auth-hint detail, non-401 4xx warn (use 403 as the canonical case), network-error fail.
    - All 5 use a MOCK fetcher (the probe's deps.fetcher) and a MOCK refreshOrchestrator (deps.refreshOrchestrator.callWithAuth simply forwards to fetcher in the simple-pass case; rejects with WhoopApiError in the network-error case).
    - Zero real HTTP, zero MSW handlers needed (the test file does not exercise httpGet — that wiring lands in Plan 05-06). ADR-0006 satisfied trivially.
    - Test for the 200-pass case asserts the detail string includes `'profile fetched in'` and a numeric milliseconds value.
    - Test for the 401-fail case asserts the detail string includes the literal `recovery-ledger auth` (the action hint).
    - Test for the network-error case constructs a fake WhoopApiError, asserts the probe catches and returns fail with `'roundtrip failed:'` prefix.
  </behavior>
  <read_first>
    - @src/services/doctor/checks/auth.test.ts (test-file shape precedent; vitest imports + describe/it block layout)
    - @src/services/doctor/checks/token-freshness.test.ts (the duration-asserting test-shape precedent — assertions on formatted strings)
    - @src/services/doctor/checks/whoop-roundtrip.ts (the file just created in Task 1 — the import + interface shape under test)
    - @src/infrastructure/whoop/errors.ts (WhoopApiError constructor signature for the network-error test fixture)
    - @.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-RESEARCH.md §Finding §3.1 "Test seam" paragraph (the 3 mock-fetcher arms)
  </read_first>
  <action>
    Create `src/services/doctor/checks/whoop-roundtrip.test.ts` with one `describe('probeWhoopRoundtrip', ...)` block and 5 `it()` cases. For each case, construct a minimal mock RefreshOrchestrator inline:

      function mockOrchestrator(fakeFetch: () => Promise<{status: number; durationMs: number}>): RefreshOrchestrator {
        return {
          callWithAuth: async (operation) => operation('fake-access-token'),
        } as unknown as RefreshOrchestrator;
      }

    (The `as unknown as` cast is needed because the real RefreshOrchestrator type is generic over T extends FetchLikeResponse; the test fetcher returns a plain object that does not satisfy FetchLikeResponse — but the probe never reads beyond `{status, durationMs}` so the runtime contract is upheld. Document this cast in a one-line comment above each invocation per agent_docs/conventions.md "comments only when the why isn't obvious" rule.)

    Test cases (one per `it()`):
      1. `'returns pass with skipped detail when offline=true'` — invoke `probeWhoopRoundtrip({refreshOrchestrator: mockOrchestrator(() => Promise.reject('should not be called')), fetcher: () => Promise.reject('should not be called')}, {offline: true})`. Assert `{name: 'whoop_roundtrip', status: 'pass'}` and `detail === 'skipped (--offline)'`. The mock fetcher rejecting ensures it was never invoked.
      2. `'returns pass with timing detail on 200'` — fetcher returns `{status: 200, durationMs: 45.7}`. Assert status === 'pass' and detail === 'profile fetched in 46ms' (Math.round of 45.7).
      3. `'returns fail with auth hint on 401'` — fetcher returns `{status: 401, durationMs: 30}`. Assert status === 'fail' and detail includes the literal substring 'recovery-ledger auth'.
      4. `'returns warn on non-401 4xx'` — fetcher returns `{status: 403, durationMs: 25}`. Assert status === 'warn' and detail includes 'WHOOP returned 403' and 'developer.whoop.com/dashboard/applications'.
      5. `'returns fail when callWithAuth throws'` — mock orchestrator's callWithAuth rejects with `new WhoopApiError('network error', {status: 0, message: 'fetch failed'})` (verify the WhoopApiError constructor signature against src/infrastructure/whoop/errors.ts — adapt if the constructor takes different args). Assert status === 'fail' and detail starts with 'roundtrip failed:' and contains the inner message.

    NO `console.*` anywhere (Gate B applies to tests too per scripts/ci-grep-gates.sh; verify Gate B exclusion regex in the script's header). NO real `fetch(` calls (Gate F). NO MSW setup in this test file — the deps-injection seam makes MSW unnecessary; MSW only enters via Plan 05-10 stopwatch test.
  </action>
  <verify>
    <automated>npx vitest run src/services/doctor/checks/whoop-roundtrip.test.ts && bash scripts/ci-grep-gates.sh</automated>
  </verify>
  <done>
    All 5 tests green. Each test case verifies one specific status mapping. The detail strings match the verbatim format documented in Task 1. All 10 grep gates green. No `console.*`, no `fetch(`, no real HTTP.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe → WHOOP API | The probe consumes `services.refreshOrchestrator.callWithAuth` (ADR-0002 single-flight chokepoint) which goes through `httpGet` (Gate F single-call-site). The probe NEVER bare-fetches. |
| Probe error → MCP error path | Any thrown error in the probe is caught + structured as a `fail` DoctorCheck; the MCP `register()` wrapper then sanitizes the `detail` string before serialization (D-21 + D-30). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-I2 | Information Disclosure | `detail` string from a thrown error could embed Bearer / JWT material from a nested cause | mitigate | The Phase 4 D-30 sanitize.ts cause-walker covers WhoopApiError shapes; the detail string is sanitized at the MCP boundary by `register()`. CLI surface uses `String(err)` per the existing precedent in `src/cli/commands/doctor.ts` outer try/catch. |
| T-05-S2 | Spoofing | Mock fetcher in test could be confused with the production fetcher | accept | Strong typing — `WhoopRoundtripDeps.fetcher` has an explicit return shape `{status, durationMs}` that the production fetcher must satisfy; the test casts via `as unknown as RefreshOrchestrator` only because the generic type is wider than the runtime contract requires. |
| T-05-T2 | Tampering | Plan-time-only — no runtime tampering surface introduced | accept | The probe is a pure consumer of upstream services; no writes, no mutable state. |
</threat_model>

<verification>
After both tasks complete:

```sh
# Probe + tests green
npx vitest run src/services/doctor/checks/whoop-roundtrip.test.ts

# No bare fetch, no console, no drizzle, no oauth-token URL in this probe (all gates)
bash scripts/ci-grep-gates.sh

# Type system clean
npx tsc --noEmit

# Probe import path resolves from doctor index (will be wired in Plan 05-06; verify the export is reachable)
node -e "console.log('skip — wiring in 05-06')" || true   # informational only
```
</verification>

<success_criteria>
- `src/services/doctor/checks/whoop-roundtrip.ts` exports `probeWhoopRoundtrip` + `WhoopRoundtripDeps`.
- 5 test cases all green: offline short-circuit, 200 pass, 401 fail, non-401 warn, network-error fail.
- No `console.*`, no bare `fetch(`, no `drizzle-orm` imports in the new file.
- The probe never imports from `src/infrastructure/whoop/*` directly — it goes through the deps-injected fetcher.
- All 10 grep gates green.
</success_criteria>

<output>
Create `.planning/phases/05-doctor-polish-install-guide-20-minute-setup-validation/05-02-SUMMARY.md` when done summarizing: probeWhoopRoundtrip ships with 5 test cases covering the 5 documented status arms. Plan 05-06 will wire the production fetcher in runDoctor.
</output>
