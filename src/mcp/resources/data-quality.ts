// `whoop://data-quality` MCP resource — Plan 04-10 Task 2 will populate.
// Task 1 ships this as an importable stub. See summary-today.ts.

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { Services } from '../../services/index.js';

export function registerDataQuality(_server: McpServer, _services: Services): void {
  // Task 2 fills this in.
}
