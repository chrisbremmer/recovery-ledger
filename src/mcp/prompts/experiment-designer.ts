// `whoop_experiment_designer` MCP prompt — Plan 04-10 Task 3 will populate.
// Task 1 ships this as an importable stub.

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { Services } from '../../services/index.js';

export function registerExperimentDesigner(_server: McpServer, _services: Services): void {
  // Task 3 fills this in.
}
