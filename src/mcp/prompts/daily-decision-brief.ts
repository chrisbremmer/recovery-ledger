// `whoop_daily_decision_brief` MCP prompt — Plan 04-10 Task 3 will populate.
// Task 1 ships this as an importable stub so the runtime attestation test
// can import without breaking the build.

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { Services } from '../../services/index.js';

export function registerDailyDecisionBrief(_server: McpServer, _services: Services): void {
  // Task 3 fills this in.
}
